- Nightly processing error. Observation loop — TypeError: readers.interactions is not a function or its return value is not iterable
- [ ] Review sessions stay "active" in the cockpit forever when the outline marker isn't detected (phase never reaches `done`).
  - **Issue**
    - The interview engine advances phases by string-matching the model's free-text reply: `detectOutline(responseText, config.outlineMarker)` in `src/reviews/interview.ts:238`. The weekly marker is the literal lowercase string `'week in review outline:'` (`src/reviews/weekly.ts:25`).
    - When the model presents the outline without that exact phrase (e.g. "Here's the full outline for approval:" followed by a code block headed "Week in Review — <dates>"), `detectOutline` returns null. The engine treats the outline turn as an ordinary interview message and leaves `phase: 'interview'`.
    - The user's "approve" then arrives while still in `interview`, so it routes to `handleInterview` (not `handleApproval`) and is forwarded to the model. The model, following `SKILL.md`, runs review-writer + post-agents + git commit itself.
    - `runWriteupAndUpdates` is the only code path that sets `phase: 'done'` (`interview.ts:460`), and it never runs on this branch. `getActiveReviewSession` filters only on `phase === 'done'` (`session.ts:35`), so the session shows active indefinitely. `/cancel-review` is the sole escape.
    - Dual orchestration is the deeper problem: both the daemon (`runWriteupAndUpdates`) and the model (per `SKILL.md`) believe they own writeup + post-agents + commit. On marker-detect success the daemon runs them; on failure the model does. The paths can double-write or, as here, strand the session. Confirmed instance: the weekly 2026-05-29 session sat stuck at `phase:"interview"` in `logs/review-sessions.json`.
  - **Fix options** (A is the real fix; C is the safety net)
    - A. Stop scraping free text for phase transitions. Drive interview→approval with a deterministic signal the model emits (a sentinel token, a fenced ` ```outline ` block, or a tool call) and assert its presence instead of matching prose.
    - B. Resolve the dual orchestration — pick one writeup owner. Either the daemon owns `runWriteupAndUpdates` and `SKILL.md` must NOT instruct the model to write the journal / run updaters, or the model owns it end-to-end and the engine just needs a completion signal to flip `phase: 'done'`.
    - C. Add a stuck-session watchdog: mirror `src/jobs/planning-expiry.ts` for review sessions, auto-closing any session left in a non-terminal phase past some age so the cockpit self-heals.
    - D. Detect model-side completion: if the target journal grew or a `"<Type> review: <date>"` commit landed while a session is non-terminal, transition it to `done`.
- [ ] "Claude activity" in the cockpit nav should be updated to "Agent activity" → 18-agent-activity-label
- [ ] Daily journal is not showing weekly goals (May 25, 2026) when there were goals set on the previous Friday (May 22, 2026)
- [ ] Review run after its scheduled date appends the summary to the wrong journal. When a review is completed late, the write-up should land in the journal entry for the day it was due, not the day it actually ran.
- [ ] Remove study from the morning prep and add the "## Notes" section at the end
- [ ] module.register() -> module.registerHooks()
- [ ] Work-run terminal alert gives no "stopped / blocked on a decision" signal — a deliberate hard-stop reads as routine progress.
  - **Issue**
    - The classifier (`src/jobs/work-run-classify.ts:38`) only has `branch-complete | partial | noop | dirty-uncommitted | failed`. A run that hard-stops on a blocker (≥1 commit + tasks remaining) classifies `partial` — the exact same bucket as "ran out of turns mid-progress."
    - The Telegram render for `partial` (`src/transport/telegram-sender.ts:96`) is `📊 <slug> partial · N commit(s), X/Y tasks done`. That reads as progress, not "stopped, needs you."
    - The agent's actual stop reason lives only in the run `transcript.jsonl`; it never reaches Telegram or the cockpit card.
    - Confirmed instance: run `20565e71` (project 10, 2026-06-01) deliberately hard-stopped on the pkms-PII-into-public-repo contradiction, made 1 commit, classified `partial`. The user saw only the commit-poll ping and never learned the run had stopped and was waiting on a decision.
  - **Fix options** (A is the real fix)
    - A. Add a distinct `blocked-on-human` / `stopped-early` outcome the agent signals explicitly (sentinel in its final message), classified separately with a ⚠️ alert that carries the stop reason.
    - B. Regardless of outcome, include the agent's final-message reason on every non-`branch-complete` terminal Telegram alert so any incomplete run carries WHY.
    - C. On the cockpit card + alert, distinguish "made progress, more to do" from "stopped, cannot proceed" (e.g. surface `tasksRemaining` + a blocked flag), so `partial` is never silent about a hard-stop.
  - Related to the `AskUserQuestion` bug below.
- [ ] `AskUserQuestion` in a headless `/work --auto` run auto-denies and silently strands the decision.
  - **Issue**
    - In headless `-p` mode there is no human attached to the run, so an `AskUserQuestion` tool call auto-fails. The question and its options never reach the user (Telegram/cockpit).
    - Best case the agent then hard-stops (as in run `20565e71`); worst case it silently picks a default and proceeds. Either way the decision the agent surfaced is lost.
  - **Fix options** (A is the real fix; C is the safety net)
    - A. Detect `AskUserQuestion` in `--auto`, convert it into a `blocked-on-human` terminal that forwards the question + options to Telegram and pauses the run pending a reply (ties into the blocked-outcome bug above).
    - B. Route the question through the existing intent-layer inline-keyboard approval surface (`8154b3b`) and resume the run on the answer.
    - C. Minimum viable: classify any run that called `AskUserQuestion` as `blocked`, never `partial`/`noop`, so the stranded decision is at least never silent.
- [x] `/work --auto` wedges open AGAIN — the agent emits terminal `result: success` but the process itself never exits (backgrounded test tasks hold it alive), so the `exit`-keyed teardown from the 2026-06-04 fix below never fires. The run sits `running` ~8.5h, quiet-pings on repeat, never auto-merges (plain work-runs have no merge path), and finally mis-classifies as `failed` when killed. _(Follows on from the fixed wedge bug immediately below — same symptom class, different trigger. Confirmed instance: run `d0679453`, project `12-writer-memory`, 2026-06-06.)_ → 15-work-run-finalizer
  - **Issue** (six distinct defects surfaced by one incident)
    - **1. Completion keys off process `exit`/`close`, not the terminal `result`.** The 2026-06-04 fix (below) deliberately keyed teardown off the agent process `exit` rather than the `result` event, because `result` arrives while the agent is still alive. But that leaves a hole: if the agent emits `result: success` and then **never exits**, nothing finalizes. `streamProcess` only sets `done` on `child.on('exit'|'close'|'error')` or cancel (`src/jobs/work-runner.ts:838-871`); `reapTree()` (`:816-830`) is triggered only from `on('exit')` (`:838-844`), so it never ran.
    - **2. Backgrounded tasks keep `claude -p` alive.** The agent ran tests as background Bash tasks; 5 `vitest run` tasks hung (open handles → vitest never exits) and `claude -p` will not exit while background tasks are alive. The agent finished the project at 04:38 (`result: success`, `terminal_reason: completed`) but the process stayed up until a human killed the tree at 13:12:30. The keep-alive ticker (`:673`) kept `lastChildAliveAt` fresh the whole time, so supervision saw "quiet," never "stalled."
    - **3. Recurring quiet ping — `quietNudgedAt` is dropped.** The stall runner stamps `quietNudgedAt` and persists it (`src/jobs/stall-check-runner.ts:70-85`), but the keep-alive heartbeat rebuilds the whole `SupervisedRun` without that field (`src/transport/mutations.ts:32-63, 305-320`) and `upsertRun` **replaces by id, it does not merge fields** (`src/jobs/supervision-store.ts:138-145`). So every 30s heartbeat clears the one-shot guard and the quiet nudge re-fires (`isQuietRun`, `src/intent/supervision.ts:124-138`; 5-min threshold `src/jobs/stall-check.ts:21-26`).
    - **4. Classifier ranks exit-signal above work product.** `src/jobs/work-run-classify.ts:233-248` checks cancelled/signal/missing-code/nonzero **before** branch-complete. The human SIGTERM (exit 143) on a completed run therefore stamped `failed` — `summary.json` recorded `outcome: "failed"`, `reason: "exited with code 143"` with null work-product fields (the classify-error path), even though the branch had all 13 commits and tasks complete. Note: a naive reorder is wrong — it would mis-mark a genuinely user-cancelled run as complete. The fix needs an exit-fact taxonomy.
    - **5. No merge/push/delete-branch finalizer for plain work-runs.** `work-runner.ts` classifies, writes summary/index, destroys the worktree (`finally`, `:556-578`), and fires GC (`:585`) — but never merges or pushes. Only `gen-eval-loop-runner.ts:254-302` (`realMergeBranch`) does merge → push → `branch -d`, and it has review gates (`:618`). Docs confirm plain work-runs are intentionally "branch-complete, not yet on main" (`docs/projects/index.md:192-208`). The project-12 merge that eventually landed was done by a **Jarvis assistant session answering the operator** (commit `5b8dec8`, stamped 13:12:48 — same second this conversation's first message hit `jarvis.log:45807`), NOT by the autonomous system. Left alone, the run would have stayed failed-on-a-branch forever.
    - **6. Recovery/reconciliation relabel but don't finalize.** `recoverRun` only maps stale `running` → `unknown` (`src/intent/supervision.ts:201-203`), persisted by `supervision-recovery.ts:36-55`; `reconcileOrphans` flips mutations to failed/orphaned (`src/transport/mutations-log.ts:43-75`) but does no process/worktree/branch cleanup. A run orphaned across a server restart is never driven to a real terminal state. Startup also runs the orphan-worktree sweep after recovery (`src/index.ts:64`), so a future recovery-finalizer could race away its own evidence.
  - **Provenance.** Two independent investigations (Claude + Codex) plus an adversarial Codex critique of the combined plan. The critique corrected an early proposal to "finalize on `result`" (unsafe — see defect 1 and the 2026-06-04 resolution note below) into a result-seen **watchdog**, and flagged that auto-promotion is a policy change, not a bug fix.
  - **Fix plan** (P0 = correctness, no policy change; P1 = the decided gated auto-merge; P2 = resilience)
    - **P0.1 — Preserve supervision metadata across heartbeats.** Make `upsertRun` field-merge unknown/current fields (or thread `quietNudgedAt` through `buildSupervisedRun`) so a heartbeat can't clear the quiet guard (`src/transport/mutations.ts`, `src/jobs/supervision-store.ts`). Regression test: quiet nudge → keep-alive → no second nudge. *Stops the recurring ping at the source.*
    - **P0.2 — Terminal-result watchdog.** On the `result` event, transition the run out of plain `running`, start a bounded drain window, and reap the tree (SIGTERM → SIGKILL → force-complete) **only if the child doesn't exit on its own** — never kill immediately on `result` (that re-introduces the false `failed` the 2026-06-04 fix avoided). Emit an explicit `reapedAfterTerminalResult` exit fact (`src/jobs/work-runner.ts`). This is option C from the fixed bug below, finally built.
    - **P0.3 — Classifier exit-fact taxonomy.** Distinguish user-cancel vs external-kill vs clean-exit-with-wedged-stdio vs internal-reap-after-terminal-result. A Jarvis-initiated reap after `result: success` with a clean, complete branch must classify branch-complete; a real user cancel stays failed (`src/jobs/work-run-classify.ts`).
    - **P0.4 — Recovery classifies before cleanup.** Make startup recovery (and the restart path) compute work product and drive stale runs to a real terminal state via the same finalizer, and ensure it runs before the orphan-worktree sweep so it doesn't lose its evidence (`src/jobs/supervision-recovery.ts`, `src/index.ts`).
    - **P1.5 — Gated auto-merge finalizer (decided 2026-06-06).** Plain `/work --auto` **will** auto-merge to main, but only through a gate. Build a shared, idempotent, phase-recorded `src/jobs/work-run-finalizer.ts` that owns: classify → verify gate → merge → push+verify → worktree remove → branch delete → terminal writes, as one resumable state machine.
      - **Gate (all must hold, else stop at branch-complete + alert, do not merge):** tests green, working tree clean, `tasksRemaining == 0`, no merge conflict / sane base relationship, no concurrent run owns the branch/project.
      - **Test before mutating main**, not after — run the gate's checks in an integration worktree (or on the branch), so a red result never leaves local `main` altered.
      - **Per-product / base-branch lock**, not per-project — different projects in a product share one `main` (`src/config.ts:250` allows concurrent runs; `supervision-store.ts:10-15` assumes a single writer).
      - **Push before deleting the branch** so origin is the durable backup; record durable phases (`merged-not-pushed`, `pushed-not-deleted`, …) so a crash mid-finalize is resumable by P0.4 recovery.
      - Reuse/refactor `realMergeBranch` (`src/jobs/gen-eval-loop-runner.ts:254-302`) rather than duplicating it; note its existing half-merged push-failure warning (`:274`).
      - Rationale for gated (not unconditional): autonomous runs spawn with `--dangerously-skip-permissions` (`work-runner.ts:361`); the gate is the line between "autonomous" and "lands broken work on main."
    - **P1.6 — Failure/partial/cancelled path.** Always reap the tree + flush transcript/summary; never merge; remove the worktree after preserving forensics OR mark explicit `blocked-on-human`; supervision becomes terminal or intentionally blocked, never quiet-pinging `running`; branch retention/deletion recorded. (Adding a `wrapping-up` status is a deliberate cross-surface change to the status union `src/intent/supervision.ts` + `supervision-store.ts:25` — do it explicitly or model the transient state without a new enum value.)
    - **P2.7 — Backstops independent of agent cooperation.** Quiet→cancel **actuator** (first sustained-quiet notifies; a longer threshold escalates to cancel/reap/finalize instead of nagging forever) + a hard **max-runtime ceiling** that group-kills regardless of liveness. Plus a **cwd-under-worktree process sweep** as a fallback reap for reparented/detached grandchildren the pgid kill misses (scoped to the one worktree path; demoted from the happy path, kept for defense-in-depth).
    - **P2.8 — Regression suite.** Every observed mode (result-before-exit; result-then-reap classifies branch-complete; quiet marker survives keep-alive; store divergence; resume/branch-reuse cleaning ALL of a project's run records) + finalizer-resume-at-each-phase + merge-conflict / push-failure don't-delete-prematurely.
  - **Related:** the fixed wedge bug immediately below (this is its uncovered case + the open option C). The `partial`-vs-`blocked-on-human` classification gap (line 18) and the `AskUserQuestion` auto-deny bug (line 29) share the classifier/terminal-state surface and should land consistently with P0.3 / P1.6.
- [x] `/work --auto` run wedges open after the agent finishes — terminal `result` event never triggers a process-tree reap, so orphaned test runners strand the run as `running` for hours. _(Fixed 2026-06-04 — chose A+B: keyed teardown off the agent process `exit` and reap the process group.)_
  - **Issue**
    - The runner keys run-completion entirely on `child.on('close')` (`src/jobs/work-runner.ts:717`) — the `while (!done …)` generator loop (`:743`) only ends when the child's stdio streams all close. It does NOT key off the agent's terminal `result` event in the stream; that event flows through as an ordinary `output` line.
    - The child is spawned with `stdio: ['ignore', 'pipe', 'pipe']` (`:300`) and no `detached`. Any grandchild the agent spawns (e.g. a `vitest run` via a Bash tool call) inherits those stdout/stderr fds. If a grandchild hangs on open handles, the parent's pipes never close, `close` never fires, `done` stays false, and the generator parks indefinitely even though the agent already emitted `result`.
    - The only teardown path is `ctx.cancel()` → `child.kill('SIGTERM')` (`:746`), which signals the direct child only — not the process group. With no `detached: true` + `process.kill(-pid)`, orphaned grandchildren survive a cancel anyway.
    - Supervision detects the silence but can't fix it: the quiet-run nudge (`src/intent/supervision.ts`, project 11 Phase 4) fires at most once (`quietNudgedAt`, `:130`) and `isStalled` keys on `lastChildAliveAt` — a child-alive-but-silent run is "quiet," never "stalled," so it's never force-terminated. The run sits `running` until a human kills it.
    - Confirmed instance: run `19cd198f` (project 09-expand-cockpit, 2026-06-03). Agent emitted `result: success` at 20:48 CT after 25 commits (Phases 1–3 shipped, Phase 4 tests written), then the tree stayed alive ~3.5h. Four orphaned `vitest run` PIDs (spawned 18:14 / 19:13 / 19:35 / 20:25) held stdio open; the quiet nudge fired once at 21:07, then the run sat `running` with a no-output alert every minute until manually killed — only then did supervision flip it to `failed`.
  - **Fix options** (A is the real fix; C is the safety net)
    - A. Drive teardown off the terminal `result` event, not stdio close. When the stream yields the agent's terminal result, mark `done`, capture exit facts, and actively reap the tree (SIGTERM → grace → SIGKILL) instead of waiting on `close`.
    - B. Spawn with `detached: true` and kill the whole process group (`process.kill(-child.pid, …)`) on both teardown and cancel, so orphaned grandchildren (hung vitest) are always reaped — `:746` currently signals only the direct child.
    - C. Escalate supervision past the one-shot nudge: if a run is quiet (or a `result` was seen) and hasn't closed within N seconds, force-kill the tree and let classification proceed from the captured result — a watchdog mirroring `planning-expiry`, so the cockpit self-heals without a human.
    - D. (defense-in-depth) Prevent the leak at the source: wrap test invocations in the run with a hard timeout / guaranteed-exit flags so a single hung `vitest` can't wedge the whole run.
  - **Resolution (2026-06-04)** — A + B in `work-runner.ts`.
    - **Key off `exit`, not just `close`.** Added a `child.on('exit')` handler that captures the agent's real exit code/signal and reaps. `exit` fires when the agent process itself terminates; `close` additionally waits for every inherited stdio fd to drain — which a hung grandchild blocks forever. Keying off `exit` is also why the run no longer mis-classifies: it records the agent's clean exit (e.g. 0) instead of a SIGTERM, so a completed run isn't force-killed into `failed` (the confirmed 09 symptom). Note: chose the process `exit` event over the doc's "terminal `result` event" — `result` arrives while the agent is still alive, so reaping on it would SIGTERM the agent and re-introduce the false `failed`.
    - **Reap the process group.** Spawn with `detached: true` so the child leads its own group, and reap via `process.kill(-pid, …)` (new `killProcessTree`, indirected behind a test seam). On `exit`: SIGTERM the group, escalate to SIGKILL after `REAP_SIGKILL_MS` (3s), and force the run to complete after `REAP_FORCE_DONE_MS` (10s) if `close` STILL never fires. Orphaned grandchildren keep the leader's pgid, so the group signal reaches them even after the agent exited.
    - **Cancel reaps the tree too.** `ctx.cancel()` now group-kills instead of signalling only the direct child, so a cancelled run no longer leaves orphaned grandchildren.
    - **Tests.** +2 (detached spawn; a wedged run — agent `exit`s but stdio never `close`s — still completes and reaps the group via SIGTERM→SIGKILL).
    - **Still open:** option C (supervision watchdog) and D (hard test timeouts) remain as defense-in-depth; the `partial`-vs-`blocked` classification gap (the line-18 bug) is separate.
- [x] `/work --auto` doesn't resume an interrupted project — every invocation re-forks a fresh branch off `main` and silently restarts the project from Phase 1, duplicating already-shipped work. _(Fixed 2026-06-04 — chose A + D, with a GC guard.)_
  - **Issue**
    - Each run derives a brand-new branch from its own id: `const branch = jarvis-work/${descriptor.id.slice(0, 8)}` (`src/jobs/work-runner.ts:195`). There is no lookup of the project's prior in-progress branch.
    - `createWorktree` is called with that fresh `branch` and **no `startPoint`**, so `git worktree add -b <branch> <path>` forks off **HEAD of the product repo (`main`)** — "no implicit base — git uses HEAD of the repo" (`src/jobs/sandbox-runtime.ts:179` doc + `CreateWorktreeOpts.startPoint`). A run's commits therefore only carry forward if its branch was merged to `main`.
    - When a run ends without merging (killed, `failed`, GC'd, or just left unmerged like every real run so far), its commits stay stranded on `jarvis-work/<old-id>`. The next `/work --auto` forks a clean tree off `main`, reads `tasks.md` with every box unchecked, and concludes the first unchecked task is Phase 1 — restarting from scratch.
    - This is arguably worse than the wedge bug above: the wedge is loud (no-output alerts), but a silent re-fork looks like a healthy run while it redoes finished work and, on approval, could double-write artifacts (new project dirs, duplicate commits).
    - Confirmed instance: run `19cd198f` shipped 25 commits (Phases 1–3 + Phase 4 tests) on `jarvis-work/19cd198f` (tip `a21bc44`), never merged to `main`. After it was killed, a "continue" spawned run `d5373941` off `main` (`9335c1b`) — merge-base with the work branch is `e445c04` (pre-work), `a21bc44` is not an ancestor, all Phase 1–3 build files absent from the worktree. Transcript: agent read `tasks.md`, found it all-unchecked, declared "the first unchecked task is Phase 1's `backlog-parser.test.ts`," and began rewriting Phase 1 tests. The 25 commits were never lost — just ignored.
  - **Fix options** (A is the real fix; D is the cheap guard)
    - A. Resume the project's existing branch. Before creating a worktree, look up the latest unmerged `jarvis-work/*` branch (or a stable per-project branch like `jarvis-work/<slug>`) for this project and either check it out or pass its tip as `createWorktree`'s `startPoint`. Use a stable per-project branch name instead of a per-run id so resumption has a deterministic target.
    - B. Reconcile `tasks.md` against reality, not just the checkbox state: if the forked tree's `tasks.md` is all-unchecked but a prior run's branch shows committed progress, refuse to restart from Phase 1 and surface the conflict.
    - C. On run end, decide branch disposition explicitly — merge to `main`, or persist the branch as the project's resume point and record it in the registry/run index — so "where does the next run continue from" is never implicit.
    - D. (cheap guard) Before spawning, detect an unmerged `jarvis-work/*` branch for the same project whose tip is ahead of `main` and block/alert instead of silently re-forking, so a duplicate-work restart is never silent.
  - **Resolution (2026-06-04)** — A (real resume) with a stable per-project branch, plus a D-style guard built into the resume path and a GC fix.
    - **Stable branch name.** `work-runner.ts` now derives the branch from the project, not the run id: `workBranchName(slug)` → `jarvis-work/<slug>` (was `jarvis-work/<short-id>`). This is the deterministic resume target. Per-project run cap is 1 and the worktree path is already per-project, so only one run is ever on the branch at a time.
    - **Resume in `createWorktree`** (`sandbox-runtime.ts`). When the requested branch already exists (probed with `git show-ref`, chosen over `rev-parse` so it never conflates with the HEAD capture) and no explicit `startPoint` forces a fresh base, the worktree checks the branch out (`git worktree add <path> <branch>`, no `-b`) instead of forking off `main`. The prior commits are present, `SandboxSpec.resumed` is set, and `baseSha` becomes the branch's pre-run tip so the work product is only this run's delta. A non-existent branch keeps the original fresh-cut path.
    - **No restart-from-Phase-1.** On a resumed run the runner prepends a note to the agent prompt: prior commits are present, do not restart, continue from the first incomplete task — killing the exact symptom from run `19cd198f`/`d5373941`.
    - **GC guard** (`work-run-gc.ts`). Because the branch is now shared across every run of a project, GC must not force-delete it when an old run ages out. Phase C now skips any branch still referenced by a retained run; the branch is pruned only once its LAST run ages out.
    - **Tests.** +6 unit tests (resume vs fresh in `createWorktree`, stable branch name + resume-note prompt in the runner, shared-branch protection in GC). Full suite green except one pre-existing, environment-driven `claude.test.ts` `WORKSPACE_DIR` failure unrelated to this change.
- [x] Daily processing shouldn’t include any of the weekly, monthly, quarterly, or yearly reviews as part of the processed content
- [x] The morning prep should include at the top this week’s goals
- [x] Whoop numbers don’t match what’s in the app. I think there’s a date mismatch (pulling recovery and sleep for the day before, not this morning)
- [x] `/work --auto` runs are blocked by the harness permission gate, so every run exits clean with zero work product (silent no-op). Surfaced by project 11 — see `docs/projects/11-work-run-observability/phase-6-diagnosis.md`.
  - **Issue**
    - Validation run `7b8410fb` (project 10, 2026-06-01) classified `noop`: 0 commits, 0 task transitions, clean tree, `exitCode: 0`. Same structural signature as the original `7828477a` / `3b002b26` silent runs that motivated project 11. Blocks the "real/productive run" half of Phase 6 validation.
    - Transcript shows 75 tool calls; the agent did the analysis, then every mutation was refused. Root cause is the spawn site `src/jobs/work-runner.ts:282`, which launches headless `claude -p` with **no permission flag** — no `--permission-mode`, no `--allowedTools`, no `--add-dir`. Default mode prompts for approval on every mutating/non-allowlisted tool, and headless (`-p`, no TTY) there is no approver, so the prompt auto-denies. Three distinct failure modes:
    - **A. Default permission mode denies all mutations (core cause).**
      - `This command requires approval` (~16): `npm install`, `npm ci`, `npm run build`, `npx vitest run …`, `tsx …`, `node -e …`, `ln -s …`.
      - `contains multiple operations. The following part requires approval` (~6): anything piped, e.g. `… 2>&1 | tail -20`, `git show … | head -40`.
      - `Claude requested permissions to write … you haven't granted it yet` (3): the two `Edit` calls on `tasks.md` (why no checkbox could be ticked → 0 task transitions), plus a `Glob` read.
      - `Contains simple_expansion` (1): `echo $PATH`.
    - **B. Allowed working dir is only the worktree.** `cwd: sandbox.worktree` is the sole allowed dir (the `--add-dir` was deliberately dropped, `work-runner.ts:278`), so reaching the parent repo was blocked: `ls …/jarvis/node_modules → "may only list files in the allowed working directories: …/.worktrees/jarvis/10-jarvis-identity-refactor"`; `Glob node_modules @ …/jarvis → "you haven't granted it yet"`.
      - **C. Worktree has no `node_modules`.** `ls node_modules → No such file or directory`. Git worktrees don't carry the gitignored `node_modules`, so there's no local `vitest`/`tsx`. Reaching the parent's was blocked by B; `npm install` / symlink blocked by A. Dead end on tests even if A and B were fixed.
  - **Fix** (all three, in order)
    - A. Spawn with real permissions at `work-runner.ts:282`. The run is a throwaway worktree on a GC'd branch that can't touch `main` — the isolated sandbox `--dangerously-skip-permissions` is built for. Conservative alternative: `--allowedTools` covering `Edit`, `Write`, `Bash(git:*)`, `Bash(npm:*)`, `Bash(npx:*)`. Note `--permission-mode acceptEdits` alone is insufficient — it unblocks `Edit` but still prompts (→ denies) on `git`/`npm` Bash.
    - B. If using the allowlist route (not skip), re-add `--add-dir <PROJECT_ROOT>` so the node_modules symlink target resolves. Moot under `--dangerously-skip-permissions`.
    - C. Make deps available in `createWorktree` (`src/jobs/sandbox-runtime.ts`): symlink the parent's `node_modules` into the worktree (fast) or `npm ci` at setup. Required regardless of A/B or the run can't run tests.
    - D. (defense-in-depth) Fail fast: if the first mutating tool call is denied, classify `failed` with reason `permission-denied` rather than running 428s to a `noop` (depends on project-11 fix #1 surfacing the denial event).
  - **Status (2026-06-01)** — A + B + C implemented in `b3ce3b3`: work-runner spawns with `--dangerously-skip-permissions` (mirrors `execClaude`), `createWorktree` symlinks the repo's `node_modules` into the worktree (`linkWorktreeDeps`), B moot under skip. Unit-tested (77 pass). **D deferred** (defense-in-depth) and **not yet validated by a real productive run** — that's the open project 11 Phase 6 task. Leave this open until a watched run lands real commits.
- [x] Registry doesn't auto-rebuild. Cockpit shows a stale project list until `logs/registry.json` is manually regenerated. _(Fixed 2026-06-03 — chose A + B.)_
  - **Issue**
    - `buildRegistry` and `writeRegistry` exist in `src/intent/registry.ts:127-162` but no code path calls them.
    - `logs/registry.json` was last written by hand on 2026-05-22.
    - `handleApiCockpit` in `src/server/webview.ts:180-214` reads the stale file on every cockpit request.
    - Symptom: new projects don't appear and status changes don't propagate. Projects 09 and 10 were missing until manually rebuilt on 2026-05-27.
  - **Fix options** (minimum viable is A + B)
    - A. Call `buildRegistry` + `writeRegistry` on Jarvis startup. ✅
    - B. Add the rebuild to the nightly job for safety. ✅
    - C. File-watcher on each product's `docs/projects/index.md` for instant rebuild.
    - D. Cockpit endpoint or CLI command to trigger a manual rebuild.
  - **Resolution (2026-06-03)**
    - New `src/jobs/registry-rebuild.ts`: scans `policies/products.json`, reads each product repo's `docs/projects/index.md` (lifecycle status) and each project's `tasks.md` (task progress), and writes a fresh registry.
    - Wired to run on daemon startup (`src/index.ts`) — so the "Restart server" button also refreshes the cockpit — and as a nightly step (`Registry rebuild` in `src/jobs/nightly.ts`, before the journal-intent producer).
    - The registry now carries per-project task progress (`RegistryProject.progress`), so the cockpit renders progress bars for **every** product, not just jarvis. `handleApiCockpit` overlays a live read of jarvis's own `tasks.md` so jarvis cards stay real-time.
  