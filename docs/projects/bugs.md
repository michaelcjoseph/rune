## Active

- [ ] **Orchestrated coder can't reach pkms: rune-kb MCP not registered in the coder's child-sandbox context, so MCP-only tasks fail on a missing deliverable.**
  - **Symptom.** The orchestrated run for product `writing`/`brand` (michaelcjoseph.com, project 01) failed on task `voice-guidelines-copy` (run `b03af189`). Round cap reached with unresolved task feedback. Empty diff. The deliverable (copied voice guidelines under `docs/rune/`) was absent from both the diff and the branch tree-state.
  - **Root cause.** The task's only sanctioned source is pkms `writing/voice.md`, and the project spec hard-gates pkms access to MCP only. The rune-kb MCP tools were not registered in the orchestrated coder's context, so that path was unreachable. The raw pkms/rune-kb daemon on `127.0.0.1:3848` was sandbox-blocked (correctly). Both doors to pkms were shut, leaving no valid way to produce the deliverable.
  - **Not a coder-logic failure.** The coder behaved correctly. It refused direct pkms file access (privacy gate), refused to stub or fabricate the guidelines (the stub-that-passes failure this project exists to prevent), left the `tasks.md` box unticked, and did not force the sandbox-blocked daemon. The orchestration correctly failed on a genuinely-incomplete deliverable instead of merging a fake one.
  - **Why retry won't help.** The blocker is environmental. Re-running the same coder in the same sandbox reproduces the same unreachable-MCP state. The fix belongs to dispatch and child-sandbox configuration, not the coder.
  - **Fix.** Register the rune-kb MCP server in the orchestrated coder's child-sandbox context for runs scoped to `writing`/`brand`. Wire it through the same dispatch surface that injects per-product credentials (`src/jobs/credential-injector.ts` and `policies/products.json`), so the writing run inherits the rune-kb MCP tools alongside its scoped credentials, and Rune's own secrets stay out of the child. Confirm the sandbox egress policy permits the MCP transport to `127.0.0.1:3848` through the sanctioned tool path, not a raw socket.
  - **Fail fast.** Add a dispatch-level precondition that a run whose tasks require pkms-via-MCP verifies rune-kb is registered before dispatching the coder. Fail with a clear `rune-kb MCP not registered` error instead of burning the full round cap on an empty diff.
  - **Blast radius.** Any orchestrated task whose only sanctioned data path is pkms-via-MCP hits this, not just `voice-guidelines-copy`. `writing-ideas-migration` (Phase B) has the same dependency and will fail the same way. Phase B cannot complete until this lands.
  - **Verification.** Re-run project 01 (product `writing`/`brand`). `voice-guidelines-copy` should fetch `writing/voice.md` through rune-kb MCP, write the copied guidelines under `docs/rune/`, produce a non-empty diff, and reach a terminal committed state. Confirm no direct pkms file reads occurred (MCP-only path held).
  - **Related, but distinct.** Separate from the context-curator missing-section no-op (project 01 `context.md`, resolved 2026-07-10). Separate from the command→pipeline seam bug (`/blog` and `/writing-critique` omit `deps`). This is a third, independent blocker on the same product line.

## Loop-filed

(empty)

## Done

- [x] Per-task closeout gate ran the whole repo suite with the merge gate's 10-minute budget. **(Fixed 2026-07-10 — products now select `vitest-related` or `product-commands` closeout policy; Rune and Rune-MCP run argv-safe `npx vitest related --run --passWithNoTests` over the NUL-delimited tracked-plus-untracked diff against `HEAD`, excluding deletions, with a dedicated 120-second `WORK_RUN_CLOSEOUT_COMMAND_TIMEOUT_MS`. Other products preserve their configured commands under the short budget. The shared validation runtime retains isolated cache injection, bounded output, timeout diagnostics, process-group reaping, and active-process tracking. Coder self-validation and the final merge gate remain full-product `validationCommands` under the unchanged 10-minute gate budget. Covered by config rejection/default tests, changed-path and injection regressions, closeout repair/parking tests, and a real two-source/two-test related-versus-full-suite fixture.)**

- [x] Work-run worktrees symlink `node_modules`, so Vitest cache writes landed in the shared tree — sandbox-denied and coupled across runs. **(Fixed 2026-07-10 — Rune now derives an opaque SHA-256 cache directory under the OS temp root from each absolute worktree path and forces `RUNE_VITEST_CACHE_DIR` into scoped agent, legacy work-run, closeout, and merge-gate validation environments after inherited/product values. `vitest.config.cjs` uses that value as Vite's top-level `cacheDir`, while bare local runs retain `node_modules/.vite`; CommonJS avoids Vite's separate ESM config bundle under `node_modules/.vite-temp`. Normal/idempotent destruction, clean preserved-worktree reclaim, reconciliation rollback, integration-gate teardown, and orphan cleanup remove only the matching cache best-effort; parked worktrees keep theirs until reclaim/destruction. Covered by derivation/opacity/distinctness, env precedence/spawn propagation, cleanup lifecycle, config-load, focused Vitest, and concurrent-worktree acceptance.)**

- [x] Gate/closeout validation timeouts capture no diagnostic for a pre-collection vitest wedge — the failure is invisible. **(Fixed 2026-07-10 — validation commands now keep bounded 20K output head and tail captures and, when a durable run directory is supplied, inherit Node report-on-`SIGUSR2` options. On timeout Rune signals the entire process group, waits one second for report flush, then performs the existing `SIGTERM`/`SIGKILL` reap. Raw reports land in a temporary directory; before persistence Rune removes the inherited environment, scrubs secrets and host paths, and writes the sanitized JSON plus a command/head/tail text artifact under `<run>/validation-diagnostics/`. Closeout failures also persist labeled head/tail sections and diagnostic filenames. Live, recovery, release, legacy, and orchestrated merge gates all pass their run directory. A real-process regression test wedges before application output and verifies the durable report contains a JavaScript stack and libuv handles before the child is reaped.)**

- [x] Update default chat model to be gpt-5.6-terra and reflect it as a clickable button option in the chat box. **(Fixed 2026-07-10 — `DEFAULT_CHAT_MODEL` and `CONVERSATION_MODEL` now default new Telegram, cockpit Home, and product chats to `gpt-5.6-terra`; the Home selector exposes Terra as the selected default and synchronizes to an existing webview session, product chat exposes the same clickable command, and `/gpt-5.6-terra` works on every chat surface. A provider-neutral chat adapter resolves aliases through model policy: Claude keeps its persistent CLI sessions, while Terra uses a persistent `codex exec` thread and `codex exec resume`; non-chat Codex callers remain ephemeral. Session persistence now records the real executor/thread id, migrates legacy sessions as Claude, resets provider state on cross-provider switches, and bootstraps the new provider from the bounded Rune transcript so visible context survives. Codex chat preserves global read-only/product-write postures, product env scrubbing, voice, operation tracking, and cancellation. `/fresh` summarizes Codex chats from Rune's durable transcript. Covered by adapter, Codex args, session migration, handler, closeout, selector, and product-chat tests; live two-turn Terra acceptance returned `READY` then resumed the same thread and recalled `cobalt`.)**

- [x] Orchestrated (and legacy) work-run Start fails for every product whose repo is not Rune's — `validate()` resolves the project against `PROJECT_ROOT` and throws away `payload.product`, so `Start failed: project not found: <slug>` fires before any worktree exists. **(Fixed 2026-07-10 — both appliers now use one shared product-aware project resolver: explicit products resolve from their configured `repoPath`, legacy product-less payloads retain the Rune `PROJECT_ROOT` fallback, and apply-time discovery uses the same repository-root `docs/projects` semantics inside the worktree. `scopePath` was resolved deliberately as content/chat scope rather than lifecycle-project placement, consistent with registry rebuild/scaffolding and pinned with the shared-repo `writing` fixture. Unknown products and malformed product config fail closed with distinct safe reasons. The per-project cap is now keyed by normalized `(product, projectSlug)`, so same-slug projects in different products no longer collide. Covered by a real temporary external git repo, both applier validators, apply-time lookup, scope semantics, config failures, and concurrency regressions; `npm run build` and full `npm test` (309 files / 5166 tests) pass. Direct production-config validation of `brand` / `01-rune-writing-product` returns `{ok:true}`.)** **(surfaced by an orchestrated Start on product `brand`, project `01-rune-writing-product`, 2026-07-10; systemic — both work-run appliers, not brand-specific)**

  - **Severity:** High
    - Orchestrated Start is unusable for every external product: `aura`, `assay`, `relay`, `writing`, `brand`.
    - It appears to work for `rune` and `rune-mcp` only by coincidence: their `repoPath` IS the Rune checkout, so `PROJECT_ROOT` happens to equal the correct base. The bug is invisible until a run is pointed at a product with its own repo.
    - Rejection happens in `validate()`, so there is no worktree, no run row, and no transcript to inspect. The operator sees only the Start error.

  - **Repro**
    - Cockpit → product `brand` → project `01-rune-writing-product` → Start (orchestrated).
    - Observe: `Start failed: project not found: 01-rune-writing-product`.
    - The project exists and is complete on disk:
      `~/workspace/michaelcjoseph.com/docs/projects/01-rune-writing-product/{spec.md,tasks.md,test-plan.md,context.md}`.

  - **Root cause**
    - `orchestratedWorkApplier.validate()` (`src/jobs/orchestrated-work-runner.ts:1193`) calls
      `findProjectDir(projectSlug, PROJECT_ROOT)`.
    - `PROJECT_ROOT` (`src/config.ts:35`) is Rune's own checkout, derived from `import.meta.url`. It is never the product's repo.
    - `findProjectDir(slug, base)` (`:523-540`) scans `join(base, PROJECTS_SUBDIR)`, where `PROJECTS_SUBDIR = docs/projects` (`:119`), matching `name === slug || name.endsWith('-' + slug)` (`:537`).
    - So the lookup scans `~/workspace/rune/docs/projects` (`01-mvp` … `23-note-triage`) and misses.
    - The payload carries the product — `type OrchestratedWorkPayload = { projectSlug: string; product?: string }` (`:124-127`) — and the cockpit Start action sets it. `validate()` shape-checks it against `VALID_SLUG` (`:1190-1192`) and then never uses it to choose a base.
    - The `spec.md` existence check on the next line (`:1195`) resolves against that same wrong directory, so for external products it is unreachable dead weight.
    - `apply()` gets it right at `:1302` (`findProjectDir(projectSlug, sandbox.worktree)`, with its own distinct reason string `project not found in worktree`, `:1305`). Validate rejects first, so the correct code path never runs.
    - Identical defect in the legacy `/work --auto` applier: `src/jobs/work-runner.ts:243` (validate, `PROJECT_ROOT`) versus `:383` (apply, `sandbox.worktree`), with its own copy of `findProjectDir` at `:186`.

  - **Fix shape**
    - Resolve the projects base from `policies/products.json` for `payload.product`:
      - base = `repoPath`, joined with `scopePath` when the product declares one.
      - `readProductsConfig(config.PRODUCTS_CONFIG_FILE)` (`src/jobs/sandbox-runtime.ts:175`) already tilde-expands `repoPath` and carries `scopePath` on `ProductConfig`.
      - `src/vault/sessions.ts:133` is the established precedent for the join:
        `repoPath && scopePath ? join(repoPath, scopePath) : repoPath`.
    - Fall back to `PROJECT_ROOT` only when `product` is absent, preserving today's behavior for rune-scoped payloads.
    - Apply the same change to `work-runner.ts:243`. Prefer hoisting the duplicated `findProjectDir` plus the new base resolver into one shared helper over editing two divergent copies.

  - **Things to watch**
    - **Two appliers, one bug.** Fixing only the orchestrated path leaves `/work --auto` broken in exactly the same way, for exactly the same reason.
    - **`scopePath` is honored nowhere in either runner.** `rg scopePath` over `orchestrated-work-runner.ts` and `work-runner.ts` returns nothing. `writing` shares `michaelcjoseph.com` with `scopePath: docs/rune`, so:
      - Fixing validate to use `repoPath` alone unbreaks `brand` (which has no `scopePath`) and leaves `writing` quietly broken.
      - Even after validate is fixed, `apply()`'s `findProjectDir(projectSlug, sandbox.worktree)` also ignores `scopePath`, so `writing` breaks a second time one layer down. Both call sites need the join.
      - **Open question, do not guess:** neither `michaelcjoseph.com/docs/rune/projects` nor `docs/rune/docs/projects` exists today. Decide where a scoped product's projects live before writing the join, or the fix will target a path that has never existed.
    - **`validate()` is synchronous and must not throw.** `readProductsConfig` throws on an unreadable or malformed file (`sandbox-runtime.ts:180`). Wrap the call and return `{ ok: false, reason }`; do not let an exception escape into the mutation pipeline.
    - **Fail closed on an unknown product.** If `payload.product` names a product absent from `products.json`, return a distinct reason. Silently falling back to `PROJECT_ROOT` reproduces the exact misleading `project not found` this bug is about.
    - **The per-project concurrency cap ignores `product`** (`:1201-1206` filters on `projectSlug` alone). `worktreePathFor(product, project, worktreeRoot)` (`src/intent/sandbox.ts:120`) *is* product-scoped, so the comment above the filter ("they share the deterministic per-project worktree path") no longer justifies a product-blind cap. Both `rune` and `aura` own an `01-mvp`; starting one while the other runs returns `already running for 01-mvp`. This is currently unreachable for external products precisely because validate rejects earlier, and it becomes reachable the moment this bug is fixed. Re-key the cap to `(product, projectSlug)` in the same pass.
    - **The test suite actively hides this.** `work-runner.test.ts`'s `setupValidProject` (`:258`) mocks `readdirSync`/`statSync` so `findProjectDir` succeeds "in BOTH the live tree (used by validate's pre-flight) and inside the worktree" — the fixture answers for both roots, so validate's wrong base is never observable. A fix that leaves that fixture alone keeps the suite green while production stays broken. `orchestrated-work-runner.test.ts:127` has the same shape.

  - **Acceptance**
    - Cockpit Start on `brand` / `01-rune-writing-product` creates a worktree and produces a run, instead of failing in validate.
    - A test starts a project for a product whose `repoPath` is not the Rune repo, against a real temp git repo, and validate resolves it. No such case exists in the suite today; the mocked fixtures above are why.
    - Validate returns a distinct, non-misleading reason for an unknown product and for a malformed `products.json`.
    - `/work --auto` validates on the same external project.
    - `scopePath` semantics are decided and pinned by a test, with `writing` as the fixture.

  - **Scope**
    - Single-repo, product `rune`. Touches `src/jobs/orchestrated-work-runner.ts`, `src/jobs/work-runner.ts`, and their tests.
    - No michaelcjoseph.com changes required. Brand's project directory is already correct on disk.

- [x] Writing `/blog` and `/writing-critique` never invoke the pipeline — commands omit `deps` _(Fixed 2026-07-10 — the seam is closed: both commands now dispatch through the mutation pipeline and the tested-but-unreachable engine runs in production. Four operator decisions (2026-07-09) shaped the build: dedicated `writing` MutationKind + applier (substrate reuse, not direct wiring, not forced through orchestrated-work); runtime target stays product `writing` (michaelcjoseph.com — the bug's "single-repo" scope constrains the FIX code to the rune repo, which held: zero site work bundled); model calls reconnect the orphaned project-12 writer machinery (SOUL on the system channel via a new `askClaudeOneShot` opts bag, memory fence on the user turn, voice flag, policy-resolved `writer` role → opus, ONESHOT_MODEL fallback for non-claude-format resolutions); both commands wired, with a minimal core critique mode. **What shipped:** `writingBranchName`/`writingSlugFromBranch` siblings in `src/intent/sandbox.ts` (literals deduped); core pipeline critique mode (`input.critique {slug, outputPath, revisionRequested}` + optional `readArtifact` dep — critiques the EXISTING `docs/rune/{slug}.md` into `docs/rune/critiques/{slug}.md`, optional revision, both committed on the draft's own branch; slug PRE-DERIVED by the caller so a path-shaped target can never fork the branch — a latent orchestrator/core slug divergence found during planning); `src/jobs/writing-run-deps.ts` — production `StartWritingProductRunDeps`: `createWorktree` on the writing product (`rune-writing/{slug}` create-or-resume), IN-PROCESS dispatch to the real `vault_search`/`journal_range`/`follow_wikilinks` handlers with input translation (the pipeline's call shapes predate the handlers: `{maxDays}` → `{startDate,endDate}`, `{query}` → `{text}`), writer one-shots per stage, containment-asserted artifact io, branch-verified git commit (the vault/writer commit helpers are main-only and untouched); every dep records its first failure and polls cancel at stage boundaries, and the deps-level adapter THROWS the recorded stage failure on a `failed` core result so the orchestrator's generic "did not commit" guard never masks the real reason (the core swallows stage errors by design). `src/jobs/writing-run-runner.ts` — `writingRunApplier` (kind `writing`, autoApprove): writing-repo preflight (missing checkout → friendly terminal), one-run-per-slug guard (shared worktree path), pipeline states bridged to run-feed log lines (`publishDerivedRunEvent` gate extended to `writing`), terminal `completed {outcome:'branch-complete', commitSha, routePath}` / `failed` (scrubbed), worktree destroyed in finally on success AND failure (committed work lives on the branch; a kept dirty tree would wedge the slug), and a load-bearing 30s keep-alive/activity ticker — the quiet→cancel backstop is NOT kind-gated and would system-reap a run silent inside a long one-shot model call. Commands ack immediately with branch + short id; `TelegramSender` gained a ✍️/💥 writing terminal formatter (the generic fallback read "✅ /work --auto finished"); `input.sender` (never read) dropped from the orchestration input; `KNOWN_MUTATION_KINDS` accepts `writing`. Safety verified during planning: `cleanupOrphanWorktrees` is registration-based and work-run GC prefix-guards `rune-work/` — a `rune-writing/` branch/worktree is never swept or force-deleted (flip side: nothing GCs abandoned writing branches — free refs, same accepted tradeoff as the GC fix). Covered by `writing-run-deps.test.ts` (MCP routing/translation, SOUL/voice/model wiring, fence unwrap, containment, adapter failed→throw, plus a REAL temp-git create/commit/resume integration test), `writing-run-runner.test.ts` (validate matrix, event order, scrubbed stage-attributed failures, cancel, destroy-always, preflight, slug guard, ticker), critique-mode + planted-private-marker-in-critique-notes tests in `writing-pipeline-core.test.ts`, run-feed gate pin in `mutations.test.ts`, TG formats in `telegram-sender.test.ts`, and rewritten command tests. Docs synced (`module-reference.md` — five new writing entries + stale blog.ts entries fixed, `subsystems.md` applier #6, CLAUDE.md mutation-pipeline bullet + command table). `npm run build` + full `npm test` (308 files / 5158 tests) green. Requires a Rune restart to register the applier. Live acceptance (the bug's own criteria + project-01 Phase A unblock): `/blog <topic>` from TG/cockpit → ack → `writing:` state lines in the run feed → ✍️ terminal with branch @ sha → `git -C ~/workspace/michaelcjoseph.com log rune-writing/<slug>` shows `Publish writing page: <topic>` touching only `docs/rune/<slug>.md`; the planted-private-marker sanitizer now guards a live path. Known limits: cancel is cooperative at stage boundaries (an in-flight model call runs to its own timeout); resume with an identical regenerated artifact fails clean as "no changes".)_
  - **Severity:** High
    - The live writing flow is a no-op.
    - Every user-facing entry point exists and looks wired, but `/blog <topic>`
      creates no branch, drafts nothing, commits nothing, and sends no reply.

  - **Provenance**
    - The project-19 Phase 6 writing engine landed on `main` (merge `b1ff45f`).
    - Implementations are real, not tests-only. This is the one integration seam
      left open.
    - Also the remaining blocker on michaelcjoseph.com's
      `01-rune-writing-product` project (its Phase A dependency).

  - **Repro**
  - Trigger `/blog some-topic` from Telegram or the cockpit.
  - Observe, all absent:
    - No `rune-writing/some-topic` branch.
    - No `/rune/{slug}` artifact.
    - No state transitions in the run feed.
    - No message back to the user.

  - **Root cause**
  - `startWritingProductRun` is overloaded (`src/jobs/writing-product-orchestration.ts:182-190`):
    - `(input)` returns only a `WritingProductRunPlan`, early-returning at `:194`
      (`if (!deps) return plan;`).
    - `(input, deps)` runs the real pipeline (`:198` `createWritingWorktree`,
      `:203` `runWritingPipeline`).
  - Both production callers invoke the plan-only overload with no `deps`:
    - `src/bot/commands/blog.ts:15`
    - `src/bot/commands/writing-critique.ts:43`
  - Consequences:
    - `runWritingPipeline` (`src/jobs/writing-pipeline.ts:200`) has zero
      production importers. It runs only under test mocks.
    - `createWritingWorktree` has no implementation at all.
      - Exists only as an interface field (`writing-product-orchestration.ts:91-95`).
      - Satisfied solely by `vi.fn()` in tests.
    - `input.sender` is accepted (`:79`, `:87`) but never read, so even the
      plan-only path stays silent to the user.
  - Privacy enforcement is correct but unreachable:
    - `sanitizePublicMarkdown` / `collectPrivateSourceBoundaries`
      (`writing-pipeline.ts:133-198`), applied before commit at `:265`.
    - Planted-marker negative test (`writing-pipeline-core.test.ts:212-299`).
    - Both guard a path that never executes in production.

  - **Fix shape**
  - Build a production `StartWritingProductRunDeps`.
    - `createWritingWorktree`: real git worktree create-or-resume for
      `rune-writing/{slug}`.
      - Reuse the existing orchestrated substrate so credential isolation and the
        egress allowlist apply for free:
        - `worktreePathFor(product, project, worktreeRoot)` (`src/intent/sandbox.ts:95`)
          is generic. Reusable as-is.
        - `buildSandboxEnv(sandbox, { productsConfigPath })`
          (`src/jobs/credential-injector.ts`) is the standard scoped-env builder.
          See `src/jobs/gen-eval-loop-runner.ts:355` for the reference call.
      - **Known friction:** `workBranchName()` (`src/intent/sandbox.ts:82`)
        hardcodes `rune-work/${slug}`. Writing's contract needs
        `rune-writing/${slug}`. Parameterize the prefix or add a sibling helper.
        Do not fork the worktree logic to get around this.
      - Ship it with a real test, not just mocks.
    - `runWritingPipeline`: wrap `writing-pipeline.ts:200` and construct its deps.
      - MCP client for pkms source reads.
      - The plan / draft / critique / revise model calls.
      - `writeArtifact`, `commitArtifact`.
      - `emitRunState` bound to the run feed.
  - Update both commands to pass `deps`.
    - `src/bot/commands/blog.ts`
    - `src/bot/commands/writing-critique.ts`
    - Use `input.sender` to report state transitions and the terminal result.

  - **Design decision to make (the crux)**
    - Route the writing run through the standard orchestrated work-runner, or
      build a bespoke writing path?
    - Recommendation: the work-runner. It reuses sandbox isolation, credential
      injection, and the `npm run build` validation gate. The bespoke path
      reimplements all three.
    - Decide before implementing. The branch-prefix friction above is the one
      known cost of the reuse path, and it is cheaper than the reimplementation.

  - **Acceptance**
  - `/blog <topic>` end to end:
    - Creates or resumes `rune-writing/{slug}`.
    - Drives states `researching → drafting → critiquing → revising →
      ready-for-review → committed`.
    - Commits the pages.
    - Messages the user the terminal state.
    - This is exactly project 01's Phase C `user-reachability-check`.
  - The planted-private-marker negative test path executes in the live flow.
  - `createWritingWorktree` ships with a real implementation and a real test.

  - **Scope**
  - Single-repo, product `rune`.
  - File and run as a rune-scoped bug or one-task project.
  - Do NOT bundle with michaelcjoseph.com site work.
    - That is the exact cross-repo failure that stranded project 19 Phase 6.
- [x] Work-run GC force-deletes an incomplete project's `rune-work/<slug>` resume branch when its run dirs age out of the 3-run retention window — resurrecting the 2026-06-04 re-fork bug, so the next run re-forks off `main` and restarts the project from task 1. **(surfaced by the project-21 orchestrated run that restarted at task 1, 2026-07-08; systemic — work-run GC, not project-21-specific)** _(Fixed 2026-07-09 — shipped fix direction 1 (operator-decided: guard only; direction 2's project-state coupling deliberately not built — accepted tradeoff: an abandoned project's unmerged branch is never pruned, a leaked git ref, essentially free). Phase C of `gcWorkRuns` (`src/jobs/work-run-gc.ts`) now proves a branch merged before the `git branch -D`: `git rev-list --count <baseBranch>..<branch>` in the run's product repo, and anything short of a provable `0` keeps the ref — unmerged (`>0`), unparseable count, and errored rev-list all skip the prune with a `keeping unmerged work-run branch` / `unmerged check failed; keeping branch` warn log (fail-safe polarity: a kept ref is free, a deleted one re-forks the project from task 1; a nonexistent branch errors into the same skip — the old path's "branch prune failed" no-op, one step earlier). Base branches thread from product config via a new optional `GcWorkRunsOpts.productBaseBranches` (built in `work-run-gc-runner.ts` alongside `productRepos`; a missing entry defaults to `'main'`, matching `readProductsConfig`'s parse-time default); run summaries are deliberately NOT consulted — `baseBranch` is only stamped on merged terminals, absent in exactly the incomplete-run case the guard protects. Dir-retention caps and the retained-branch guard are unchanged — the new check is what makes the resume point durable once every run dir ages out. Covered by `work-run-gc.test.ts`: unmerged branch survives total age-out (the bug's pin — red before the fix), rev-list-error + unparseable-count fail-safes, per-product base-branch threading (`develop..` assertion) with `main` default, merged-branch prune preserved, plus a REAL temp-git integration test (`defaultRunGit` against an actual repo: an unmerged closeout commit survives a `maxRuns: 0` pass; after `git merge` the next pass prunes the ref). Docs synced (`module-reference.md` work-run-gc/gc-runner entries). `npm run build` + full `npm test` (306 files / 5104 tests) green. Live signal: the next GC pass over an aged-out incomplete project logs `keeping unmerged work-run branch` instead of issuing `branch -D`, and a resumed run continues from the branch tip instead of restarting at task 1. The upstream problem — project 21 repeatedly ending `dirty-uncommitted`/`failed` and never merging, which is what let its run count blow past the retention window — remains separate and open.)_
  - **Issue**
    - The `rune-work/<slug>` branch is a project's durable resume point: `createWorktree` resumes an existing branch (`git worktree add <path> <branch>`, no `-b`, `src/jobs/sandbox-runtime.ts:374-393`) so prior task closeouts survive across runs; only when the branch is absent does it take the FRESH path and cut a new one from `main`'s HEAD (`:394-421`), where `tasks.md` is all-unchecked. `selectNextTask` (`src/intent/orch-task-select.ts`) then returns the first unchecked `- [ ]`, i.e. task 1.
    - Work-run GC treats that durable branch as a disposable run artifact. `gcWorkRuns` (`src/jobs/work-run-gc.ts`) retains only the newest terminal run DIRS under GLOBAL caps — `WORK_RUN_RETENTION_MAX_RUNS = 3` / `WORK_RUN_RETENTION_MAX_BYTES = 200MB` (`src/config.ts:341-342`, passed at `work-run-gc-runner.ts:69-76`) — deletes the rest oldest-first, and force-deletes each pruned run's branch in Phase C (`git branch -D`, `work-run-gc.ts:252`). The caps are global across ALL products and projects, so a project's run dirs age out fast when other work interleaves.
    - The existing GC guard (`work-run-gc.ts:210-233`) — added as the "GC guard" half of the 2026-06-04 re-fork fix (see the Done entry "`/work --auto` doesn't resume an interrupted project… re-forks a fresh branch off `main`") — is incomplete. It builds `retainedBranches` only from run dirs STILL ON DISK and skips deleting a branch a retained run references, so the branch is protected only while ≥1 of that project's run dirs survives the 3-run window. Its own comment names the failure it is meant to prevent ("re-introduce the re-fork bug in a new form"), but it has no notion that the branch is an incomplete project's live resume point: once every one of that project's run dirs ages out, the guard lapses and the branch is force-deleted with all its unmerged task closeouts. An incomplete project that needs many attempts (project 21 took 7+) is exactly the case that blows through a 3-run cap.
    - The `-D` (force) is deliberate — run branches are "intentionally never merged into the checkout" — but that assumption is only safe for a COMPLETED project whose work already landed on `main`. For an in-flight project the branch carries real unmerged progress, so force-delete silently discards it.
    - **Confirmed instance:** on `21-parallel-product-chats`, `logs/rune.log` at `2026-07-08T22:13:58.840Z` records `gcWorkRuns` issuing `git branch -D rune-work/21-parallel-product-chats` (tied to aged-out run `6f57193f-407e-4672-8f07-613f0e9382e5`, the 20:01→21:38 run); the command reports "branch not found" because an earlier GC pass in the same window had already succeeded (successful prunes are not logged, only failures warn). Main advanced to `89533e3` at 22:17:42; the next run found no branch, cut fresh from `89533e3` (branch reflog: "Created from 89533e3…", one lone closeout commit `9caa700` at 22:51), read an all-unchecked `tasks.md`, and restarted at task 1 (`session-scope-key-helper`) before failing again. No project-21 run ever merged (all 7 ended `dirty-uncommitted`/`failed`), so `main` carries zero ticks and the branch was the only record of progress.
  - **Fix direction** (operator decision 2026-07-09: direction 1 only)
    - 1. **Never force-delete an unmerged resume branch** (primary, self-contained — **shipped**). Before the Phase C `git branch -D`, check `git rev-list --count <baseBranch>..<branch>`; if the branch has commits not on `baseBranch`, skip the prune and keep the ref. This directly retires the "run branches are never merged so `-D` is always safe" assumption, which is exactly wrong for an incomplete project. Pin with a test: a project branch carrying one unmerged commit survives a GC pass that evicts ALL of that project's run dirs.
    - 2. **Tie the resume branch's lifecycle to the PROJECT, not the run-dir count** (alternate / complement — **not built**). Only prune `rune-work/<slug>` once the owning project reaches a terminal state (done/abandoned) in the registry; run dirs (transcripts/forensics) keep GC'ing freely under the 3-run cap. More correct conceptually, but couples GC to project state.
    - Note this is separate from the upstream problem that project 21 keeps ending `dirty-uncommitted`/`failed` and never merges, which is what let its run count climb past the retention window in the first place.
- [x] The test-intent gate can veto QA's tests but cannot repair them — on a tech-lead rejection the feedback threads back to QA and the same agent/model retries against a stable blind spot until the round cap trips and the whole orchestrated run fails. Give the gate a corrective action: the tech-lead patches the tests directly on first rejection. **(surfaced by the project-21 orchestrated run `6f57193f`, 2026-07-08; systemic — team-task workflow, not project-21-specific)** _(Fixed 2026-07-08 — shipped the decided approach on both layers. **Workflow** (`team-task-workflow.ts`): on the first `test-intent` rejection the loop invokes a new optional `techLeadRepairTests` dep before the QA bounce — a `repaired` result is re-reviewed by `techLeadReviewTests`, and approval breaks straight to the coder (QA is never re-rolled); `not-repaired` or a throwing dep degrades to the QA bounce with the repair outcome appended to `actionableNotes`. The `tl-test-review` verdict gained an optional `repairable` flag (`false` = structural rework / spec ambiguity ⇒ skip the repair, straight bounce; absent ⇒ attempt — a failed repair falls back safely). Repair is attempted once per task, never for no-code-test rationales; `ORCHESTRATED_ROUND_CAP` stays as the unchanged backstop. The attempt emits a `test-repair` activity event (run-feed visible) and lands as `TaskEvidence.testIntentRepair` on every terminal, including the outer-catch `failed` path. **Production seam** (`team-task-deps.ts`): an `execute('tech-lead', models.techLead)` worktree session (the opus binding is claude-format + coding-capable; `execute`'s role param widened), then mechanical enforcement — the repair delta is computed against a pre-repair `git add -A && git write-tree` snapshot (NOT the executor's returned diff, which is scrubbed and includes QA's uncommitted work); any delta path outside `*.test.ts(x)` is reverted on disk (`git restore --source=<preTree>` / `git rm -f`) before closeout's `git add -A` could ever commit it; then **confirm-red** runs the product `validationCommands` (new `runRepairValidation` seam, `WORK_RUN_GATE_COMMAND_TIMEOUT_MS`): a green run rolls the whole patch back (`not-repaired`, "vacuous or behavior-pinning"), as does a timeout, while a red run threads command/exit/bounded-scrubbed `outputTail` into the re-review body as `Confirm-red evidence` so the tech-lead judges red-for-the-right-reason; `lastQaDiff` refreshes to the patched cumulative diff (same scrub/redact as the execution agent) so the re-review and `qaRevalidateDiff` stay consistent, and returned `testIds` merge QA's with the surviving delta. Every internal failure returns `{kind:'not-repaired'}` — the repair can never fail the task. New `TeamTaskSeams` members `runGit`/`runRepairValidation` default to `defaultRunGit`/`runValidationCommands`; deps-test fixtures get a fail-deterministic throwing `runGit` so no fixture reaches real git. No runner changes needed (`validationCommands`/`cap`/`emit` already thread through `createProductionTaskWorkflowRunner`). Covered by `team-task-workflow.test.ts` (repair success skips the QA retry; not-repaired/re-review-reject/throw bounce with truthful notes; `repairable:false` + rationale skip; once-per-task; cap backstop; `test-repair` + dual `role-verdict` events) and `team-task-deps.test.ts` (snapshot-fail spends no executor call; delta guard reverts violations and keeps the surviving patch; all-violations/green/timeout roll back; skipped red-check on empty commands; testId merge; confirm-red evidence in the next review body, cleared on fresh QA tests; `repairable` parse; plus two REAL temp-git integration tests proving on-disk restore/rollback). Docs synced (`project-lifecycle.md` C2 gate c + repair paragraph, `subsystems.md`, `module-reference.md`). `npm run build` + full `npm test` (305 files / 5075 tests) green. The optional complements below remain unbuilt — the planning-time completeness lint is the natural next pick if task-6-shaped gaps recur. Live signal: the next orchestrated run whose tech-lead rejects a test intent shows `tech-lead: test-repair` in the run feed and proceeds to the coder instead of triple-bouncing into a failed run. (Amended 2026-07-08 after a codex code review found a BLOCK-severity guard hole in the first cut: the allowlist also accepted any path in `qa.testIds`, and `qa.testIds` is `filesFromDiff(QA's diff)` — every path QA touched, not only test files — so a QA stray into product source (e.g. `src/prod.ts` in QA's diff) would have silently licensed the tech-lead repair to edit that same source, violating the shipped tests-only contract. Fixed by narrowing `isAllowedRepairPath` to the `*.test.ts(x)` regex alone — a repair needing a non-test-file change is structural by definition, which is exactly the QA-bounce case — and filtering the repair prompt's `## QA test files` list to guard-editable paths so a stray is never advertised as editable. Pinned by three new tests in `team-task-deps.test.ts`: a QA-stray source path in the delta is reverted while the test patch survives, a stray-only delta rolls back to not-repaired even when `qa.testIds` lists the path, and the repair prompt omits non-test paths.))_
  - **Issue**
    - The QA-first test-intent loop (`src/intent/team-task-workflow.ts:379-429`) is: QA authors the task's tests (`deps.qaWriteTests`) → the tech-lead judges them BEFORE the coder starts (`deps.techLeadReviewTests`) → on `approved` it breaks to the coder; on a fail it builds `GateRejectionFeedback` (reason + `suggestedChangeNotes(tlTests.suggestedChange)` as `actionableNotes`), threads it back to **QA**, and re-runs QA up to `ORCHESTRATED_ROUND_CAP = 3` (`src/jobs/orchestrated-work-runner.ts:122`). At `qaAttempt === cap-1` it `block()`s the task with `loopExitReason: 'hard-budget'`, which `mapResultToTerminal` renders as a `failed` orchestrated terminal (`orchestrated-work-runner.ts:2169-2173`, `orchestration blocked on "<task>": <reason>`).
    - The tech-lead's test review is **verdict-only** — `techLeadReviewTests` returns `{approved, notes, suggestedChange}` and writes no files (`src/jobs/team-task-deps.ts:329-336`). So the only actor who can change the tests is QA, and the gate has veto power without repair power.
    - Two things make this dead-loop-prone, and both fired on the confirmed instance: (1) **the actor who knows exactly what is missing is not allowed to fix it** — the tech-lead wrote a precise rejection but had to route it through QA's hands; (2) **retries are identical, not escalating** — rounds 2 and 3 re-invoke the same QA agent at the same model with accumulated notes, so a stable blind spot reproduces every round. The concrete-feedback layer already shipped (see the 2026-07-06 "Reviewer and tech-lead feedback was diagnostic-only" Done entry, which added `suggestedChange`), so feedback *quality* is not the gap — routing is.
    - **Confirmed instance:** orchestrated run `6f57193f-407e-4672-8f07-613f0e9382e5` on `21-parallel-product-chats` (2026-07-08 20:01→21:38 UTC). Tasks 1–5 landed (5 commits). Task 6 `unread-activity-cue` failed at the test-intent gate: tech-lead `role-verdict` = `test-intent`/`fail` at 21:28:12, 21:33:14, and 21:38:11, then `gate-rejection` at 21:38:47 and the run classified `failed`. Every rejection named the same gap — QA's tests covered the raise/clear/isolation cases but never sent a scoped frame for the *currently-viewed* product to assert that foreground output raises **no** cue, so a buggy impl that badges the actively-read chat would pass green. QA never closed it across three rounds.
    - This is the general form of the principle already stated in the removed "Confirm red" analysis and the closeout-repair-loop fix: a gate that can only say "no," against a state no subsequent turn reliably fixes, converges to a whole-run failure rather than a recoverable stop.
    - **Interim mitigation already applied** (not the systemic fix): on `rune-work/21-parallel-product-chats`, commit `622d38f` strengthened the `unread-activity-cue` task line and added a matching 🔴 line to test-plan §4 so the *resumed* run's QA is forced to pin foreground suppression. That unblocks the one task; it does nothing for the next task-6-shaped gap on any project.
  - **Decided approach** (operator decisions 2026-07-08)
    - 1. **Tech-lead repairs on first rejection.** When the tech-lead rejects QA's test intent, instead of bouncing feedback to QA it patches the tests itself (adds/adjusts the missing assertions) and re-reviews to approval. Scope the repair to bounded gaps — add or adjust assertions, the common case and exactly this bug; keep the QA-bounce path only when the tests need structural rework or expose a spec ambiguity the tech-lead cannot resolve alone. This makes the gate terminate in approve or approve-after-patch, never in "reject an unfixed state N times."
    - 2. **Independence tradeoff accepted.** The tech-lead becomes author+judge for the patched test slice. Downstream reviewer independence is sufficient (operator decision — no QA re-ack step): the coder's implementation must still pass the tech-lead-authored assertion, and the later `implementation-diff` / `reviewer-verdict` gates run under the distinct-provider reviewer-independence rule (`team-task-workflow.ts:340-368`).
  - **Implementation notes**
    - `techLeadReviewTests` is verdict-only today; the repair needs a tech-lead write path into the QA test files in the run worktree (new dep/tool), guarded to `*.test.ts` under the task's target paths so the gate cannot rewrite product source.
    - Preserve test-first: a patched assertion must be red against the not-yet-written implementation. Run the product `validationCommands` (or the task-scoped tests) after the patch to confirm red-for-the-right-reason before approving, so the gate cannot approve a vacuous/green test.
    - Keep `ORCHESTRATED_ROUND_CAP` as a backstop; with first-rejection repair the loop should rarely bind it.
  - **Optional complements** (noted, not decided)
    - Escalate-don't-repeat: if any QA retry path remains, change something on repeat (bump the QA model/context) rather than re-rolling identically — the escalation-policy framework already exists (`src/intent/escalation.ts`).
    - Planning-time completeness lint: enforce that every conditional qualifier in `test-plan.md` ("backgrounded", "only when", "unless") gets both a positive and a negative assertion line. This is the durable prevention — the upstream miss here was test-plan §4 omitting the negative case.
- [x] Orchestrated recovery can't resume a run whose first task hasn't closed out — the resumable cursor is only written at task-closeout. **(orchestrated-run restart safety 2/2 — resumability; the task-start-cursor half)** _(Fixed 2026-07-08 — shipped fix options A+B+C; the restart-safety series is complete. **(A) Task-start cursor** — `runProjectOrchestration` now writes a cursor right after task selection, before the workflow runs (`persistTaskStartCursorSafely` → `buildRunCursor(..., currentTask)`, `src/intent/project-orchestrator.ts`): a non-null `cursor.currentTaskId` marks the in-progress task as untrusted; closeout cursors keep `currentTaskId: null` and `persistRunCheckpoint` keeps its fail-closed contract. The start write is deliberately **best-effort** (commitWipSafely precedent): a failed write must not hold a run that has done no work — proceeding risks only the pre-cursor status quo (shutdown park / orphan). Both cursor validators already accepted a string `currentTaskId`; no consumer changes. **(B) came nearly free** — recovery never consumed cursor position anyway: it re-derives from tasks.md + records, and `reconstructRun` already returns `{completedTaskIds: [], nextTask: <first unchecked>, drift: false}` for empty records, so the interrupted task (checkbox still unticked) is re-selected and re-run from scratch through the full role workflow (QA-first → coder → gates → closeout validation). What B actually needed was the **restart salvage**: the recovery redispatch reuses the worktree exactly as the interrupted run left it, and closeout stages `git add -A` — the interrupted task's uncommitted dirt would have been silently absorbed into the next closeout commit (`verifyCleanWorktree` runs after the commit and cannot catch it). The apply recovery branch (the shared funnel for boot recovery and cockpit Recover) now best-effort WIP-commits a dirty tree first (`rune(<product>): WIP — restart salvage — <project>`, via `commitWorktreeWip` — generalized from the shutdown parker's `commitShutdownWip`), so the re-run starts clean and the half-work stays inspectable on the branch. **(C)** drift guard unchanged. **Composition with 1/2:** the shutdown parker parks only non-resumable runs, so it degenerates to the rare fallback (start-write failure, or the seconds-wide pre-first-selection window); a graceful restart mid-task now auto-resumes, and a SIGKILL/power-loss mid-task is now resumable too (the cursor is already on disk; salvage commits the dirt at resume). Verified end-to-end against the real cursor store + real `reconstructRun` + real recovery gates: the exact confirmed-instance state (task-start cursor, zero records, zero ticked boxes) yields `resumed:[id]` re-dispatching from the interrupted task — previously `orphaned`. Covered by `project-orchestrator.test.ts` (start-cursor-before-workflow ordering + shape, per-iteration refresh with closeout `currentTaskId: null`, best-effort throw tolerance with closeout fail-closed pins untouched), `orchestrated-work-runner.test.ts` (dirty-recovery salvage sequence lands before the orchestration re-runs; clean recovery makes no commit), and `orchestrated-work-recovery.test.ts` (mid-first-task boot recovery re-dispatches instead of orphaning). Docs synced (`subsystems.md`, `module-reference.md`). `npm run build` + full `npm test` (305 files / 5051 tests) green. Live confirmation signal: a production restart during an early task logs `resumed:[<id>]` at boot with a `WIP — restart salvage` commit on the run branch.)_
- [x] Process restart during an orchestrated run discards the run and its uncommitted work — `shutdown()` is orchestration-unaware. **(orchestrated-run restart safety 1/2 — durability; the park-on-shutdown half)** _(Fixed 2026-07-08 — shipped fix options A+B with one operator-confirmed narrowing: shutdown parks only runs boot recovery could NOT resume, so a resumable-cursor run keeps today's automatic boot-resume instead of degrading to a human-gated release. Three pieces. **(1) Suppression** — `setMutationShutdownInProgress` (`src/transport/mutations.ts`), armed by `shutdown()` BEFORE `killActiveProcesses()`: while set, `startApply` suppresses terminal/crash persistence and heartbeat upserts for orchestrated-work mutations, `persistTerminalMutationState` returns immediately, and the apply `finally` skips `destroyWorktree` — closing the race where the SIGTERM'd child surfaces during the 5s grace window as a failed terminal + worktree teardown that would clobber the park (or a resumable run's `running` state). Other mutation kinds keep their crash semantics. **(2) Parker** — `parkInFlightOrchestratedRuns` (`src/jobs/orchestrated-work-runner.ts`), called after `waitForActiveProcesses()` (children dead → stable trees): system-cancels each active orchestrated run; resumable-cursor runs are left `running` for boot recovery; no-cursor runs (the confirmed instance's mid-first-task window) resolve branch/worktree deterministically (`workBranchName` + `worktreePathFor`, baseBranch from product config), best-effort WIP-commit a dirty tree (`rune(<product>): WIP — shutdown park — <project>`), and park via `writeRecoveredTerminalMutation` — completed+`parked:true` terminal + `blocked-on-human` supervision row with `operatorWorktreePath`, branch + worktree preserved, visible-and-releasable in the approvals surfaces after restart. A missing worktree is skipped (nothing to preserve); a per-run failure never aborts parking the rest. **(3) Wiring** — `shutdown()` re-entrancy guard + `{parked, resumable, skipped}` summary log. Boot side needed no changes (verified): a parked mutation is `completed` → invisible to `recoverOrchestratedWorkRuns` and `reconcileOrphans`; `blocked-on-human` is durable through supervision recovery, protected from the orphan-worktree sweep (git-registered) and work-run GC (non-terminal); the standard release path works on it (the WIP commit leaves the tree clean → cold-finalize gated-merge, which holds branch-complete with tasks remaining). The **durability ceiling** stands: no shutdown handler runs on SIGKILL/power-loss — that's the still-open resumability half above (task-start cursor), which composes with this parker unchanged. Covered by `orchestrated-shutdown-park.test.ts` (park shape, resumable skip, clean-tree park, missing-worktree skip, best-effort WIP, per-run isolation), `mutations.test.ts` (suppression: terminal + crash + non-orchestrated kinds unaffected), `orchestrated-work-runner.test.ts` (apply skips persistence, preserves worktree), and `index-startup-recovery.test.ts` (SIGTERM ordering: arm → kill → wait → park). Docs synced (`subsystems.md`, `module-reference.md`). `npm run build` + full `npm test` (305 files / 5045 tests) green. (Amended 2026-07-08 after a codex code review found a BLOCK-severity race in the composition: the suppressed applier's `startApply` finally still deleted its handle from `activeRuns`, and the parker's default deps snapshot `activeRuns` only after `waitForActiveProcesses()` — a fast unwind during the kill/wait window could hide exactly the no-cursor run the parker exists to save, leaving it `running` with no cursor → orphaned at next boot. Fixed by RETAINING a shutdown-suppressed orchestrated run's handle in the finally (the process is exiting; retention leaks nothing) — ordering-independent, unlike a pre-kill snapshot. Pinned by a new production-composition test in `orchestrated-work-runner.test.ts` (real `startApply` unwind under suppression → real default `listActiveRuns` + `writeTerminal` still park the run; verified red without the guard) and updated retention assertions in `mutations.test.ts`.))_
- [x] Operator report 2026-07-07: a long multi-clause `kb_query` ("What has been discussed about updating the depository contracts… Include notes from conversations with Peter Watts, George Roman, and Julien Genestoux.") "timed out" via the Claude App while a short keyword retry worked. _(Closed 2026-07-07 — **two distinct findings.** (1) The reported timeout was NOT length-related: the long question left zero server-side trace (no `Querying KB` log line, and the post-restart boot's metrics show exactly 2 kb_query calls — the two successful short ones). It was sent on a stale MCP session right after the 19:58 daemon restart (sessions are process-local; OAuth tokens survive, sessions don't) and hit `mcp-transport.ts`'s then-silent 404 `Unknown MCP session`; the App surfaced that as a tool timeout, and by the retry it had re-initialized. That 404 now emits a warn log (`Unknown MCP session rejected`), pinned by `mcp-transport.test.ts`. (2) The report DID expose a real latent length defect: the long question's term alternation matched 543/1035 wiki files and candidates were taken as rg's first-10 in directory-walk order — measured: `restaurant-industry-economics`, `nuclear-power-renaissance`… while `peter-watts.md`/`george-roman.md`/`julien-genestoux.md` all exist and never made the list. Fixed with ranked retrieval: `rankWikiPages` (`src/kb/search.ts`) scores pages by weighted distinct-term coverage (per-term `rg -il`, weight = 1/files-matched — rare terms dominate), `searchInFiles` pulls snippets from the ranked set, and every quoted line is capped at `MAX_SNIPPET_CHARS` = 300 (giant single-line JSON/journal matches can no longer balloon the prompt). Live-verified: the exact failing question now answers in 42s with dated notes from the julien-genestoux/george-roman pages; the short query stays at 34s. Covered by `search.test.ts` (rare-vs-generic weighting, coverage accumulation, type filter, caps, vault-containment) and `query.test.ts` (ranked-path wiring, stopword fallback to raw-question search, snippet clipping). Docs synced (`module-reference.md` `query.ts`/`search.ts`/`mcp-transport.ts`).)_
- [x] The MCP daemon never warms the vault index at boot — for up to 15 minutes after every restart, `kb_query` broad retrieval falls back to a cold full-vault ripgrep scan instead of the warm index. **(kb_query speedup 3/3 — daemon-path retrieval)** _(Closed 2026-07-07 — **already implemented; the entry was filed from a misread of `daemon.ts`.** `startMcpDaemon` schedules a boot-time `buildVaultIndex()` via `setImmediate` immediately after the HTTP surface binds (`src/mcp/daemon.ts:200-207`, cleared in `stop()`), alongside the 15-minute `refreshVaultIndex` freshness interval — landed in commit `3e6a699` ("daemon-warm-index-lifecycle", project 19 W1) with full test coverage in `daemon.test.ts` (health served before the build, `buildVaultIndex` called once after the `setImmediate` flush, background-not-awaited source contract, timer/immediate teardown on stop). The entry's root cause cites only `daemon.ts:132-140` and greps for a boot `refreshVaultIndex()` — missing the differently-named `buildVaultIndex` boot call at `:200`. Live confirmation on the running daemon: `/health` reports `warmIndex.ready: true` with `buildMs: 261` — the full 4,326-file / 515K-line index builds in ~¼s, so the only real cold window is that sub-second build, by design (the non-blocking startup contract). No code change; the kb_query speedup series is complete: 1/3 + 2/3 shipped on this branch, 3/3 pre-existing.)_
- [x] [team-loop] security/high @ src/security.ts:42 — token material can be written to disk without redaction (finding finding-token-disk-write · task close-the-objection) _(Closed 2026-07-07 — **invalid**: `src/security.ts` does not exist in this repo and never has (`git log` has no trace of it); the finding id and task name (`close-the-objection`) match team-loop test-fixture shapes — synthetic data that leaked into the ledger, not a real finding against Rune.)_
- [x] [team-loop] data-integrity/medium @ src/jobs/recovery-finalize-runner.ts:243-245 — The widened guard now forces outcome=branch-complete on a project-marked-done resume whenever classifyOutcome would not say branch-complete. The new test asserts tasksRemaining=1 and a real merge/push/delete still fire — confirming the intended behavior: a project-marked-done flip is authoritative over the absolute unchecked-task scan. Risk noted but reversible: if a run reaches project-marked-done WITHOUT the index flip actually having landed, this would push a branch the task scan reads as partial. The test's rev-list fixture (projectDone3 commit present) and the existing same-base-branch-lock test cover the landed-flip path; the not-yet-landed case is not directly exercised, but the recorded-phase contract treats project-marked-done as post-flip by construction. (finding finding-1hs0kch · task extend-finalizer-crash-recovery-recovery-finalize-runner-must-treat-project-mark) _(Closed 2026-07-07 — reviewed-accepted: the finding's own text confirms intended behavior; residual not-yet-landed-flip risk is excluded by the recorded-phase contract. A dedicated negative test for that case remains unwritten — pick it up as a task if wanted.)_
- [x] [team-loop] data-integrity/medium @ src/jobs/recovery-finalize-runner.ts:243-256 — The widened predicate resumeGatedMerge (now covering resumeAfterProjectDone) forces outcome=branch-complete and sets gateTasksRemaining=0 on any project-marked-done resume, regardless of whether the index flip commit actually landed. This is coherent with the recorded-phase contract: project-marked-done is post-flip by construction, so treating it as authoritative over the absolute unchecked-task scan is the intended fix and matches the existing same-base-branch-lock test. Residual risk (a run reaching project-marked-done without the flip landing) is reversible and not directly exercised, but is excluded by the phase contract; the new test pins the landed-flip path with a real merge/push/delete and tasksRemaining=1 preserved in the summary. (finding finding-djrjv9 · task extend-finalizer-crash-recovery-recovery-finalize-runner-must-treat-project-mark) _(Closed 2026-07-07 — reviewed-accepted; duplicate of finding-1hs0kch above, same disposition.)_
- [x] [team-loop] data-integrity/medium @ src/jobs/recovery-finalize-runner.ts:251-256 — gateTasksRemaining=0 is forced on any project-marked-done resume (resumeAfterProjectDone) independent of whether the index-flip commit actually landed. This is coherent with the recorded-phase contract — project-marked-done is post-flip by construction — and the override correctly separates the merge-gate input (0) from the preserved summary work-product facts (transitions.tasksRemaining stays at boot-time value). The new test pins both halves: runGate receives tasksRemaining:0 while lastSummary.workProduct.transitions.tasksRemaining===1. Residual risk for a not-yet-landed flip is reversible and excluded by the phase contract, but remains unexercised by a dedicated negative test. (finding finding-narc56 · task extend-finalizer-crash-recovery-recovery-finalize-runner-must-treat-project-mark) _(Closed 2026-07-07 — reviewed-accepted; duplicate of finding-1hs0kch above, same disposition; the unwritten negative test is the one actionable residue, noted there.)_
- [x] [team-loop] concurrency/medium @ src/jobs/work-run-reconciler.ts:reconcileTerminalWorkRunsOnce — Per-row upsertRun re-reads supervised-runs.json per write, narrowing the race to a single upsert so a concurrent heartbeat on a sibling row survives; matches the store's existing file-based last-writer-wins model and is pinned by the 'preserves supervision updates to other rows' test. Residual single-upsert TOCTOU is consistent with how the live supervisor already writes this store — no contract divergence. (finding finding-qi0fe8 · task periodic-reconciler-test-a-supervised-entry-still-running-whose) _(Closed 2026-07-07 — reviewed-accepted: the finding's own text concludes the behavior matches the store's existing model with no contract divergence.)_
- [x] [team-loop] data-integrity/high @ launchd/com.jarvis.rune-mcp.plist:8-9 (WorkingDirectory) — WorkingDirectory is `rune/.worktrees/rune/19-rune-product-os` — a RELATIVE path that also points at the ephemeral build worktree. launchd requires an absolute WorkingDirectory; a relative value will not resolve to the repo, and `npm run mcp:start` will fail to find package.json. Worse, the worktree path is deleted once this project finalizes, so even if launchd tolerated it, the always-on daemon would be permanently chdir'd into a non-existent directory. The install script `cp`s the plist verbatim (no WorkingDirectory rewrite), so this broken value is baked into ~/Library/LaunchAgents on install. This defeats the task's whole point (a working, bootstrap-able plist) and the manual/live gate would hit it as an opaque launch failure. Fix: hardcode the canonical absolute repo path (the rune checkout on main, e.g. an absolute `/Users/jarvis/.../rune`), matching the absolute style already used for the log paths. (finding finding-1qizrao · task launchd-plist-and-install-produce-the-second-launchd-job-com-jarvis-rune-mcp-pli) _(Closed 2026-07-07 — **fixed before landing**: the plist at HEAD carries the absolute `WorkingDirectory` `/Users/jarvis/workspace/rune`; `git log -S 'rune/.worktrees' -- launchd/` is empty, so the relative value never reached main — these findings were filed against the task's in-flight worktree diff, and the closeout commit `731b040` landed the corrected file.)_
- [x] [team-loop] data-integrity/high @ launchd/com.jarvis.rune-mcp.plist:8-9 — WorkingDirectory is the relative string 'rune'; launchd requires an absolute path, so chdir cannot resolve to the repo, npm run mcp:start cannot find package.json, and the always-on daemon fails to launch. The install script cp's the plist verbatim into ~/Library/LaunchAgents, persisting the broken config. Confirmed authored (sibling log paths absolute/intact) and fails the diff's own QA assertion toMatch(/^/). Fix: hardcode the absolute rune-on-main checkout path. (finding finding-8e6703 · task launchd-plist-and-install-produce-the-second-launchd-job-com-jarvis-rune-mcp-pli) _(Closed 2026-07-07 — duplicate of finding-1qizrao above; fixed before landing, absolute path at HEAD.)_
- [x] [team-loop] data-integrity/high @ launchd/com.jarvis.rune-mcp.plist:8-9 — WorkingDirectory is the relative string `rune`; launchd requires an absolute WorkingDirectory, so chdir resolves against launchd's default cwd (/), `npm run mcp:start` cannot find package.json, and the always-on daemon fails to launch. The install script cp's the plist verbatim into ~/Library/LaunchAgents with no path rewrite, persisting the broken config. The same plist's log paths are fully absolute, so this is the authored value, not a transport artifact, and it fails the deliverable's own QA assertion toMatch(/^\//). Fix: hardcode the canonical absolute rune-on-main checkout path. (finding finding-elpwqz · task launchd-plist-and-install-produce-the-second-launchd-job-com-jarvis-rune-mcp-pli) _(Closed 2026-07-07 — duplicate of finding-1qizrao above; fixed before landing, absolute path at HEAD.)_
- [x] `kb_query` runs as an agentic multi-turn retrieval loop (read index → read 5–15 pages → grep → synthesize) when the retrieval is already deterministic and fast — it should be a single-pass retrieve-then-synthesize (RAG) call. **(kb_query speedup 2/3 — biggest structural win)** _(Fixed 2026-07-07 — `queryKB` is now single-pass RAG. Retrieval happens entirely in-process: the question is distilled to a stopword-stripped term alternation (`searchPatternFor` — live verification exposed that the raw question string almost never matches page text literally, so the old deterministic layer rarely resolved candidates at all), the top candidates' full bodies are pre-fetched into the prompt (`MAX_PREFETCH_PAGES` = 8, 8k/page, 48k total), and the kb-query agent synthesizes in ONE turn with NO tools. Tool-lessness is real, not honor-system: the SOUL frontmatter now declares the inline form `tools: []`, which `loadAgentDef` flags as `AgentDef.noTools` and `runAgent` forwards inside the `--agents` JSON — the CLI strips the subagent's entire toolset at the availability level (verified live: agent self-reports NONE), effective under `--dangerously-skip-permissions` where `--allowedTools` is not a hard boundary. The SOUL was rewritten as a synthesis-only agent (answer from provided context; name relevant candidate/index pages when coverage is missing). No `maxTurns` cap — the CLI has no such flag, and zero tools already forces one turn. The tiered tool-enabled escalation pass stays unbuilt (optional in the bug; the term-alternation recall fix removed most of its motivation). Live-verified end-to-end against the real vault: "Who is Paul Graham?" resolves candidates → pre-fetches bodies → cited single-pass answer in 21s; a no-coverage question degrades gracefully via the index excerpt in 7s (previously: 100% of calls blew past 30s). Covered by `query.test.ts` (term alternation + stopword fallback, bodies with per-page/total/page-count caps, failed-read degrade, single-pass prompt pins, real-SOUL `tools: []` pin) and `claude.test.ts` (`tools: []` → `noTools` parse, agents-JSON embedding, `--allowedTools` omitted). Docs synced (`module-reference.md` `query.ts` + `claude.ts` entries). Requires MCP daemon restart; unlocks the Haiku synthesis tier as future tuning.)_
- [x] `kb_query` latency is dominated by the agent reading the entire 740KB `knowledge/index.md` (~185K tokens) at the top of every query — the deterministic filtered search already resolves the candidate pages, so the full-index read is wasted work paid on every call. **(kb_query speedup 1/3 — highest single lever)** _(Fixed 2026-07-07 — `queryKB` (`src/kb/query.ts`) now pre-resolves the candidate set for the agent: the deterministic `searchWithFilter` hits are grouped by distinct page, each page's `- [[slug]] — summary` row is looked up from `knowledge/index.md` in-process (an ~ms `readVaultFile`, vs the agent's ~185K-token Read), and the prompt carries a "Pre-resolved candidate wiki pages … do NOT read knowledge/index.md" block with per-page index summaries + matched snippets. When the deterministic search finds nothing, the prompt instead carries a bounded index excerpt (`MAX_INDEX_FALLBACK_CHARS` = 20k, keep-the-start, marked when truncated); an unreadable/missing index degrades to no block (the SOUL falls back to Grep/Glob over `knowledge/wiki/`). Both step-1 instructions were rewritten to consume the injected candidates: the `queryKB` prompt workflow and the SOUL (`.claude/agents/kb-query.md`) — tools/model frontmatter unchanged. Removes ~185K input tokens and the index-read round-trip from every call; pairs with speedup 2/3 (single-pass RAG), which builds on this candidate resolution. Covered by `query.test.ts` (candidate block with index summaries + multi-match grouping, fallback excerpt, truncation cap, unreadable-index degrade; existing snippet/broad-search/timeout pins unchanged); `evals/kb-query.yaml` pins no workflow step. Docs synced (`module-reference.md` `query.ts` entry). Requires MCP daemon restart; post-deploy signal is `kb_query` p50 dropping in `rune-mcp-metrics-history.jsonl`.)_
- [x] A timed-out `kb_query` (or any agent-spawning MCP tool) orphans its Claude child for up to 30 minutes — the wrapper detaches the execution promise on timeout but never aborts the process, and the agent inherited the 30-min default timeout. _(Fixed 2026-07-07 — `QueryKBDeps` gained `agentTimeoutMs` (threaded as `runAgent`'s `timeoutMs` arg), and the MCP `kb_query` handler (`src/mcp/server.ts`) passes `KB_QUERY_AGENT_TIMEOUT_MS = 150_000` on both branches (daemon broad-search and admin stdio) — mirroring `generate_workout`'s 210s-under-240s `MCP_AGENT_TIMEOUT_MS` precedent. `execClaude`'s own timer SIGTERMs the child when the agent timeout fires and returns `Claude timed out after 150s`, which surfaces as a clean `isError` result under the 180s wrapper ceiling — no orphan lingers. Telegram `/kb` passes no deps and keeps the `CLAUDE_TIMEOUT_MS` (30-min) default. The "ideally also" generic abort in `instrumentMcpTool` was deliberately deferred: it needs a change to the MCP tool-callback contract (or an AbortSignal param on `runAgent`), and with both agent-spawning tools now sized under their wrapper ceilings no orphan path remains; it stays as possible future hardening for the next agent-spawning tool. Covered by `query.test.ts` (agentTimeoutMs threads to runAgent's 3rd positional arg) and `server.test.ts` (both daemon-path deps captures assert `agentTimeoutMs: 150_000`; the admin-stdio assertion pins the deps bag). Docs synced (`subsystems.md` metrics paragraph, `module-reference.md` `query.ts` entry). Requires MCP daemon restart to take effect.)_
- [x] `kb_query` MCP tool times out on 100% of calls — it spawns the `kb-query` Claude agent but is missing from `TOOL_TIMEOUT_OVERRIDES_MS`, so the wrapper kills every call at the 30s global default before the agent can answer. _(Fixed 2026-07-07 — added `kb_query: 180_000` to `TOOL_TIMEOUT_OVERRIDES_MS` (`src/mcp/metrics.ts`), mirroring the `generate_workout: 240_000` precedent; the ceiling sits above real query latency (including the slower daemon path through `daemonBroadSearch`) and leaves room for the paired orphaned-agent fix to size the agent-side timeout (~150s) underneath it. `metrics-timeout.test.ts` gained a kb_query override block (survives past the 30s default and a shorter env value, times out at 180s with `isError`); the non-overridden-tool exemplar swapped from `kb_query` to `vault_search`. Docs synced (`configuration.md`, `module-reference.md`, `subsystems.md` — the "only current override" mentions now list both entries). The running MCP daemon must be restarted to pick up the change; post-deploy signal is `kb_query` errors/timeouts dropping off 100% in `rune-mcp-metrics-history.jsonl`. The orphaned-agent bug above stays open — a wrapper-timed-out call still leaves the agent running toward its 30-min inherited default until that fix threads an explicit agent timeout under this ceiling.)_
- [x] A `held` orchestrated terminal with a preserved worktree blocks the project's next Start indefinitely and has no release affordance. **(surfaced by the 2026-07-07 codex code review of the closeout repair-loop fix; pre-existing for finding/operational holds)** _(Fixed 2026-07-07 — fix direction (b): `createWorktree` now self-heals at the exact failure point. When the target path exists and is a git-REGISTERED worktree with a clean `git status --porcelain`, it is reclaimed (`git worktree remove --force` — cleanliness verified ourselves, `--force` only bypasses git's ignored-file refusal for the node_modules symlink) and the normal fresh/resume flow proceeds; preserved work is already committed on the branch, which survives removal and is checked back out by the resume path. A DIRTY preserved worktree (e.g. a finding-hold's uncommitted coder diff or a human's manual fix) fails closed with an actionable commit-or-discard error — uncommitted work is never auto-destroyed. An unregistered existing dir keeps the original orphan throw (the boot sweep's job, where the error's `cleanupOrphanWorktrees()` advice actually applies). Option (a) — a cockpit release/cleanup affordance for held rows — remains possible future polish; with (b) the operational trap (Start blocked forever) is gone. Covered by `sandbox-runtime.test.ts` (clean reclaim removes-then-adds in order, dirty refusal removes nothing, unregistered-dir throw preserved); docs synced (`project-lifecycle.md` held row); `npm run build` and full `npm test` passed.)_
- [x] Closeout failure is terminal and destructive — a red suite hard-blocks the orchestration and discards the diff (0 commits, worktree GC'd). Make closeout failure a bounded coder repair loop that preserves the work. **(defense-in-depth for when green still slips past the coder loop)** _(Fixed 2026-07-07 — `runCloseoutChecks` now returns a structured `CloseoutCheckResult` carrying the already-scrubbed failing command/exit/`outputTail`; on failure the orchestrator threads it back to the coder as qa-attributed `GateRejectionFeedback` (`implementation-diff`, 4k keep-the-end tail + drive-green actionable notes) and re-runs the whole task workflow up to `CLOSEOUT_REPAIR_CAP` = 2 repair attempts (3 total; repair re-entry needs no rollback since a failed check persists nothing; attempts surface as `attempt-start` events). On exhaustion the run no longer fails destructively: a best-effort `commitWip` dep preserves the diff on the run branch (`rune(<product>): WIP — closeout blocked — <task>`, skipped on a clean tree, never blocks the terminal) and the run **parks blocked-on-human** with branch + worktree preserved — visible in the release/approvals surfaces, releasable (release cold-finalizes the clean worktree and removes it), and re-dispatch resumes the branch tip and re-selects the same unchecked task. (Amended 2026-07-07 after a codex code review: the first cut used an operational hold, whose `completed` supervision row is invisible to the release path and whose git-registered preserved worktree blocks the project's next Start — see the held-worktree Active entry.) Non-check closeout failures, `mapResultToTerminal`, supervision statuses, and the merge gate are unchanged. Covered by `project-orchestrator.test.ts` (repair success, exhaustion → WIP + park, optional-dep/throw/no-worktreePath paths, non-ready repair evidence) and `orchestrated-work-runner.test.ts` (repair-success + exhaustion integration: parked terminal, blocked-on-human supervision row, preserved worktree, WIP commit, per-attempt artifact entries); docs synced (`project-lifecycle.md`, `subsystems.md`); `npm run build` and full `npm test` passed.)_
- [x] Orchestrated coder never runs the test suite — it optimizes only its task's tests and can hand back a diff that breaks the full suite, which closeout then hard-fails. **(primary — kills the project-21 failure class)** _(Fixed 2026-07-06 — the product's `validationCommands` now thread from the orchestrated runner (already resolved for closeout) through `TaskWorkflowRunnerArgs`/`BuildTeamTaskDepsArgs` into the coder prompt as a `## Validation commands` section; `CODER_EXEC_INSTRUCTION` and the coder SOUL make full-suite green part of the definition of done (run every command from the worktree root, iterate fix → re-run until all exit 0, before handback), so closeout's `npm test` becomes a confirming re-run. Test-removal is last-resort only (sandbox-impossible external/live dependency or demonstrated flake; prefer manual-live-gate conversion) and must be recorded as a final `TEST-REMOVED: <path> — <reason>` output line; the coder's handoff notes now reach the reviewer and tech-lead diff bodies as `## Coder handoff notes`, and both gate instructions fail a diff that deletes or weakens a test without that justification — a test red from the implementation may never be removed. No gate-logic/timeout/QA changes; C3/D3 remain the mechanical backstops. Covered by `team-task-deps.test.ts`, `team-task-workflow.test.ts`, and the plumbing pin in `orchestrated-work-runner.test.ts`; docs synced (`project-lifecycle.md`, `subsystems.md`); `npm run build` and full `npm test` passed.)_
- [x] Orchestrated closeout logs only the validation exit code, not the failing test output — a closeout-failed run names no failing test, and the throwaway worktree is GC'd so the diff is unrecoverable. _(Fixed 2026-07-06 — `defaultRunValidationCommand` now pipes stdout+stderr and returns a merged rolling tail capped at `MAX_VALIDATION_OUTPUT_TAIL_CHARS` (20k, keep-the-end) as a required `ValidationCommandResult.outputTail`; an exit-keyed 10s drain fallback bounds the grandchild-holds-pipes wedge so a daemon-leaving suite can't flip a pass into a false timeout. On closeout failure the orchestrated runner persists a path-scrubbed, secret-redacted entry (task, command, outcome, tail) to `closeout-validation-failure.txt` in the run dir (append-mode, best-effort), enriches the warn log with a bounded tail, and emits a `closeout-validation-failed` activity event into transcript.jsonl + the live run feed. Terminal reason/blocked semantics unchanged; the closeout repair-loop bug can now consume `result.outputTail` for its feedback payload. Covered by `work-run-gate-runtime.test.ts` (real-spawn capture, tail bounding, timeout partial capture) and `orchestrated-work-runner.test.ts`; `npm run build` and full `npm test` passed.)_
- [x] Product deep-view does not reconcile the product model when a run terminates on its own — the project keeps showing "Cancel" instead of "Start" and the Runs panel's "Agent activity" list shows roles as active while the run reads failed. Only a page reload clears it. _(Fixed 2026-07-06 — `handleRunFrame` now treats a terminal `run-event: state` frame (state not in `{running, parked}`) as the signal that `current` is stale: it marks the run's roster idle locally (the bus never emits an all-idle agents frame) and re-fetches the product model via `reloadProductAndOperations` behind an in-flight guard, flipping Cancel back to Start and dropping the stale `activeRun` — same end state as a manual page reload. Also fixed the run-card status pill hardcoded to `pill-inprogress`: the class is now derived from run state (new `pill-failed`/`pill-warn` CSS). Covered by `product-deep-view-client.test.ts` (terminal reconcile + in-flight dedupe, no re-fetch on non-terminal frames, roster idles before the re-fetch lands, pill class derivation); `npm run build` and full `npm test` passed.)_
- [x] `AskUserQuestion` in a headless `/work --auto` run wasn't surfaced or answerable, so the decision was lost. _(Fixed 2026-07-06 — legacy work-run stream scanning now detects Claude `AskUserQuestion` tool_use blocks, parks as `blocked-on-human` with structured question/options, surfaces option buttons in Telegram and cockpit approval rows, and routes `work-run-answer:<runId>:<optionId>` to a supervised:false answer mutation that resumes the existing parked worktree with the selected answer injected. Malformed question payloads park with an inspect-transcript fallback instead of classifying noop/partial. Sentinel Release/finalize remains separate. Covered by `work-run-question.test.ts`, `work-runner.test.ts`, `work-run-answer.test.ts`, `approval-actions.test.ts`, `telegram-sender.test.ts`, and `home-view-client.test.ts`; `npm run build` passed.)_
- [x] Reviewer and tech-lead feedback was diagnostic-only and reached the coder/QA lossily. _(Fixed 2026-07-06 — reviewer and tech-lead verdict schemas now carry `suggestedChange` guidance, rejection feedback keeps diagnostic `whatFailed` separate from prescriptive `actionableNotes`, tech-lead diff findings drive coder retry summaries, tech-lead test-intent suggestions reach QA retries, and the findings ledger renders `Suggested change:` for open findings. Reviewers remain tool-less; the existing objection loop and re-verification behavior are unchanged.)_
- [x] Projects should not carry a standalone "Tests (write first)" task list — QA already authors the tests at the start of every task (per-task workflow C2b), so the separate list is redundant AND can never pass the per-task green closeout. _(Fixed 2026-07-06 — `sizedTasksToMarkdown` now renders each planned task exactly once under its phase's implementation checklist and preserves per-task `testStrategy`/designer metadata for the QA-first workflow. The project templates, AGENTS/CLAUDE convention, and lifecycle docs now describe per-task QA-before-coder behavior instead of phase-level red blocks. The `project-orchestrator` confirm-red special case was removed, so every unchecked line enters the normal role workflow. Covered by `planning-artifact.test.ts`, `planner.test.ts`, `planning-roles.test.ts`, `project-orchestrator.test.ts`, and `orch-task.test.ts`; `npm run build` passed.)_
  - **Issue**
    - The split is NOT authored by the tech lead. A6 (tech-lead breakdown) only tags each deliverable with a `testStrategy`. The standalone "Tests (write first)" block is generated deterministically at A11 by `sizedTasksToMarkdown` (`src/intent/planning-artifact.ts:61`) — it duplicates every `code-tests-required` task into a test checkbox + an implementation checkbox. A12's `project-setup-writer` then writes `tasks.md` following `docs/projects/templates/tasks.md`, which adds the `Write the test suite for **X**` phrasing and the `Confirm red before implementation.` line.
    - The per-task closeout gate (C3, `runCloseoutChecks` → product `validationCommands` = `npm run build` + `npm test`) requires a green suite on **every** task. This was only wired on 2026-07-05 (`3d850e8` "Fix orchestrated closeout validation"); before that it was a no-op, so projects 19/20 (finalized 2026-07-01) never hit it. Project 21 is the first to run against a real per-task green gate — and its first task, `Write the test suite for parallel-dispatch`, is a deliberately-red deliverable the gate can never pass (blocked `closeout checks failed`, 2026-07-06 03:35 run).
    - The `Confirm red before implementation.` checkpoint is structurally dead: it is exact-string special-cased to skip the role workflow (`isConfirmRedTask`, `src/intent/project-orchestrator.ts:565`) but still flows through `performCloseout` → `runCloseoutChecks`, so it can only pass a GREEN suite — it can never witness red.
  - **Decided approach** (shipped)
    - 1. `sizedTasksToMarkdown` (`planning-artifact.ts:61`) stops emitting the `### Tests (write first)` block; render each task once in the implementation checklist, preserving the per-task `testStrategy`/`designerNeeded` annotations the QA-first and designer gates read.
    - 2. `docs/projects/templates/tasks.md` drops the `Tests (write first)` block and both `Confirm red…` lines.
    - 3. Remove the now-dead `isConfirmRedTask` + `confirmRedGateEvidence` (`project-orchestrator.ts:564-578`).
    - 4. Update the CLAUDE.md "Project work is test-first … every phase opens with a Tests (write first) block" convention to "QA authors the tests at the start of each task before the coder (C2b); each task lands green."
    - 5. Leave `tests-as-deliverable` and `manual-live-gate` tasks in the implementation list untouched (green-or-manual, not red-first).
- [x] Cockpit Home "Pending approvals" Approve/Reject buttons don't work correctly — a backend-successful action shows no visible change (no row removal, no re-fetch, no toast), and for a parked (`blocked-on-human`) run Approve silently 404s while Reject is a no-op, so parked rows never clear. _(Fixed 2026-07-06 — Home approval actions now optimistically remove the clicked row, refetch `/api/approvals` and `/api/state` on success, restore the row with inline server/error text on failure, and count pending approvals from the visible inbox including `blocked-on-human`. `blocked-on-human` Reject now clears the parked supervision row; Approve on a swept/not-parked row clears stale supervision and returns ok; dirty worktrees still require the explicit release confirmation path. Covered by `home-view-client.test.ts` and `approval-actions.test.ts`; `npm run build` passed.)_
  - **Issue**
    - **No success feedback (every row).** The Home click handler (`createHomeView`, `src/server/static/home-view.js:325-335`) POSTs `/api/approvals/:id/:action`, sets `button.disabled = true`, and on a 200 does nothing — no row removal, no re-fetch of `/api/approvals`, no toast (`load()` at `:347-361` fetches approvals once and is never re-run after an action). So even the actions that succeed server-side (intent-proposal + playbook approve/reject, and Reject on a parked run all return 200 via `handleApiApprovalAction`, `src/server/webview.ts:2521-2543`) look dead: the row stays put and only the clicked button greys out.
    - **Parked-run Approve silently 404s.** For a `blocked-on-human` row, Approve routes to `requestWorkRunRelease` → `releasePreflight`, which returns `not-parked` when the run's worktree is gone (`src/jobs/work-run-release.ts:134`). `dispatchApprovalStatus` (`src/transport/approval-actions.ts:157-183`) maps that to `not-found` → HTTP 404 → `defaultPostJson` throws on `!response.ok` (`home-view.js:260-269`) → the `.catch()` re-enables the button and swallows the error. The operator sees nothing.
    - **Parked-run Reject is a working no-op.** `dispatchApprovalStatus` returns `'ok'` for a `blocked-on-human` reject but leaves the supervised record untouched (`approval-actions.ts:165`). So Reject returns 200 but the row reappears on the next `/api/approvals` fetch — even after the frontend re-fetch fix, a parked row can never be cleared from the inbox.
    - **Minor, count mismatch.** The status strip's "Pending approvals" number sums only intent + playbook + proposal (`home-view.js:159`) and omits `blocked-on-human`, so it under-reports the panel it sits above (30 vs 35 rows in the confirmed instance).
    - **Confirmed instance (2026-07-06):** the Home panel shows 35 rows — 29 intent-proposals, 1 playbook draft, and **5 parked `gen-eval-loop` runs** (product `rune`, ids `1a089aca`/`b72dd943`/`13f5b6b9`/`9ecd5317`/`80700147`, parked Jul 3–6). All 5 worktrees are swept (verified absent on disk), so every parked-run Approve returns 404 and every Reject returns a 200 no-op — none of the 5 can be cleared from the cockpit. (Root cause of the nightly parked-run accumulation is owned by the "Autonomous bug-fix lane" idea, which makes those rows obsolete for bugs; this entry is only the approve/reject UI + stale-record defect it explicitly scoped out.)
  - **Fix options** (shipped: A + B + C + D)
    - A. **Re-render on success.** On a 2xx, remove the actioned row optimistically and re-fetch `/api/approvals` to reconcile (or re-run `load()`), so a landed action is visible. Covers every source.
    - B. **Surface non-2xx.** On a thrown/`!ok` response, re-enable the button AND show why inline (e.g. "run no longer parked") instead of silently swallowing it.
    - C. **Make a parked row clearable.** A `blocked-on-human` Reject (or a relabeled "Dismiss") on a run whose worktree is gone should terminalize/clear the stale supervised record via a supervision-store write, so the row actually disappears and doesn't rehydrate on the next fetch. Optionally, a swept-worktree Approve can resolve to the same clean dismiss rather than 404.
    - D. **Align the status count** (`home-view.js:159`) to include `blocked-on-human`, or drop the separate number and reuse the panel's own row count.
- [x] module.register() -> module.registerHooks(). _(Fixed 2026-07-06 — Rune app/script entrypoints and the local `rune-kb` MCP registration now run TypeScript through `node --import ./scripts/register-ts.mjs`, a local `module.registerHooks()` loader backed by `esbuild`, instead of `tsx`'s deprecated loader path. Covered by `register-ts.test.ts`; `npm run build` and full `npm test` passed.)_
- [x] Review run after its scheduled date appends the summary to the wrong journal. When a review is completed late, the write-up should land in the journal entry for the day it was due, not the day it actually ran. _(Fixed 2026-07-06 — the review-writer prompt now includes the authoritative `target_journal_path` and explicitly forbids writing to today's journal when the target date differs. The daemon also repairs the observed late-run failure mode: if the scheduled journal did not change but today's journal gained a structured review block by pure append, Rune moves that block back to the scheduled journal and restores today's journal. Covered by `weekly.test.ts`; review regression suite, `npm run build`, and full `npm test` passed.)_
- [x] Review sessions stay "active" in the cockpit forever when the outline marker isn't detected (phase never reaches `done`). _(Fixed 2026-07-06 — review outline detection now accepts a deterministic fenced `outline` block and the recorded fenced weekly-review heading shape, so the shared interview handler transitions to `approval` and surfaces the approval sidecar even when the exact prose marker is absent. The review system prompt now explicitly tells the model to stop after the fenced outline and not write the journal, run post-review updater agents, or commit changes itself; daemon-owned `runWriteupAndUpdates` remains the post-approval path. Covered by `interview.test.ts` plus weekly/monthly/quarterly/yearly/session review regressions; `npm run build` passed.)_
- [x] Nightly processing error. Observation loop — TypeError: readers.interactions is not a function or its return value is not iterable. _(Fixed 2026-07-05 — the nightly observation wiring is pinned to pass `readers.interactions`, append-time loop-filed idea dedupe now prevents repeated observation ideas, Jarvis→Rune reconciliation skips previously rejected/ambiguous candidates by text + evidence fingerprint, and the nightly run repairs missing `knowledge/index.md` wiki entries before reconciliation/lint. Covered by `nightly.test.ts`, `nightly-knowledge-reachability.test.ts`, `knowledge-supersession.test.ts`, `observation-ideas-io.test.ts`, and `index-integrity.test.ts`; full `npm test` passed.)_
- [x] The terminal work-run reconciler only terminalizes a `running` supervised run if a `summary.json` already exists — a run wedged before finalize never self-heals. _(Fixed 2026-07-05 — the reconciler can now recover a stale no-summary `running` row through the same recovery finalizer used at startup, then immediately terminalize the mutation from the generated summary. Production recovery is gated by `WORK_RUN_MAX_RUNTIME_MS` liveness age and skipped while a live mutation handle exists, so active runs keep their normal applier-owned lifecycle. The timer is async-aware and avoids overlapping recovery ticks. Covered by `work-run-reconciler.test.ts`.)_
- [x] No targeted operator lever to recover a running-but-idle orchestrated run — the only recovery was a full daemon restart. _(Fixed 2026-07-05 — added `POST /api/work-runs/:id/recover` plus cockpit Recover controls for active orchestrated runs. The handler validates the active run, reads its durable cursor/task records, reconstructs state, system-cancels and detaches only that active handle, then re-dispatches the same mutation id through the existing recovered-orchestrated path. `startApply` cleanup now only removes its own handle, so an old stuck apply cannot clear the replacement active run if it later exits. Covered by `orchestrated-work-recovery.test.ts`, `webview.test.ts`, `product-deep-view-client.test.ts`, and `mutations.test.ts`.)_
- [x] Cockpit shows a finalized run as still "running" / the project as "planned 0/84" because `registry.json` was not rebuilt after a run finalized. _(Fixed 2026-07-05 — both legacy and orchestrated work runners now refresh the rebuildable product/project registry from the finalizer's post-landing hook, after a gate-passing branch lands on the product base branch. The refresh is best-effort and cannot block terminalization; gate-held/non-merged runs do not refresh stale base-branch state. Covered by `work-runner.test.ts` and `orchestrated-work-runner.test.ts`.)_
- [x] `cancelMutation` / max-runtime cancellation for orchestrated runs only flipped an in-process flag; a cancel that landed after start was not observed until the whole orchestration naturally returned. _(Fixed 2026-07-05 — mutation apply contexts now expose a cooperative `onCancel` listener, the orchestrator checks `cancel`/`cancelReason` at task-selection, post-workflow, post-closeout, and pre-finalizer boundaries, and the orchestrated runner wakes its stream loop immediately with a non-user-facing activity event. User cancels remain failed/cancelled; system cancels stop at a boundary, skip merge/finalizer, write normal terminal artifacts, and classify on work product via `system-cancel`. Covered by `mutations.test.ts`, `project-orchestrator.test.ts`, and `orchestrated-work-runner.test.ts`.)_
- [x] Orchestrated task closeout claims to run task-scoped checks, but production `runCloseoutChecks` is a no-op; task commits can be accepted without executing the tests they just added. _(Fixed 2026-07-05 in `3d850e8` — production closeout now runs the product's `validationCommands` in the task sandbox worktree before staging/committing, using the shared timeout/process-group validation helper. Failed closeout validation returns `closeout checks failed`, emits no closeout progress, makes no git commit, and leaves the task unchecked. Empty closeout command lists still pass per task scope; the project-level merge gate remains fail-closed. Covered by `work-run-gate-runtime.test.ts`, `project-orchestrator.test.ts`, and `orchestrated-work-runner.test.ts`; full `npm test` passed.)_
- [x] /fresh is failing
  - Keep getting errors like this: `Could not summarize conversation — session reset. Error: No conversation found with session ID: 5f3dadf7-b327-418f-acb0-6cbb94b9945d`
  - **Fixed 2026-07-02:** when Claude's CLI session store no longer has a restored Rune session id, `/fresh` now summarizes Rune's stored transcript with a fresh one-shot call instead of failing and discarding the conversation unsummarized. Other summarization failures still fail closed and reset the stale session. Covered by `fresh.test.ts` and `claude.test.ts`.
- [x] Operations container should show stream in reverse descending order starting with most recent at the top instead of most recent at the bottom. _(Fixed 2026-07-02 — product deep-view operation activity now renders newest-first while preserving chronological storage/session trimming semantics. Covered by `product-deep-view-client.test.ts`.)_
- [x] `runDownstreamPlan` stages emit progress to the user but nothing to `rune.log` — a post-approval pipeline failure names no stage server-side. **(project 20 — logging; scoped from the declined observability sweep 2026-07-02.)** _(Fixed 2026-07-02 — `runDownstreamPlan` logs each stage start and one scrubbed terminal failure with product/stage/reason/retryability/mismatches. Existing tests assert stage start logs and a `pm-review-match` failure log.)_
- [x] An agent's self-review must run on that agent's own defined model, not a hardcoded judgment model — applies to every role (PM, tech-lead, coder). **(project 20 — self-review correctness; direction set by operator 2026-07-02.)** _(Fixed 2026-07-02 — product-team judgment calls now receive the full policy-resolved binding and Codex-format calls route through `runCodex`; coder self-review uses the coder binding (`gpt-5.5`/openai/codex). Planning PM/tech-lead role calls and self-reviews now resolve model policy too, and PM authoring plus PM self-review share the policy-resolved PM model.)_
- [x] `runPlanningApprovalPipeline` swallows a post-approval failure with no `log.error` — a failed `/approve` leaves no server-side trace. **(project 20 — logging; scoped from the declined observability sweep 2026-07-02.)** _(Fixed 2026-07-02 — both Telegram `/approve` and cockpit approval now log `planning approval failed` with user/product/stage/retryability/path-scrubbed error before sending terminal user guidance; covered by `approve.test.ts` and `webview.test.ts`.)_
- [x] `runSelfReview` emits no logs — a self-review's role, resolved model/provider, and outcome (revised/confirmed/failed) are invisible in `rune.log`. **(project 20 — logging; scoped from the declined observability sweep 2026-07-02.)** _(Fixed 2026-07-02 — `runSelfReview` logs start, completion with `revised`, and scrubbed failures. Callers can pass resolved model/provider metadata; the production coder self-review now passes its policy-resolved model alias and provider.)_
- [x] The `pmReviewMatch` gate has no escape hatch for manual/live operator verification. **(project 20 — planning-pipeline design.)** _(Fixed 2026-07-02 — planning now has a first-class `manual-live-gate` test strategy. Tech-lead, PM review, self-review parsing, and critique prompts can emit/preserve it; generated `tasks.md` marks these tasks as `*(manual/live - not automatable)*`; `test-plan.md` gets a Manual/live release gates section; and the production task runner parks selected manual gates for operator evidence instead of invoking QA/coder/reviewer automation.)_
- [x] The PM review (`pmReviewMatch`) should REPAIR mismatches, not throw. **(project 20 — planning-pipeline design; direction set by operator 2026-07-02.)** _(Fixed 2026-07-02 — `pmReviewMatch` can return repaired tech spec/tasks plus a repair summary. `runPlannerRoles` and `runDownstreamPlan` continue with repaired artifacts when present, including manual/live gate repairs, while unrepaired mismatches still fail closed as non-retryable planning errors.)_
- [x] `/plan` post-approval pipeline swallows a legitimate `pmReviewMatch` mismatch with no server-side log and dead-ends the session — the only advice is "run /approve again," which just re-rolls the same tech-lead breakdown and hits the same structural gap. **(project 20 — post-approval remediation.)** _(Fixed 2026-07-02 — downstream planning now throws structured `PlanningDownstreamError` values carrying stage/reason/mismatches/retryability; `runDownstreamPlan` logs stage starts and one scrubbed terminal failure, and both Telegram `/approve` plus cockpit `/api/planning/approve` log scrubbed approval failures. Non-retryable `pm-review-match` mismatches now keep the session approved while showing the concrete mismatches, suppressing blind retry advice, and naming the valid next steps: amend the spec/DoD or approve/add a manual live release-gate task.)_
  - **Issue**
    - On a real `/plan` run (2026-07-02, product `parallel-product-chats`), the PM interview + spec + approval succeeded, then `runDownstreamPlan` (`src/intent/planning-roles.ts:293`) threw at the `pmReviewMatch` gate (`:340-350`, `throw "PM review mismatch: …"`). The session was left `approved` with `approvedSpec` and no `downstreamArtifact`; no project scaffolded.
    - The failure left **zero server-side trace.** The catch in `runPlanningApprovalPipeline` (`src/bot/commands/approve.ts:139-150`) sends the operator a terminal message plus "Planning session is still approved — run /approve again to retry," but never calls `log.error`. Combined with the state-machine transition and `runDownstreamPlan` progress both not logging, a whole approval can fail invisibly — confirmed: nothing in `rune.log` for the run beyond "Planning session started."
    - The mismatch itself was **correct** (the tech-lead breakdown verified the frontend only with jsdom + WS tests; the spec DoD requires a live operator gate). The defect is the *handling* of a correct mismatch: it dead-ends, and the "retry" advice re-runs a nondeterministic tech-lead that keeps producing the same structural gap, so the operator can loop indefinitely with no resolution and no log.
  - **Fix options**
    - A. (observability) `log.error` the mismatch/failure in the `approve.ts` catch, and add start/terminal logs around `runDownstreamPlan`, so a failed approval is never silent server-side.
    - B. (resolution path) On a legitimate `pmReviewMatch` mismatch, surface the specific mismatches to the operator and offer a real next step — amend the spec/DoD, or accept a designated manual release-gate task — instead of a bare "retry."
    - C. (retry semantics) The `/approve` retry path (`approve.ts:58-65`) re-runs `runDownstreamPlan` from the stored spec; when the mismatch is structural (spec vs plan) a re-roll can't converge, so gate the retry advice on whether a re-roll can plausibly help.
- [x] `src/ai/claude.test.ts > WORKSPACE_DIR env passthrough` failed on `main` — a spawned child inherited the parent daemon's `RUNE_WORKSPACE_DIR` even when `WORKSPACE_DIR` was empty. _(Fixed 2026-06-29 — `src/ai/claude.ts` now governs the child's `RUNE_WORKSPACE_DIR` solely by `config.WORKSPACE_DIR`: set when configured, delete the inherited value when not, instead of spreading `...process.env` then conditionally adding it. Landed with the project-19 merge; full suite green.)_
- [x] Codex agent output is reduced to event-type marker strings — every gpt-5.5 role in every work run displays as `codex item.completed` spam instead of its real prose, and the no-code test-intent gate hard-fails because the QA rationale never survives transport. _(Fixed 2026-06-23 — `src/jobs/execution-agent.ts` now whitelists Codex `item.completed` / `agent_message` prose, suppresses lifecycle/reasoning JSON as activity-only, preserves malformed raw stdout through `{type:"raw"}`, and prevents raw JSONL from falling back into `ExecutionAgentResult.output`; covered by `execution-agent.test.ts` Codex JSON event regressions and the `team-task-deps.test.ts` no-code rationale consumer.)_
- [x] Remove study from the morning prep and add the "## Notes" section at the end. _(Fixed 2026-06-23 — morning prep no longer reads or prompts with study/writing files, fallback output omits `### Study` and `### Writing Focus`, normalized Claude output strips either stale section, and the required output format ends with `## Notes`.)_
- [x] `/work --auto` resume reuses a stale project branch — when `main` advances out-of-band after the branch was cut, the resumed run never rebases onto it, so a fix landed directly on `main` is invisible to the in-flight project. The uncovered flip side of the 2026-06-04 stable-per-project-branch resume fix below. _(Fixed 2026-06-23 — chose A+B: resume-time base reconciliation via auto-rebase, with conflict cleanup.)_
  - **Issue**
    - The 2026-06-04 resume fix made each project run check out a stable per-project branch (`rune-work/<slug>`) when it already exists, instead of re-forking off `main` (`work-runner.ts` `workBranchName`; resume path `src/jobs/sandbox-runtime.ts:297-309`). That correctly killed the restart-from-Phase-1 bug — but it only ever checks the branch OUT (`git worktree add <path> <branch>`, no `-b`); it never rebases or merges the branch's now-older base forward.
    - So once `main` moves ahead of the branch's merge-base, the resumed run builds on a stale tree. Any commit landed directly on `main` (an out-of-band fix, a hand-built phase, a hotfix) is absent from the worktree even though `git log main` shows it. The run can't see it, build on it, or be guarded by it.
    - This is the resume fix's blind spot: it preserves the branch's OWN prior commits (its whole purpose) at the cost of never picking up `main`'s new commits. There's no "if base advanced, rebase/merge before resuming" step and no warning that the branch is N commits behind `main`.
    - Worst case it's silent and harmful: a fix is deliberately landed on `main` to UNBLOCK the project (a resilience fix the next run depends on), the run resumes onto the pre-fix base, and reproduces the exact failure the fix was meant to remove — reading as "the fix didn't work."
    - Confirmed instance: Phase 11A (gate-rejection feedback retries) was built out-of-band and committed straight to `main` (tip `2659ae4`) specifically to unblock Phase 10. Re-running Phase 10 for project `14-product-team-agents` resumed the leftover `rune-work/14-product-team-agents` branch (tip `b7c5dbd`), whose merge-base with `main` is `392d142` — pre-11A. The branch is 12 commits behind `main` and does NOT contain `2659ae4`, so the Phase 10 run never saw the feedback-retry fix and stayed exposed to the one-shot-gate deadlock 11A removes (2026-06-15).
  - **Fix options** (A is the real fix; D is the cheap guard)
    - A. On resume, reconcile the branch base with `main` before handing the worktree to the agent: when `main` is ahead of the merge-base, rebase the project branch onto (or merge `main` into) the product's `baseBranch`, then check it out. Surface conflicts as a blocked outcome instead of silently proceeding on a stale base.
    - B. Make base reconciliation explicit at run start: record the branch's merge-base vs `main` and, if behind, either auto-rebase (clean) or stop-and-alert (conflicts), so "what base does this run build on" is never implicit.
    - C. Prefer a fresh cut when the branch carries no unmerged-but-wanted work: if the project branch's commits are all already on `main` (or are throwaway), delete it and let the resume path re-fork fresh from current `main` (`sandbox-runtime.ts:310-337`).
    - D. (cheap guard) Before resuming, detect that the project branch is behind `main`'s tip and warn/block with the count + merge-base, so an operator can rebase or delete rather than unknowingly run on a stale base.
  - **Resolution (2026-06-23)** — A+B in `src/jobs/sandbox-runtime.ts`.
    - **Resume-time base reconciliation.** `createWorktree` still checks out an existing stable project branch first, then compares `HEAD` against the product's configured `baseBranch` inside the new worktree. If the base tip is not already contained, it runs `git rebase <baseBranch>` before the agent sees the tree. No remote fetch is performed; the local configured base branch remains the source of truth.
    - **Stable diff base after reconciliation.** `SandboxSpec.baseSha` is now the branch tip after resume-time reconciliation, so `baseSha..branch` continues to mean "work created by this run only." Rebased resumes include optional `baseReconciled` metadata (`strategy`, `baseBranch`, previous/new tips, base-ahead count).
    - **Conflict/failure behavior.** Rebase conflicts abort setup instead of proceeding stale: the runtime attempts `git rebase --abort`, removes the just-created worktree with `git worktree remove --force`, and throws a clear `createWorktree: base reconciliation failed...` error naming branch, base branch, previous tip, and base-ahead count.
    - **Tests.** `sandbox-runtime.test.ts` covers already-current resumes, behind-base auto-rebase, rebase conflict cleanup, base-inspection cleanup, and explicit `startPoint` skipping resume/reconciliation. Targeted suite: 51 tests passing.
  - **Related:** the 2026-06-04 stable-per-project-branch resume fix below (`[x]`, `work-runner.ts` `workBranchName` + `sandbox-runtime.ts` resume) — this is its uncovered case, same `createWorktree` resume seam.
- [x] Cockpit "Pending Approvals" can't release a `blocked-on-human` run — Approve/Reject are disabled and Open does nothing, so the web cockpit is a dead end for parked runs (Telegram's release button is the only working surface). _(Closed 2026-06-22 — folded into project 17's `operational-panels-cutover`. Still genuinely live in current `app.js` (Approve/Reject hardcoded `disabled` for `blocked-on-human`, Open a no-op; backend release path works via `approval-actions.ts:157-184`), but project 17 replaces this entire `app.js` cockpit, so a standalone fix would be thrown away at the Phase 6 cutover. The cutover task now owns building the parked-run-release affordance working.)_
  - **Issue**
    - `renderApprovalRow` in `src/server/static/app.js:1220-1225` hardcodes `disabled` on both Approve and Reject for any row whose `type === 'blocked-on-human'`. The inline comment justifies it as "blocked-on-human rows are queue-uneditable (`dispatchApprovalStatus` returns 'not-found' for that source)."
    - That justification is **stale**. `dispatchApprovalStatus` was upgraded (Project 13 Phase 1c, `src/transport/approval-actions.ts:157-184`): a `blocked-on-human` row with status `approved` now routes to `requestWorkRunRelease` → cold-finalize, returning `'ok'` on a clean release. So the backend the Approve button already POSTs to (`POST /api/approvals/:id/approve` → `webview.ts:1313`) *can* release the run — the frontend just greys the button out before the click can reach it.
    - The Open button is an explicit no-op: `handleApprovalsClick` (`app.js:1268-1274`) returns immediately for `action === 'open'` — "a no-op placeholder for now … wire-up arrives with the live-verification refinement task." That wire-up never landed.
    - Net: the panel renders three buttons for a parked run and none of them work. The only functioning release surfaces are the dedicated endpoint `POST /api/work-runs/:id/release` (`webview.ts:828`, optional `{ confirmDirty: true }`) and the Telegram release callback (`src/bot/work-run-release-callback.ts`).
    - Confirmed instance: run `376396eb` (project `14-product-team-agents`, 2026-06-14) sat `blocked-on-human` in the cockpit; Approve/Reject were greyed out and Open did nothing on click. It was released only via the Telegram inline button.
  - **Fix options** (A is the real fix)
    - A. Stop disabling Approve for `blocked-on-human`. Enable it and let the existing `POST /api/approvals/:id/approve` → `dispatchApprovalStatus` path run the release (`approval-actions.ts:157` already handles it). Remove the stale "returns not-found" comment. Reject can stay disabled (it's a no-op for a parked run) or be relabeled "Dismiss."
    - B. Wire the Open button (`app.js:1268`) to a real action — open the run's detail/diff, or scroll to the cockpit project card — so a reviewer can inspect the branch before releasing.
    - C. Handle the dirty-worktree case. The inbox quick-approve sends `confirmDirty: false`, so a dirty parked run silently no-ops (`'not-found'`). Surface a confirm step that posts to `POST /api/work-runs/:id/release` with `{ confirmDirty: true }` when the worktree isn't clean, matching the Telegram callback's two-path behavior.
    - D. (stopgap) If Approve must stay disabled, show a tooltip/link pointing to the working release surface so the row isn't a silent dead end.
- [x] Work-run terminal alert gives no "stopped / blocked on a decision" signal — a deliberate hard-stop reads as routine progress. _(Fixed — superseded by projects 13 + 15. A hard-stop now emits the `RUNE_WORK_RUN_SENTINEL` and parks as a distinct `blocked-on-human` supervision status (`src/jobs/work-run-sentinel.ts`, `work-runner.ts:515-569`); Telegram renders `⏸️ <slug> parked · needs you` with the reason + a Release button (`telegram-sender.ts:100-111`) instead of the misleading `📊 partial`. The non-cooperative `AskUserQuestion` trigger remains — see the narrowed bug below.)_
  - **Issue**
    - The classifier (`src/jobs/work-run-classify.ts:38`) only has `branch-complete | partial | noop | dirty-uncommitted | failed`. A run that hard-stops on a blocker (≥1 commit + tasks remaining) classifies `partial` — the exact same bucket as "ran out of turns mid-progress."
    - The Telegram render for `partial` (`src/transport/telegram-sender.ts:96`) is `📊 <slug> partial · N commit(s), X/Y tasks done`. That reads as progress, not "stopped, needs you."
    - The agent's actual stop reason lives only in the run `transcript.jsonl`; it never reaches Telegram or the cockpit card.
    - Confirmed instance: run `20565e71` (project 10, 2026-06-01) deliberately hard-stopped on the pkms-PII-into-public-repo contradiction, made 1 commit, classified `partial`. The user saw only the commit-poll ping and never learned the run had stopped and was waiting on a decision.
  - **Fix options** (A is the real fix)
    - A. Add a distinct `blocked-on-human` / `stopped-early` outcome the agent signals explicitly (sentinel in its final message), classified separately with a ⚠️ alert that carries the stop reason.
    - B. Regardless of outcome, include the agent's final-message reason on every non-`branch-complete` terminal Telegram alert so any incomplete run carries WHY.
    - C. On the cockpit card + alert, distinguish "made progress, more to do" from "stopped, cannot proceed" (e.g. surface `tasksRemaining` + a blocked flag), so `partial` is never silent about a hard-stop.
  - Related to the `AskUserQuestion` bug below.
- [x] The "Confirm red before implementation" TDD gate is honor-system with no recovery path — when the coder bundles a phase's tests and implementation into one commit, the suite is already green when the gate evaluates, red can never be demonstrated, the tech-lead reviewer can only veto, and the run dead-loops to the round cap and fails. A plain auto-continue re-trips forever because the bad state is committed on the branch. _(Confirm-red trip fixed 2026-06-22 — Rune now treats the repeated exact "Confirm red before implementation." checklist item as orchestrator-owned bookkeeping, auto-closes it without role review, commits the tick, and resumes at the next real task. Regression coverage proves the role workflow is not called for the gate and a later task failure does not reselect it. **Scope:** this resolves ONLY the confirm-red trigger. The second, independent dead-loop trigger on the same out-of-sequence committed state — the diff-vs-tree completeness gate (option E, run `a6958309`) — is untouched and is now tracked as its own open bug below, so a run that bundles impl ahead of sequence still dead-loops on the impl task whose deliverable is already on the tree.)_
  - **Issue**
    - "Confirm red before implementation." is boilerplate in every project's `tasks.md` (projects 05, 06, 08, 11–17), but it is **not machine-enforced**. There is no `orch-task.ts` (only `orch-task-select.ts`, `orch-closeout.ts`) that runs the phase's tests and asserts they fail. The gate is a plain checklist line: the coder is trusted to write tests, observe red, and tick the box.
    - The only enforcement is post-hoc — the tech-lead role gate in the team-task workflow inspects the task's diff against the task description. It can return `fail` with feedback, which threads back to the coder and retries within the round cap, but it has **no corrective action**. When the diff already contains the implementation, no coder turn can make the suite red again, so the feedback is permanently unresolvable: the loop exhausts the round cap and the whole run classifies `failed`.
    - The honor-system itself isn't the defect — the **missing recovery path** is. A gate that can only say "no," against a state no subsequent turn can fix, guarantees a dead-loop rather than a recoverable stop.
    - Because the bad state is committed on the project's stable branch, a plain `/work --auto` resume can't self-heal: on resume the orchestrator selects the next unchecked task (the phase's "Confirm red"), runs the suite, finds it green (impl present), the reviewer blocks again, round cap trips again. It re-fails every time until the commit is re-sequenced or the gate gains a recovery action.
    - Confirmed instance: run `c1e18632-4a79-4972-8c7d-a359c74c001d` (project `17-cockpit-redesign`, 2026-06-20), `outcome: failed`, reason "orchestration blocked on \"Confirm red before implementation.\": round cap reached with unresolved task feedback." Phase 1 was sequenced **correctly** — `9b53304` (tests for §1) → `322b427` (Confirm red, ticked against tests-only) → `f3fd2b6`/`cc1c598`/`f347087` (implementation). The break is commit `00849c7`, labeled *"Write the test suite for run event publish, live snapshot, and subscription client"* but actually carrying the §2 implementation alongside the tests: `run-feed-client.test.ts` (+174), `work-run-live-api.test.ts` (+328), `run-event.test.ts` (+123) **plus** `run-feed-client.js` (+131), `webview.ts` (+93), `notification-bus.ts` (+40), `sender.ts` (+14), `webview-sender.ts` (+13). When the Phase 2 "Confirm red" task evaluated, the §2 impl was already present, so those tests were green and red was undemonstrable.
    - **Second confirmed trip — same committed state, a different gate (run `a6958309-...`, 2026-06-20 20:42).** A later run on the same branch hit the SAME bundled-`00849c7` state but tripped a *different* gate: the team-task **diff-vs-tree completeness check**. The `run-event-bus-contract` task (`tasks.md:39`) was judged incomplete because the reviewer evaluates the task's **diff** against the task description — and that task's deliverables (`BusRunEvent` typing in `notification-bus.ts`, `WebviewSender` forwarding, the client frame parser) were already on the tree from `00849c7`, so they never appeared in *this* task's diff. The gate judges the delta, not the tree state, so a deliverable that already exists reads as "missing." Same root as the "Confirm red" trip (implementation landed before the gate meant to guard it), surfaced through a second, independent mechanism — which is why a naive auto-continue trips a *new* gate even if the "Confirm red" one is satisfied.
    - **The coder has no cross-round memory.** Within the round-cap retry loop, each coder invocation starts fresh — it does not carry forward what prior rounds attempted or why the reviewer rejected. Against an unfixable committed state it can only thrash; even against an actionable rejection it can re-attempt a dead-end it already tried. This compounds both trips: the retry budget the round-cap counts down is spent by a coder that can't accumulate context across the very rounds being counted.
  - **Fix options** (A + B is the recommended pair — prevention plus recovery; A alone leaves committed branches stuck)
    - A. (prevention — commit-time guard) Reject the commit for any task that precedes a "Confirm red" gate if it adds non-test source under the phase's target paths. Test files are identifiable by `.test.ts`; the guard fails the commit with an actionable message telling the coder to land tests alone first. Stops the bundling at the source so the reviewer is never the only line of defense.
    - B. (recovery — makes the branch self-heal) Give the orchestrator a recovery action: when a "Confirm red" task finds the suite already green because the phase's implementation is present, mechanically split the offending commit by the same `.test.ts` heuristic (tests vs source under the phase's paths), re-run to confirm red on the tests-only state, tick the gate, then re-apply the implementation as a follow-up commit. Converts the dead-loop into self-healing, so a plain auto-continue recovers this and every future run without manual commit surgery.
    - C. (bigger rethink — remove the honor-system) Make the gate machine-run: when the orchestrator hits a "Confirm red" task, it establishes red itself — revert the phase's implementation files to base, run the phase's new tests, capture the red result as a durable artifact, then proceed. Removes reliance on coder discipline and the reviewer veto entirely. Heavier; not required to fix the actual bug.
    - D. (manual, this branch only) If A ships without B, the already-committed `rune-work/17-cockpit-redesign` needs a hand re-sequence: split `00849c7` into a tests-only commit and an implementation commit, confirm red on the tests-only state, then re-land the impl. Also pin the publish-isolation (concurrency) contract for `publishRunEvent` before the §2 implementation lands, per the reviewer's secondary finding. _(Box-check alternative actually used 2026-06-22: because Phase 2 "Confirm red" is already ticked and no gate re-evaluates it, checking off the three tasks whose deliverables are already on the tree — `run-event-bus-contract`/39, `live-run-state-snapshot`/41, `webview-run-subscription-client`/42 — moves the branch forward without commit surgery and without re-tripping either gate. `run-event-bus-publish`/40 is genuinely unbuilt and stays unchecked, so the next run resumes there.)_
    - E. (diff-vs-tree gate) Make the completeness gate judge **tree state**, not only the task's diff. When a deliverable named by the task already exists on the branch (landed by an earlier commit), count it satisfied rather than missing-from-this-diff. Without this, any out-of-sequence landing permanently fails the task that was "supposed" to introduce it — the run `a6958309` trip.
    - F. (coder memory) Thread prior-round attempts + reviewer feedback into the coder's context across the round-cap loop, so retries accumulate instead of resetting. Pairs with B (recovery): a coder that remembers is far likelier to converge on the retries the round-cap is budgeting.
  - **Owner:** project 14 (product-team-agents) — it owns the multi-task orchestrator loop, the role gates, and the feedback/round-cap mechanism (`c965642`, `e330a33`, `14df869`, `2659ae4`). The reviewer that adjudicates task-to-diff fit only stabilized to `branch-complete` on 2026-06-18/19, so project 17 (2026-06-20) is the first feature run executed under it — which is why this gate never tripped on earlier projects despite the same tests-first convention.
- [x] The diff-vs-tree completeness gate dead-loops on a deliverable already on the branch — when a task's named deliverable was landed by an earlier (out-of-sequence) commit, it never appears in *this* task's diff, the role gate that judges the task's delta against its description reads it as "missing/unsatisfied," and the run exhausts the round cap and blocks. This is the **second, independent trigger** of the dead-loop carved out of the "Confirm red" bug above (its confirm-red trigger is fixed; this one is not). Confirmed instance: run `a6958309-...` (project `17-cockpit-redesign`, 2026-06-20 20:42), task `run-event-bus-contract` (`tasks.md:39`) judged incomplete because its deliverables (`BusRunEvent` typing in `notification-bus.ts`, `WebviewSender` forwarding, the client frame parser) were already on the tree from commit `00849c7`.
  - **Why the confirm-red fix doesn't cover it.** Auto-closing the confirm-red gate only stops resuming *at* that gate; the run then advances to the impl task whose deliverable is already committed, and trips this gate instead. The 2026-06-19 reviewer-prompt hardening (`413fa36` — forbid absence-from-partial-diff objections) pre-dates this run and did not prevent it, so prompt hardening alone is insufficient; the tech-lead diff-review gate (`TL_DIFF_REVIEW_INSTRUCTION` in `src/jobs/team-task-deps.ts`) still judges coherence against the diff without the same partial-view caveat.
  - **Fix:** option E from the entry above — make the completeness gate judge **tree state**, not only the task's diff: when a deliverable named by the task already exists on the branch, count it satisfied rather than missing-from-this-diff. Apply the same partial-view caveat to the tech-lead diff review the reviewer already carries. Pairs well with option F (coder cross-round memory).
  - **Fixed 2026-06-23:** each task workflow now receives a capped branch-wide git tree-state evidence block (changed files, diffstat, and branch diff excerpt) appended to the project context, and the tech-lead diff gate receives that context plus the bounded spec alongside the current task diff. Its instruction now matches the reviewer fail-safe: no tool/repo-access claims, no missing-from-this-diff rejection when the provided tree-state/context evidence shows the deliverable already exists, and only fail when the deliverable is absent from both the diff and provided tree-state evidence or the diff regresses it. Follow-up (code-review of codex's fix): the **reviewer** gate — the historical *primary* trigger of this dead-loop, which already received the same context — had its instruction aligned to the tech-lead's: it now references the whole-*branch* partial view (not only whole-repository) and treats the tree-state evidence as positive proof a deliverable landed in an earlier commit, rather than only suppressing absence objections. Covered by `orchestrated-work-runner.test.ts` and `team-task-deps.test.ts`.
  - **Owner:** project 14 (product-team-agents) — same gate/round-cap machinery as the entry above.
- [x] `/work --auto` wedges open AGAIN — the agent emits terminal `result: success` but the process itself never exits (backgrounded test tasks hold it alive), so the `exit`-keyed teardown from the 2026-06-04 fix below never fires. The run sits `running` ~8.5h, quiet-pings on repeat, never auto-merges (plain work-runs have no merge path), and finally mis-classifies as `failed` when killed. _(Follows on from the fixed wedge bug immediately below — same symptom class, different trigger. Confirmed instance: run `d0679453`, project `12-writer-memory`, 2026-06-06.)_ → 15-work-run-finalizer
  - **Issue** (six distinct defects surfaced by one incident)
    - **1. Completion keys off process `exit`/`close`, not the terminal `result`.** The 2026-06-04 fix (below) deliberately keyed teardown off the agent process `exit` rather than the `result` event, because `result` arrives while the agent is still alive. But that leaves a hole: if the agent emits `result: success` and then **never exits**, nothing finalizes. `streamProcess` only sets `done` on `child.on('exit'|'close'|'error')` or cancel (`src/jobs/work-runner.ts:838-871`); `reapTree()` (`:816-830`) is triggered only from `on('exit')` (`:838-844`), so it never ran.
    - **2. Backgrounded tasks keep `claude -p` alive.** The agent ran tests as background Bash tasks; 5 `vitest run` tasks hung (open handles → vitest never exits) and `claude -p` will not exit while background tasks are alive. The agent finished the project at 04:38 (`result: success`, `terminal_reason: completed`) but the process stayed up until a human killed the tree at 13:12:30. The keep-alive ticker (`:673`) kept `lastChildAliveAt` fresh the whole time, so supervision saw "quiet," never "stalled."
    - **3. Recurring quiet ping — `quietNudgedAt` is dropped.** The stall runner stamps `quietNudgedAt` and persists it (`src/jobs/stall-check-runner.ts:70-85`), but the keep-alive heartbeat rebuilds the whole `SupervisedRun` without that field (`src/transport/mutations.ts:32-63, 305-320`) and `upsertRun` **replaces by id, it does not merge fields** (`src/jobs/supervision-store.ts:138-145`). So every 30s heartbeat clears the one-shot guard and the quiet nudge re-fires (`isQuietRun`, `src/intent/supervision.ts:124-138`; 5-min threshold `src/jobs/stall-check.ts:21-26`).
    - **4. Classifier ranks exit-signal above work product.** `src/jobs/work-run-classify.ts:233-248` checks cancelled/signal/missing-code/nonzero **before** branch-complete. The human SIGTERM (exit 143) on a completed run therefore stamped `failed` — `summary.json` recorded `outcome: "failed"`, `reason: "exited with code 143"` with null work-product fields (the classify-error path), even though the branch had all 13 commits and tasks complete. Note: a naive reorder is wrong — it would mis-mark a genuinely user-cancelled run as complete. The fix needs an exit-fact taxonomy.
    - **5. No merge/push/delete-branch finalizer for plain work-runs.** `work-runner.ts` classifies, writes summary/index, destroys the worktree (`finally`, `:556-578`), and fires GC (`:585`) — but never merges or pushes. Only `gen-eval-loop-runner.ts:254-302` (`realMergeBranch`) does merge → push → `branch -d`, and it has review gates (`:618`). Docs confirm plain work-runs are intentionally "branch-complete, not yet on main" (`docs/projects/index.md:192-208`). The project-12 merge that eventually landed was done by a **Rune assistant session answering the operator** (commit `5b8dec8`, stamped 13:12:48 — same second this conversation's first message hit `rune.log:45807`), NOT by the autonomous system. Left alone, the run would have stayed failed-on-a-branch forever.
    - **6. Recovery/reconciliation relabel but don't finalize.** `recoverRun` only maps stale `running` → `unknown` (`src/intent/supervision.ts:201-203`), persisted by `supervision-recovery.ts:36-55`; `reconcileOrphans` flips mutations to failed/orphaned (`src/transport/mutations-log.ts:43-75`) but does no process/worktree/branch cleanup. A run orphaned across a server restart is never driven to a real terminal state. Startup also runs the orphan-worktree sweep after recovery (`src/index.ts:64`), so a future recovery-finalizer could race away its own evidence.
  - **Provenance.** Two independent investigations (Claude + Codex) plus an adversarial Codex critique of the combined plan. The critique corrected an early proposal to "finalize on `result`" (unsafe — see defect 1 and the 2026-06-04 resolution note below) into a result-seen **watchdog**, and flagged that auto-promotion is a policy change, not a bug fix.
  - **Fix plan** (P0 = correctness, no policy change; P1 = the decided gated auto-merge; P2 = resilience)
    - **P0.1 — Preserve supervision metadata across heartbeats.** Make `upsertRun` field-merge unknown/current fields (or thread `quietNudgedAt` through `buildSupervisedRun`) so a heartbeat can't clear the quiet guard (`src/transport/mutations.ts`, `src/jobs/supervision-store.ts`). Regression test: quiet nudge → keep-alive → no second nudge. *Stops the recurring ping at the source.*
    - **P0.2 — Terminal-result watchdog.** On the `result` event, transition the run out of plain `running`, start a bounded drain window, and reap the tree (SIGTERM → SIGKILL → force-complete) **only if the child doesn't exit on its own** — never kill immediately on `result` (that re-introduces the false `failed` the 2026-06-04 fix avoided). Emit an explicit `reapedAfterTerminalResult` exit fact (`src/jobs/work-runner.ts`). This is option C from the fixed bug below, finally built.
    - **P0.3 — Classifier exit-fact taxonomy.** Distinguish user-cancel vs external-kill vs clean-exit-with-wedged-stdio vs internal-reap-after-terminal-result. A Rune-initiated reap after `result: success` with a clean, complete branch must classify branch-complete; a real user cancel stays failed (`src/jobs/work-run-classify.ts`).
    - **P0.4 — Recovery classifies before cleanup.** Make startup recovery (and the restart path) compute work product and drive stale runs to a real terminal state via the same finalizer, and ensure it runs before the orphan-worktree sweep so it doesn't lose its evidence (`src/jobs/supervision-recovery.ts`, `src/index.ts`).
    - **P1.5 — Gated auto-merge finalizer (decided 2026-06-06).** Plain `/work --auto` **will** auto-merge to main, but only through a gate. Build a shared, idempotent, phase-recorded `src/jobs/work-run-finalizer.ts` that owns: classify → verify gate → merge → push+verify → worktree remove → branch delete → terminal writes, as one resumable state machine.
      - **Gate (all must hold, else stop at branch-complete + alert, do not merge):** tests green, working tree clean, `tasksRemaining == 0`, no merge conflict / sane base relationship, no concurrent run owns the branch/project.
      - **Test before mutating main**, not after — run the gate's checks in an integration worktree (or on the branch), so a red result never leaves local `main` altered.
      - **Per-product / base-branch lock**, not per-project — different projects in a product share one `main` (`src/config.ts:250` allows concurrent runs; `supervision-store.ts:10-15` assumes a single writer).
      - **Push before deleting the branch** so origin is the durable backup; record durable phases (`merged-not-pushed`, `pushed-not-deleted`, …) so a crash mid-finalize is resumable by P0.4 recovery.
      - Reuse/refactor `realMergeBranch` (`src/jobs/gen-eval-loop-runner.ts:254-302`) rather than duplicating it; note its existing half-merged push-failure warning (`:274`).
      - Rationale for gated (not unconditional): autonomous runs spawn with `--dangerously-skip-permissions` (`work-runner.ts:361`); the gate is the line between "autonomous" and "lands broken work on main."
    - **P1.6 — Failure/partial/cancelled path.** Always reap the tree + flush transcript/summary; never merge; remove the worktree after preserving forensics OR mark explicit `blocked-on-human`; supervision becomes terminal or intentionally blocked, never quiet-pinging `running`; branch retention/deletion recorded. (Adding a `wrapping-up` status is a deliberate cross-surface change to the status union `src/intent/supervision.ts` + `supervision-store.ts:25` — do it explicitly or model the transient state without a new enum value.)
    - **P2.7 — Backstops independent of agent cooperation.** Quiet→cancel **actuator** (first sustained-quiet notifies; a longer threshold escalates to cancel/reap/finalize instead of nagging forever) + a hard **max-runtime ceiling** that group-kills regardless of liveness. Plus a **cwd-under-worktree process sweep** as a fallback reap for reparented/detached grandchildren the pgid kill misses (scoped to the one worktree path; demoted from the happy path, kept for defense-in-depth).
    - **P2.8 — Regression suite.** Every observed mode (result-before-exit; result-then-reap classifies branch-complete; quiet marker survives keep-alive; store divergence; resume/branch-reuse cleaning ALL of a project's run records) + finalizer-resume-at-each-phase + merge-conflict / push-failure don't-delete-prematurely.
  - **Related:** the fixed wedge bug immediately below (this is its uncovered case + the open option C). The `partial`-vs-`blocked-on-human` classification gap (line 18) and the `AskUserQuestion` auto-deny bug (line 29) share the classifier/terminal-state surface and should land consistently with P0.3 / P1.6.
- [x] `/work --auto` run wedges open after the agent finishes — terminal `result` event never triggers a process-tree reap, so orphaned test runners strand the run as `running` for hours. _(Fixed 2026-06-04 — chose A+B: keyed teardown off the agent process `exit` and reap the process group.)_
  - **Issue**
    - The runner keys run-completion entirely on `child.on('close')` (`src/jobs/work-runner.ts:717`) — the `while (!done …)` generator loop (`:743`) only ends when the child's stdio streams all close. It does NOT key off the agent's terminal `result` event in the stream; that event flows through as an ordinary `output` line.
    - The child is spawned with `stdio: ['ignore', 'pipe', 'pipe']` (`:300`) and no `detached`. Any grandchild the agent spawns (e.g. a `vitest run` via a Bash tool call) inherits those stdout/stderr fds. If a grandchild hangs on open handles, the parent's pipes never close, `close` never fires, `done` stays false, and the generator parks indefinitely even though the agent already emitted `result`.
    - The only teardown path is `ctx.cancel()` → `child.kill('SIGTERM')` (`:746`), which signals the direct child only — not the process group. With no `detached: true` + `process.kill(-pid)`, orphaned grandchildren survive a cancel anyway.
    - Supervision detects the silence but can't fix it: the quiet-run nudge (`src/intent/supervision.ts`, project 11 Phase 4) fires at most once (`quietNudgedAt`, `:130`) and `isStalled` keys on `lastChildAliveAt` — a child-alive-but-silent run is "quiet," never "stalled," so it's never force-terminated. The run sits `running` until a human kills it.
    - Confirmed instance: run `19cd198f` (project 09-expand-cockpit, 2026-06-03). Agent emitted `result: success` at 20:48 CT after 25 commits (Phases 1–3 shipped, Phase 4 tests written), then the tree stayed alive ~3.5h. Four orphaned `vitest run` PIDs (spawned 18:14 / 19:13 / 19:35 / 20:25) held stdio open; the quiet nudge fired once at 21:07, then the run sat `running` with a no-output alert every minute until manually killed — only then did supervision flip it to `failed`.
  - **Fix options** (A is the real fix; C is the safety net)
    - A. Drive teardown off the terminal `result` event, not stdio close. When the stream yields the agent's terminal result, mark `done`, capture exit facts, and actively reap the tree (SIGTERM → grace → SIGKILL) instead of waiting on `close`.
    - B. Spawn with `detached: true` and kill the whole process group (`process.kill(-child.pid, …)`) on both teardown and cancel, so orphaned grandchildren (hung vitest) are always reaped — `:746` currently signals only the direct child.
    - C. Escalate supervision past the one-shot nudge: if a run is quiet (or a `result` was seen) and hasn't closed within N seconds, force-kill the tree and let classification proceed from the captured result — a watchdog mirroring `planning-expiry`, so the cockpit self-heals without a human.
    - D. (defense-in-depth) Prevent the leak at the source: wrap test invocations in the run with a hard timeout / guaranteed-exit flags so a single hung `vitest` can't wedge the whole run.
  - **Resolution (2026-06-04)** — A + B in `work-runner.ts`.
    - **Key off `exit`, not just `close`.** Added a `child.on('exit')` handler that captures the agent's real exit code/signal and reaps. `exit` fires when the agent process itself terminates; `close` additionally waits for every inherited stdio fd to drain — which a hung grandchild blocks forever. Keying off `exit` is also why the run no longer mis-classifies: it records the agent's clean exit (e.g. 0) instead of a SIGTERM, so a completed run isn't force-killed into `failed` (the confirmed 09 symptom). Note: chose the process `exit` event over the doc's "terminal `result` event" — `result` arrives while the agent is still alive, so reaping on it would SIGTERM the agent and re-introduce the false `failed`.
    - **Reap the process group.** Spawn with `detached: true` so the child leads its own group, and reap via `process.kill(-pid, …)` (new `killProcessTree`, indirected behind a test seam). On `exit`: SIGTERM the group, escalate to SIGKILL after `REAP_SIGKILL_MS` (3s), and force the run to complete after `REAP_FORCE_DONE_MS` (10s) if `close` STILL never fires. Orphaned grandchildren keep the leader's pgid, so the group signal reaches them even after the agent exited.
    - **Cancel reaps the tree too.** `ctx.cancel()` now group-kills instead of signalling only the direct child, so a cancelled run no longer leaves orphaned grandchildren.
    - **Tests.** +2 (detached spawn; a wedged run — agent `exit`s but stdio never `close`s — still completes and reaps the group via SIGTERM→SIGKILL).
    - **Still open:** option C (supervision watchdog) and D (hard test timeouts) remain as defense-in-depth; the `partial`-vs-`blocked` classification gap (the line-18 bug) is separate.
- [x] `/work --auto` doesn't resume an interrupted project — every invocation re-forks a fresh branch off `main` and silently restarts the project from Phase 1, duplicating already-shipped work. _(Fixed 2026-06-04 — chose A + D, with a GC guard.)_
  - **Issue**
    - Each run derives a brand-new branch from its own id: `const branch = rune-work/${descriptor.id.slice(0, 8)}` (`src/jobs/work-runner.ts:195`). There is no lookup of the project's prior in-progress branch.
    - `createWorktree` is called with that fresh `branch` and **no `startPoint`**, so `git worktree add -b <branch> <path>` forks off **HEAD of the product repo (`main`)** — "no implicit base — git uses HEAD of the repo" (`src/jobs/sandbox-runtime.ts:179` doc + `CreateWorktreeOpts.startPoint`). A run's commits therefore only carry forward if its branch was merged to `main`.
    - When a run ends without merging (killed, `failed`, GC'd, or just left unmerged like every real run so far), its commits stay stranded on `rune-work/<old-id>`. The next `/work --auto` forks a clean tree off `main`, reads `tasks.md` with every box unchecked, and concludes the first unchecked task is Phase 1 — restarting from scratch.
    - This is arguably worse than the wedge bug above: the wedge is loud (no-output alerts), but a silent re-fork looks like a healthy run while it redoes finished work and, on approval, could double-write artifacts (new project dirs, duplicate commits).
    - Confirmed instance: run `19cd198f` shipped 25 commits (Phases 1–3 + Phase 4 tests) on `rune-work/19cd198f` (tip `a21bc44`), never merged to `main`. After it was killed, a "continue" spawned run `d5373941` off `main` (`9335c1b`) — merge-base with the work branch is `e445c04` (pre-work), `a21bc44` is not an ancestor, all Phase 1–3 build files absent from the worktree. Transcript: agent read `tasks.md`, found it all-unchecked, declared "the first unchecked task is Phase 1's `backlog-parser.test.ts`," and began rewriting Phase 1 tests. The 25 commits were never lost — just ignored.
  - **Fix options** (A is the real fix; D is the cheap guard)
    - A. Resume the project's existing branch. Before creating a worktree, look up the latest unmerged `rune-work/*` branch (or a stable per-project branch like `rune-work/<slug>`) for this project and either check it out or pass its tip as `createWorktree`'s `startPoint`. Use a stable per-project branch name instead of a per-run id so resumption has a deterministic target.
    - B. Reconcile `tasks.md` against reality, not just the checkbox state: if the forked tree's `tasks.md` is all-unchecked but a prior run's branch shows committed progress, refuse to restart from Phase 1 and surface the conflict.
    - C. On run end, decide branch disposition explicitly — merge to `main`, or persist the branch as the project's resume point and record it in the registry/run index — so "where does the next run continue from" is never implicit.
    - D. (cheap guard) Before spawning, detect an unmerged `rune-work/*` branch for the same project whose tip is ahead of `main` and block/alert instead of silently re-forking, so a duplicate-work restart is never silent.
  - **Resolution (2026-06-04)** — A (real resume) with a stable per-project branch, plus a D-style guard built into the resume path and a GC fix.
    - **Stable branch name.** `work-runner.ts` now derives the branch from the project, not the run id: `workBranchName(slug)` → `rune-work/<slug>` (was `rune-work/<short-id>`). This is the deterministic resume target. Per-project run cap is 1 and the worktree path is already per-project, so only one run is ever on the branch at a time.
    - **Resume in `createWorktree`** (`sandbox-runtime.ts`). When the requested branch already exists (probed with `git show-ref`, chosen over `rev-parse` so it never conflates with the HEAD capture) and no explicit `startPoint` forces a fresh base, the worktree checks the branch out (`git worktree add <path> <branch>`, no `-b`) instead of forking off `main`. The prior commits are present, `SandboxSpec.resumed` is set, and `baseSha` becomes the branch's pre-run tip so the work product is only this run's delta. A non-existent branch keeps the original fresh-cut path.
    - **No restart-from-Phase-1.** On a resumed run the runner prepends a note to the agent prompt: prior commits are present, do not restart, continue from the first incomplete task — killing the exact symptom from run `19cd198f`/`d5373941`.
    - **GC guard** (`work-run-gc.ts`). Because the branch is now shared across every run of a project, GC must not force-delete it when an old run ages out. Phase C now skips any branch still referenced by a retained run; the branch is pruned only once its LAST run ages out.
    - **Tests.** +6 unit tests (resume vs fresh in `createWorktree`, stable branch name + resume-note prompt in the runner, shared-branch protection in GC). Full suite green except one pre-existing, environment-driven `claude.test.ts` `WORKSPACE_DIR` failure unrelated to this change.
- [x] Daily processing shouldn’t include any of the weekly, monthly, quarterly, or yearly reviews as part of the processed content
- [x] The morning prep should include at the top this week’s goals
- [x] Whoop numbers don’t match what’s in the app. I think there’s a date mismatch (pulling recovery and sleep for the day before, not this morning)
- [x] `/work --auto` runs are blocked by the harness permission gate, so every run exits clean with zero work product (silent no-op). Surfaced by project 11 — see `docs/projects/11-work-run-observability/phase-6-diagnosis.md`.
  - **Issue**
    - Validation run `7b8410fb` (project 10, 2026-06-01) classified `noop`: 0 commits, 0 task transitions, clean tree, `exitCode: 0`. Same structural signature as the original `7828477a` / `3b002b26` silent runs that motivated project 11. Blocks the "real/productive run" half of Phase 6 validation.
    - Transcript shows 75 tool calls; the agent did the analysis, then every mutation was refused. Root cause is the spawn site `src/jobs/work-runner.ts:282`, which launches headless `claude -p` with **no permission flag** — no `--permission-mode`, no `--allowedTools`, no `--add-dir`. Default mode prompts for approval on every mutating/non-allowlisted tool, and headless (`-p`, no TTY) there is no approver, so the prompt auto-denies. Three distinct failure modes:
    - **A. Default permission mode denies all mutations (core cause).**
      - `This command requires approval` (~16): `npm install`, `npm ci`, `npm run build`, `npx vitest run …`, `tsx …`, `node -e …`, `ln -s …`.
      - `contains multiple operations. The following part requires approval` (~6): anything piped, e.g. `… 2>&1 | tail -20`, `git show … | head -40`.
      - `Claude requested permissions to write … you haven't granted it yet` (3): the two `Edit` calls on `tasks.md` (why no checkbox could be ticked → 0 task transitions), plus a `Glob` read.
      - `Contains simple_expansion` (1): `echo $PATH`.
    - **B. Allowed working dir is only the worktree.** `cwd: sandbox.worktree` is the sole allowed dir (the `--add-dir` was deliberately dropped, `work-runner.ts:278`), so reaching the parent repo was blocked: `ls …/rune/node_modules → "may only list files in the allowed working directories: …/.worktrees/rune/10-rune-identity-refactor"`; `Glob node_modules @ …/rune → "you haven't granted it yet"`.
      - **C. Worktree has no `node_modules`.** `ls node_modules → No such file or directory`. Git worktrees don't carry the gitignored `node_modules`, so there's no local `vitest`/`tsx`. Reaching the parent's was blocked by B; `npm install` / symlink blocked by A. Dead end on tests even if A and B were fixed.
  - **Fix** (all three, in order)
    - A. Spawn with real permissions at `work-runner.ts:282`. The run is a throwaway worktree on a GC'd branch that can't touch `main` — the isolated sandbox `--dangerously-skip-permissions` is built for. Conservative alternative: `--allowedTools` covering `Edit`, `Write`, `Bash(git:*)`, `Bash(npm:*)`, `Bash(npx:*)`. Note `--permission-mode acceptEdits` alone is insufficient — it unblocks `Edit` but still prompts (→ denies) on `git`/`npm` Bash.
    - B. If using the allowlist route (not skip), re-add `--add-dir <PROJECT_ROOT>` so the node_modules symlink target resolves. Moot under `--dangerously-skip-permissions`.
    - C. Make deps available in `createWorktree` (`src/jobs/sandbox-runtime.ts`): symlink the parent's `node_modules` into the worktree (fast) or `npm ci` at setup. Required regardless of A/B or the run can't run tests.
    - D. (defense-in-depth) Fail fast: if the first mutating tool call is denied, classify `failed` with reason `permission-denied` rather than running 428s to a `noop` (depends on project-11 fix #1 surfacing the denial event).
  - **Status (2026-06-01)** — A + B + C implemented in `b3ce3b3`: work-runner spawns with `--dangerously-skip-permissions` (mirrors `execClaude`), `createWorktree` symlinks the repo's `node_modules` into the worktree (`linkWorktreeDeps`), B moot under skip. Unit-tested (77 pass). **D deferred** (defense-in-depth) and **not yet validated by a real productive run** — that's the open project 11 Phase 6 task. Leave this open until a watched run lands real commits.
- [x] Registry doesn't auto-rebuild. Cockpit shows a stale project list until `logs/registry.json` is manually regenerated. _(Fixed 2026-06-03 — chose A + B.)_
  - **Issue**
    - `buildRegistry` and `writeRegistry` exist in `src/intent/registry.ts:127-162` but no code path calls them.
    - `logs/registry.json` was last written by hand on 2026-05-22.
    - `handleApiCockpit` in `src/server/webview.ts:180-214` reads the stale file on every cockpit request.
    - Symptom: new projects don't appear and status changes don't propagate. Projects 09 and 10 were missing until manually rebuilt on 2026-05-27.
  - **Fix options** (minimum viable is A + B)
    - A. Call `buildRegistry` + `writeRegistry` on Rune startup. ✅
    - B. Add the rebuild to the nightly job for safety. ✅
    - C. File-watcher on each product's `docs/projects/index.md` for instant rebuild.
    - D. Cockpit endpoint or CLI command to trigger a manual rebuild.
  - **Resolution (2026-06-03)**
    - New `src/jobs/registry-rebuild.ts`: scans `policies/products.json`, reads each product repo's `docs/projects/index.md` (lifecycle status) and each project's `tasks.md` (task progress), and writes a fresh registry.
    - Wired to run on daemon startup (`src/index.ts`) — so the "Restart server" button also refreshes the cockpit — and as a nightly step (`Registry rebuild` in `src/jobs/nightly.ts`, before the journal-intent producer).
    - The registry now carries per-project task progress (`RegistryProject.progress`), so the cockpit renders progress bars for **every** product, not just rune. `handleApiCockpit` overlays a live read of rune's own `tasks.md` so rune cards stay real-time.
- [ ] Intermittent vitest hang unresolved — An intermittent open-handle vitest hang in Rune's orchestrated runs was diagnosed but not reproduced or fixed; the plan is to capture the handle stack the next time it fires. (journal 2026-07-08)
