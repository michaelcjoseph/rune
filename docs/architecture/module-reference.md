# Module Reference

Per-file reference for every `src/` module, plus root tooling config, `cli/`, `scripts/`, and `policies/`. This is the deep detail extracted from `CLAUDE.md` — read it on demand when working in a given area. `CLAUDE.md` carries only the area-level module map; the per-file annotations and project-phase history live here.

The annotations preserve implementation invariants and the project-phase forensics ("project 13 Phase 1c", "project 15 P0.4", etc.) that explain *why* a given seam exists. Security/durability invariants that any change must preserve are also summarized in `CLAUDE.md` → Key Conventions → Invariants.

---

## Entry point & config

- **`src/index.ts`** — Entry point: validates the full operator environment via `assertOperatorConfig()` before boot side effects, then boots HTTP server, Telegram bot, scheduler. Startup sequence: `reconcileOrphans()` → `await runRecoveryFinalize()` (project 15 P0.4 — drives stale `running` supervised runs to a real terminal state through the hold-mode finalizer, classified on work product, while their worktrees still exist; awaited BEFORE the sweep) → `recoverSupervisedRuns()` (fallback: flips any run that couldn't be finalized `running` → `unknown`, fail-safe) → `restorePlanningSessions()` (reload persisted planning sessions, fail-safe) → `await cleanupOrphanWorktrees()` (startup-gated before Telegram polling, HTTP/server, and scheduler dispatch surfaces) → `runWorkRunGc()` (fire-and-forget — prunes `logs/work-runs/` artifacts over retention caps). Calls `startStallCheck(bus)`, `startMcpWatchdog(bus)` (MCP monitoring redesign), and `startPlanningExpiry()` after `startScheduler()`; `stopStallCheck()`, `stopMcpWatchdog()`, and `stopPlanningExpiry()` in shutdown. `persistPlanningSessions()` called on clean shutdown and uncaughtException paths. Does not mount `/mcp`; the Claude App connector lives in the standalone MCP daemon. Registers all mutation appliers. `shutdown()` (shutdown-park): re-entrancy guard (`shuttingDown`); arms `setMutationShutdownInProgress(true)` immediately after logging, BEFORE `killActiveProcesses()`, so a SIGTERM'd orchestrated-work child can't clobber a boot-resumable run; after `waitForActiveProcesses()` (children dead → worktrees stable) awaits `parkInFlightOrchestratedRuns()` and logs the `{parked, resumable, skipped}` summary — best-effort, a parker throw is caught and never blocks shutdown.
  Boot-time orchestrated recovery now delegates all eligibility checking to `preflightOrchestratedRecovery` before redispatch; non-resumable entries continue into orphan reconciliation instead of receiving a bespoke startup-only decision.
- **`src/config.ts`** — Typed env vars and constants. Telegram operator credentials are lazy getters so scoped MCP subprocesses can import shared modules without dummy credentials; `assertOperatorConfig()` eagerly validates them (and `VAULT_DIR`) for the main Rune process.
- **`src/cancellation.ts`** — Provider-neutral cancellation domain types shared by live transport tracking, model executors, team workflow, and durable work-run storage: `CancellationSource` (`telegram`/`cockpit`/`internal`) and `OperationCancellation` (`operationId`, source, request timestamp).

## `src/ai/`

- **`chat.ts`** — Provider-neutral conversational adapter: resolves the selected alias through `model-policy.json`, then calls the Claude or Codex primitive with the common chat system prompt, voice, scope/cwd, environment, explicit `ChatAuthority`, operation metadata, and—only for `product-full-access` chats—the dedicated strict MCP registration from `product-chat-mcp.ts`. `ChatRequest` is a discriminated union that requires product, repo cwd, and scoped root for full access. Claude conversations retain CLI session ids; Codex conversations opt into persistent threads and resume only with a non-empty thread id whose persisted authority, effective cwd, and scoped root exactly match. Explicit legacy `writeEnabled` metadata is normalized; metadata-less or threadless Codex executors rotate with bounded transcript replay. Every Codex chat—global or product—gets the secret-scrubbed chat environment; a failed first turn removes its newly-created rollout via `cleanupCodexThread`.
- **`chat-turn-lock.ts`** — Keyed promise queue for complete provider-neutral turns. Serializes one transport/user/scope conversation from session read through model call and persistence while distinct users/transports/products remain concurrent; a rejected turn does not poison the queue.
- **`codex-sessions.ts`** — Best-effort Codex rollout cleanup on provider switch, session deletion, or failed first turn. Validates the thread id before scanning `$CODEX_HOME/sessions`, deletes only matching filenames, and never lets cleanup failure break `/fresh` or `/clear`.
- **`isolated-mcp-config.ts`** — Shared encoder for one isolated stdio MCP registration. Produces Claude's strict inline config and Codex's required replacement `mcp_servers` table from the same command/environment/tool allowlist/timeouts, keeping product-chat and artifact-role provider posture equivalent. Fails closed on a blank server/command/tool name, an empty allowlist, or a non-positive/non-finite timeout before either provider is spawned.
- **`product-chat-mcp.ts`** — Builds the isolated product-chat MCP registration through `isolated-mcp-config.ts`. Validates the product slug, launches the dedicated stdio entry with a small explicit non-secret environment carrying server-owned `RUNE_PRODUCT_CHAT_PRODUCT`, and pins the exact seven-tool allowlist. Applied on fresh and resumed configured product turns; Home/global and unresolved products do not use it.
- **`claude.ts`** — All Claude CLI spawning: `askClaude`, `runAgent`, `summarizeSession`. Exports `setBus(bus)` (called from index.ts so user-visible `runAgent()` calls can emit `BusAgentEvent` frames; type-only `NotificationBus` import avoids circular dep). `ClaudeResult` carries optional structured `OperationCancellation`; `execClaude` captures it before unregistering on both child `close` and `error`, so a cancellation/error race cannot erase attribution. `runAgent()` appends `{agent, startedAt, durationMs, status}` to `logs/agent-runs.jsonl` after each invocation; its `userVisible=false` path does not read `TELEGRAM_USER_ID`, emit operator bus frames, or register an operator-visible in-flight op, so scoped/background subprocesses need no fabricated Telegram identity. Exports `CLAUDE_BIN` (resolved binary path), `registerActiveProcess`/`unregisterActiveProcess` (for external spawners like work-runner), and the product-chat environment builder reused by the chat adapter. `runAgent()` resolves each agent's model through the model selection policy (`src/intent/model-policy.ts`) — pin → role-default → global-fallback. Also accepts an optional `writeScope?: AgentWriteScope` (`{cwd, writableDirs}`) threaded into `execClaude` — overrides the default vault cwd and adds each `writableDir` via `--add-dir` so an agent (e.g. project-setup-writer scaffolding into a target product repo) gets real write access there; omitted = legacy vault-cwd behavior. The four entry points (`askClaude`, `askClaudeOneShot`, `runAgent`, `askClaudeWithContext`) each accept an optional `voice` flag (default false) — when true, the writing-voice block is appended via `--append-system-prompt`. `askClaudeOneShot` additionally accepts a trailing `opts: {systemPrompt?, model?}` — systemPrompt rides `--append-system-prompt` composed BEFORE the voice block (the writer SOUL channel), model overrides `ONESHOT_MODEL`. Claude-only fallbacks remain Claude aliases even though the application chat default is Codex-backed. Agent frontmatter `tools:` semantics: a block list becomes `--allowedTools`; the inline form `tools: []` (`AgentDef.noTools`) declares an explicitly TOOL-LESS agent — the empty list rides inside the `--agents` JSON, stripping the subagent's toolset at the availability level (effective under `--dangerously-skip-permissions`), for single-pass synthesis agents like kb-query; an omitted field leaves CLI default tools.
- **`codex.ts`** — Codex CLI spawn primitive for Layer 5 multi-model dispatch and provider-aware chat: `resolveCodexPath()`, `getCodexBin()` (lazy — Rune boots without Codex installed), `isCodexAvailable()` (non-throwing probe), `isCodexLoggedIn()` (spawns `codex login status`, 10s hard timeout, reads + drains BOTH stdout and stderr — the current Codex CLI prints the "Logged in" marker to stderr with stdout empty; returns `Promise<boolean>`), `probeCodexProvider()` (combined binary + login probe, returns `ProviderAvailability`), `runCodex(prompt, opts)`. Types `RunCodexOpts` + `CodexResult` + `CodexSandboxMode` + `ProviderAvailability`; `CodexResult` optionally returns the shared structured cancellation record, captured before unregister on child `close` or `error`. `persistentSession` creates a resumable thread, `resumeSessionId` uses `codex exec resume`, and chat calls can register cancellable in-flight operation metadata. Existing dispatch, role, critique, and other callers remain ephemeral unless they explicitly opt in. `RunCodexOpts.env` is the sandbox seam — sandbox callers (A5.2 `dispatchToExecutor`) must pass `buildSandboxEnv()`; default `process.env` is only safe for non-sandboxed Rune-internal runs. `configOverrides` appends argv-safe `-c key=value` pairs; `ignoreUserConfig` adds `--ignore-user-config` for controlled automation while leaving project config and explicit overrides active. The discriminated `externallySandboxed` alternative requires `sandboxProfilePath`, wraps the Codex binary in `/usr/bin/sandbox-exec -f <profile>`, emits `--dangerously-bypass-approvals-and-sandbox`, and rejects any `sandboxMode`; artifact Codex uses it so the outer profile is the only Seatbelt layer. Non-external callers cannot pass a profile path. Registers each child via `registerActiveProcess`/`unregisterActiveProcess` from claude.ts for unified graceful shutdown.
- **`tool-labels.ts`** — Friendly labels for streaming tool-use events; `formatToolUse`, `scrubPathsInText` (reused by work-run transcript adapter).

## `src/bot/`

- **`telegram.ts`** — Bot init: `createBot()` factory + `wireHandlers(bot, sender)` wires message + callback_query events after senders are ready. The callback_query handler (Phase 6 C6.2) auth-gates by `TELEGRAM_USER_ID`, acks the query, then routes composite-id payloads through `dispatchApprovalStatus` (shared with the cockpit inbox) or falls back to `dispatchText` for conversational values (slash-prefixed values are rejected to prevent inline buttons from triggering destructive commands).
- **`handlers/text.ts`** — Command routing + multi-turn conversation handler. `handleTextMessage(sender, msg)` — no direct bot dependency. Exports `dispatchText(sender, userId, text)` shared with webview. Default conversation turns serialize the complete session transaction through `withChatTurnLock`, derive one `ChatAuthority` from product scope plus workspace resolution, call the provider-neutral adapter, and persist its executor binding; `/gpt-5.6-terra` joins the Claude model-switch commands, resetting the executor only when the provider changes. Only configured `product-full-access` chats add the three Cockpit diagnostics to `PRODUCT_CHAT_TOOLS`; Home/global and unresolved product chats retain `CONVERSATION_TOOLS`. Active-planning-session check takes routing priority over the default conversation handler — free-form text routes through `routeToPlanning` → `handlePlanningTurn` when a planning session is active. `routeToPlanning` appends "— spec proposed · /approve to scaffold · /clear to abandon" footer when the planning handler returns spec-proposed status. `/approve` is wired in `dispatchText` but intentionally excluded from `SLASH_COMMAND_METADATA` (approval is an explicit gate, not resolver-inferred).
- **`handlers/url.ts`** — URL detection, fetch, content-triager agent, routing.
- **`handlers/photo.ts`** — Photo download, photo-classifier agent, routing.
- **`skill-registry.ts`** — Resolver skill registry: `SkillEntry`, `SLASH_COMMAND_METADATA`, `buildSkillRegistry`, `getSkillRegistry` (cached), `reloadSkillRegistry`.
- **`resolver.ts`** — Classify free-form TG messages against skill registry via Haiku; returns `ClassifyResult {skill, args, confidence, second_skill, second_confidence, ambiguous}`.
- **`work-run-release-callback.ts`** — Project 13 Phase 1c — Telegram release callback handler: `parseWorkRunReleaseCallback(data)` recognises `work-run-release:<id>` and `work-run-release-confirm:<id>` callback payloads; `dispatchTelegramWorkRunRelease(sender, userId, data)` delegates to the shared `requestWorkRunRelease` runtime (dirty-confirm path sets `{confirmDirty:true}`), replies with `formatReleaseRequestReply`.

### `src/bot/commands/`

One file per slash command:

- **`fresh.ts`** — `/fresh`: summarize active chat, append to journal, optionally enqueue KB-worthy summary, commit, reset session; also abandons any active planning session. Claude-backed chats summarize through their CLI session; Codex-backed chats summarize the stored transcript through the provider-independent transcript summarizer. Exports `closeConversation` helper reused by `/journal`.
- **`fresh-full.ts`** — `/fresh-full`: verbatim conversation transcript logging (no summarization).
- **`clear.ts`** — `/clear`: discard active session without journaling; also abandons any active planning session.
- **`journal.ts`** — `/journal`: append literal entry to today's journal; if a chat session is active, also calls `closeConversation` (mirrors `/fresh`).
- **`ask.ts`** — `/ask`: one-shot freeform Claude question (legacy escape hatch; no longer a resolver route).
- **`kb.ts`** — `/kb`: one-shot knowledge base query (legacy escape hatch; no longer a resolver route).
- **`ingest.ts`** — `/ingest`: enqueue vault file for KB ingestion.
- **`status.ts`** — `/status`: system health overview.
- **`prep.ts`** — `/prep`: trigger morning prep.
- **`priorities.ts`** — `/priorities`: review/set daily priorities.
- **`daily.ts`** / **`weekly.ts`** / **`monthly.ts`** / **`quarterly.ts`** / **`yearly.ts`** — review sessions.
- **`health.ts`** — `/health`: health review session.
- **`blog.ts`** — `/blog <topic>`: dispatches a `writing` mutation (`createMutation('writing', WritingRunPayload)`) and acks immediately with branch + short run id; the applier drives the writing pipeline (research → draft → critique → revise → commit on `rune-writing/{slug}` of the `writing` product). Terminal ✍️/💥 arrives via the mutation bus.
- **`writing-critique.ts`** — `/writing-critique [--revise] <target>`: dispatches a `writing` mutation in critique mode — critique of the EXISTING `docs/rune/{slug}.md` into `docs/rune/critiques/{slug}.md` (revision optional, same branch). Slug via `writingTargetSlugSource` + `slugifyWritingIdentifier` so a path-shaped target never forks a second branch name.
- **`workout.ts`** — `/workout`: parses location/focus/free-text args and chunk-sends the result to TG; the goals/equipment/exercises/Whoop-recovery prompt build + workout-generator agent call + `logs/last-workout.json` persistence live in `src/health/workout-generation.ts` (extracted so the MCP `generate_workout` tool shares the same pipeline).
- **`done-workout.ts`** — `/done-workout`: TG staleness-confirm UX (10-minute "run again to confirm" window) wrapping the shared `src/health/last-workout.ts` read/format/clear helpers (extracted so the MCP `log_workout_done` tool shares the same artifact).
- **`syllabus.ts`** — `/syllabus`: current study syllabus progress and assignments.
- **`study.ts`** — `/study`: spaced-repetition session: quiz over due wiki concepts.
- **`family.ts`** — `/family`: family planning/review.
- **`career.ts`** — `/career`: career reflection/planning.
- **`learn.ts`** — `/learn`: append a runtime learning; auto-prepended to future agents.
- **`learn-list.ts`** — `/learn-list`: echo the current prepended learnings.
- **`cancel.ts`** — `/cancel [opId-prefix]`: SIGTERM the user's most recent in-flight op (or one selected by id prefix), stamping the accepted cancellation source as `telegram`.
- **`plan.ts`** — `/plan [product]`: start a PM-led scoping conversation scoped to a product; with a known slug creates a planning session and replies with the kickoff prompt; without args or with unknown slug lists registered products.
- **`approve.ts`** — `/approve`: approve the PM spec and run the automated downstream planning/scaffold pipeline; a thin orchestrator that gates (normal vs already-approved retry path), streams tech-lead breakdown → critique → context seed → scaffold progress, delegates the scaffold + linked-promotion drive to the shared `runScaffoldApproval` runtime (`src/jobs/scaffold-approval.ts`), maps outcomes to a chat reply (scrubAbsolutePaths-sanitized on failure), and uses a delete-on-success / leave-for-retry decision; not in `SLASH_COMMAND_METADATA`.
- **`library-sync.ts`** — `/library-sync`: trigger on-demand Lenny posts/podcasts sync via lenny-sync agent.
- **`seed.ts`** — `/seed`: bulk-seed KB from vault files via `seedAndProcess()`.
- **`cancel-review.ts`** — cancel an in-progress review session.
- **`active-context.ts`** — context listing/switching.

## `src/transport/`

- **`sender.ts`** — `MessageSender` interface, `SendOpts` (`approval?: {prompt, options[]}`) type, `createSenders(bot, bus)` factory; subscribes tg/webview to bus `message`, `agent-event`, `mutation-event`, and `op-event`; returns `{ tg, webview, destroy }`.
- **`notification-bus.ts`** — `NotificationBus`: typed event bus with publish/on/off. `BusEvent = BusMessageEvent | BusAgentEvent | BusMutationEvent | BusOpEvent`. `BusOpEvent` has subKind `start`/`progress`/`end` with opKind `agent`/`chat`/`one-shot`/`classifier`. `BusMutationEvent.subKind` widened with `start` (project 13 Phase 1a — carries the local-operator `operatorWorktreePath`). Fault-isolates failing subscribers.
- **`mutations.ts`** — Mutation pipeline: `MutationDescriptor`/`MutationKind`/`MutationStatus` types, applier registry, `createMutation()`, `cancelMutation()`, `activeRuns` map, `setMutationBus()`. AutoApprove appliers start immediately. Hooks into supervision-store at every state transition (seed on create, flip on startApply/completed/failed/crash, throttled heartbeat on output events). `buildSupervisedRun` carries both `lastChildAliveAt` and `lastOutputAt` params — output events advance `lastOutputAt` (LLM-output signal for quiet-run nudge), keep-alive/terminal writes thread the current value through so it is never reset. `MutationDescriptor` carries optional `outcome?: WorkOutcome` + `workProduct?: WorkProductFacts` (project 11 Phase 2) — populated by `applyOutcomeToDescriptor` in startApply's terminal branch (gated on `kind === 'work-run'`) BEFORE `appendMutationLine`. `MutationEvent.kind` union includes `start` (project 13 Phase 1a) — the `startApply` loop publishes it to the bus with NO supervision side effect and does NOT copy its data onto the descriptor (the un-scrubbed `operatorWorktreePath` it carries must never reach `mutations.jsonl`). Project 13 Phase 1b + AskUserQuestion: terminal supervision flip treats work-run/orchestrated events with `data.parked === true` as a supervision override and preserves `parkedQuestion` when present. `MutationKind` includes `work-run-release`, `work-run-answer`, and `writing` (the /blog / /writing-critique applier; its events pass the `publishDerivedRunEvent` run-feed gate alongside work-run/orchestrated-work); `MutationApplier.supervised?: false` suppresses createMutation supervision seeding for control mutations. **Shutdown-park suppression:** `setMutationShutdownInProgress(v)`/`isMutationShutdownInProgress()` — armed by `index.ts`'s `shutdown()` before `killActiveProcesses()`; while set, `startApply` suppresses ALL terminal/crash persistence and heartbeat upserts, but ONLY for `orchestrated-work` mutations — a SIGTERM'd child can't flip a boot-resumable `running` mutation to failed or clobber a just-written shutdown park. Other mutation kinds keep their existing crash semantics; on-disk state for a suppressed run is owned exclusively by `parkInFlightOrchestratedRuns` and next-boot recovery. The `startApply` finally also RETAINS a suppressed run's `activeRuns` handle (normal runs delete it) — the parker snapshots `activeRuns` only after the children are dead, and a suppressed unwind that removed its handle would hide the run from the parker entirely (codex-found race, 2026-07-08).
  Recovery safety adds an ephemeral `MutationDescriptor.recoverable?` projection (never persisted), `RunHandle.settled` (resolves after the applier's `finally`), and mutually exclusive per-run lifecycle claims for recovery handoff versus terminal teardown. While a recovery claim is held, the superseded orchestrated invocation suppresses terminal/crash persistence and destructive cleanup until cancellation has fully settled and redispatch is ready.
- **`in-flight.ts`** — Shared in-flight model-op registry: `registerOp`/`unregisterOp`/`cancelOp`/`cancelMostRecentForUser`/`listOps`. `OperationCancellation` records `{operationId, source:'telegram'|'cockpit'|'internal', requestedAt}`. `cancelOp` synchronously records the first request before SIGTERM; repeats preserve the original metadata, and executors read it before unregistering the live child. 5s heartbeat ticker emits op-event:progress; `setInFlightBus(bus)` wires bus emission; cancellation overrides the reported exit status.
- **`telegram-sender.ts`** — `TelegramSender` implements `MessageSender`; delegates to `sendLongMessage`; per-user typing timer map. `onMutationEvent()` sends one-line TG summary on completed/failed (gen-eval-loop terminals → structured ✅/⏸/💥 via `formatGenEvalLoopTerminal`; work-run terminals → outcome-aware `formatWorkRunTerminal`; project 15 Phase 3.5 specializes the branch-complete case by gated-merge disposition; project 13 Phase 1a `formatWorkRunStart`; parked-aware branch). Sentinel/manual parked runs render a Release inline button; `AskUserQuestion` parked runs render one answer button per option (`work-run-answer:<runId>:<optionId>`). The un-scrubbed `operatorWorktreePath` is delivered verbatim because Telegram (to `TELEGRAM_USER_ID`) is a local-operator surface — scrubbing exemption applies equally to the cockpit WebSocket (localhost-bound, auth-gated). `send()` with `opts.approval` renders an inline keyboard (one button per option, `callback_data = option.value`). `onOpEvent()` sends/edits/deletes "🤔 label · Xs · /cancel" tracker messages (10s edit throttle, skips classifier ops). `shutdown()` drains timers.
- **`webview-sender.ts`** — `WebviewSender` implements `MessageSender`; `register(userId, ws)`, `unregister(userId, ws)`, per-user WS fan-out; `onAgentEvent()`, `onMutationEvent()`, `onOpEvent()` forward bus frames to connected WS clients.
- **`approval-actions.ts`** — Phase 6 C6: transport-agnostic approval-actioning module shared between the HTTP cockpit (`POST /api/approvals/:id/{approve,reject}`) and the Telegram callback_query handler. `parseApprovalId(id)` splits a composite `<source>:<payload>` id; `dispatchApprovalStatus(id, status)` routes to per-source queue mutators and returns three-valued `ok`/`not-found`/`error` (HTTP maps `error` to 500). All writes wrapped in `safeWrite`. `blocked-on-human` Approve routes to `requestWorkRunRelease`; Reject dismisses the parked supervision row. `work-run-answer:<runId>:<optionId>` routes to `requestWorkRunAnswer`.

## `src/reviews/`

- **`session.ts`** — `ReviewSession` type, persistence, lifecycle management.
- **`orchestrator.ts`** — Review flow orchestrator: start, route messages, handler registry.
- **`interview.ts`** — Interactive interview phase for review sessions; review prep surfaces pending playbook drafts, Ask-Twice proposals, and journal-to-intent proposals for approval.
- **`worldview-drift.ts`** — Detect world-view changelog entries affecting active projects.
- **`kb-activity.ts`** — Scan `knowledge/log.md` INGEST entries → structured digest for review prep.
- **`daily.ts`** / **`weekly.ts`** / **`monthly.ts`** / **`quarterly.ts`** / **`yearly.ts`** — review handlers.
- **`health.ts`** — Health review handler.
- **`blog.ts`** — RETIRED stub (`export {}`): the legacy review-session `/blog` was replaced by the writing-product pipeline (`src/bot/commands/blog.ts` → `writing` mutation). The project-12 writer machinery it consumed (`composeWriterContext`, sentinel/capture) now feeds the pipeline's model stages via `src/jobs/writing-run-deps.ts`.
- **`planning.ts`** — Planning session store: `createPlanningSession`, `getPlanningSession`, `getActivePlanningSession`, `updatePlanningSession`, `deletePlanningSession`, `abandonActivePlanningSession`, `persistPlanningSessions`, `restorePlanningSessions`; sessions keyed by chatId, persisted to `logs/planning-sessions.json`. Exports `approveActivePlanningSession(chatId)` → `ApproveResult`. `StoredPlanningSession` carries an optional `promotionId` linking a backlog-Plan-opened session to its durable Promotion. `deletePlanningSession` is the single chokepoint every abandonment path funnels through, so it hosts `abandonLinkedPromotion`: when the deleted session has a `promotionId`, it advances the linked promotion to `planning-abandoned` (best-effort; a promotion-log failure never blocks session cleanup).
- **`planning-handler.ts`** — Multi-turn Socratic planning handler: `handlePlanningTurn` (drives one scoping turn through the `defaultScopingTurn` LLM call), `ScopingResult`/`ScopingTurn`/`PlanningHandlerDeps` types; injected `ScopingTurn` seam enables test doubles.

## `src/server/`

- **`http.ts`** — HTTP server: health, session capture, Whoop OAuth callback; mounts webview routes when `WebviewDeps` provided.
- **`auth.ts`** — `verifyAuth(req)`, `isAllowedHost(req)`, `safeCompare(a, b)` — cookie + host-guard auth helpers.
- **`mcp-transport.ts`** — `/mcp` Streamable HTTP route (project 16 Phase 2): `mountMcpRoute(opts?)` → handled-boolean handler with `closeAll()` teardown, `getActiveSessionCount()`, and (MCP monitoring redesign) `getSessionStats()` status seams; gate order host-allowlist 403 → FAIL-CLOSED bearer 401 → SDK `StreamableHTTPServerTransport`; per-session `McpServer` instances (default `createRuneMcpServer(APP_SURFACE_TOOLS)`); sessions open only via initialize POST (1MB body cap), evicted on close/DELETE + 30-min idle timer; a request on an unknown/stale session id (e.g. from before a daemon restart — sessions are process-local) 404s WITH a warn log, since the client surfaces it as an opaque tool failure. `McpSessionStat` (`{id, openedAt, lastSeenAt}`, id truncated to 8 chars) tracks per-session open/last-seen times for the daemon `/health` body and the cockpit monitoring panel.
- **`mcp-oauth.ts`** — Single-user OAuth 2.1 for `/mcp` (project 16 Phase 2): `createMcpOAuth({gateSecret, userId, now?, tokenTtlMs?, issuerBaseUrl?, loadState?, saveState?})` → `{handleOAuthRoute, verifyBearer}`. `tokenTtlMs` null=never-expire / undefined=1h / N=ms. `loadState`/`saveState` persistence seam keeps clients+tokens across restart. DCR (http/https redirect_uris only, MAX_CLIENTS=20 cap), consent-form gate, PKCE S256-only, codes single-use + redirect_uri/client-bound; tokens in-memory, userId-bound, expiry-checked per call; fail-closed `verifyBearer` never throws; serves RFC 8414 AS metadata + RFC 9728 protected-resource metadata (issuer pinned via `MCP_ISSUER_URL`, Host-header fallback local-only). `ClientRecord` now also carries a sanitized `clientName` (DCR is unauthenticated — treated as hostile input, control-chars stripped, ≤64 chars) + `createdAt`, both display-only, for the cockpit monitoring UI's registered-clients list.
- **`mcp-oauth-store.ts`** — File persistence for `/mcp` OAuth state: `readOAuthStore(path)`/`writeOAuthStore(path, state)` over a caller-supplied path (web legacy store or standalone daemon store), 0600, atomic temp-then-rename. Holds bearer tokens; revoke all = delete the relevant store file + restart.
- **`mcp-metrics-history-read.ts`** — Webview-side read layer over the shared MCP metrics history (MCP monitoring redesign): `readHistoryCached(file, opts?)` (mtime+size cache — the file changes once per ~60s flush tick, so a 5s cockpit poll re-parses only on a real change) + pure bucketing helpers `bucketHourly` (zero-filled trailing-N-hours, UTC boundaries), `bucketDaily` (zero-filled trailing-N-days, America/Chicago calendar dates via `toChicagoDate`), `perToolWindow` (per-tool call/error totals over a trailing window). Parsing/delta math stay in `src/mcp/metrics-history.ts`; this module never reimplements them.
- **`rune-run-metrics.ts`** — `readRuneRunMetrics(opts)`: Rune-side work-run metrics (active/parked run counts from the supervision store, terminal-outcome tally + p95 runtime + recent failures from the work-runs index). Previously defined but unwired; now consumed by `GET /api/mcp/monitoring` with real `config.SUPERVISED_RUNS_FILE`/`config.WORK_RUNS_INDEX_FILE`.
- **`read-body.ts`** — Shared bounded request-body reader (1MB default; destroys the socket on overflow, typed `BodyTooLargeError`).
- **`restart.ts`** — `restartServer()`: production-only daemon relaunch — fires a detached `launchctl kickstart -k gui/<uid>/$LAUNCHD_LABEL`; returns `RestartResult`; backs `POST /api/server/restart` and the cockpit "Restart server" button.
- **`webview.ts`** — `mountWebviewRoutes(server, deps)`: serves `GET /`, `GET /static/*`, the cockpit/home/backlog/planning REST API, `POST /api/mutations`, `POST /api/server/restart`, work-run routes, and `WS /api/ws`. `handleApiCockpit` reads run-status via `readCockpitRunStatus(config.SUPERVISED_RUNS_FILE)` (not in-memory `activeRuns`), layers `readWorkRunProjections` and per-product backlog counts, and feeds `buildCockpitView`. REST endpoints include `GET /api/cockpit`, `GET /api/backlog/:product`, `POST /api/backlog/:product/:kind`, `POST /api/backlog/:product/items/:id/plan`, `GET /api/promotions/:id`, `POST /api/promotions/:id/retry`, `POST /api/mutations/:id/cancel`, `POST /api/ops/:id/cancel`, `GET /api/work-runs/:id`, `GET /api/work-runs/:id/transcript`, `POST /api/work-runs/:id/release`, and (MCP monitoring redesign) `GET /api/mcp/monitoring` — `handleApiMcpMonitoring` composes the live `mcp_metrics_snapshot` tool call, the daemon `/health` body, registered OAuth clients (display fields only), file-backed history rollups (`mcp-metrics-history-read.ts`, 60s in-process cache), Rune-side run metrics (`rune-run-metrics.ts`), and active watchdog alerts (tolerant read of `config.MCP_WATCHDOG_STATE_FILE`) into one payload; every section degrades independently and the endpoint always answers 200 with `status:'degraded'` + a scrubbed error on any failure. The legacy `GET /api/mcp/tools/mcp_metrics_snapshot` endpoint is unchanged. `attachMcpMonitoring` (feeding the cockpit's `rune-mcp` product card) now also folds in the watchdog alert rollup — an active `daemon-down` alert flips the card's status to degraded even when the instant probe succeeded. `loadIndexHtml` templates `__OBSIDIAN_VAULT_NAME__` and `__IS_PRODUCTION__` into index.html; `index.html` is cached at mount time in production, re-read per `GET /` in dev. See `subsystems.md` for the full endpoint/behavior catalog.
  Active orchestrated mutations are enriched before `/api/state` and product deep-view responses with a short-TTL, promise-deduplicated `recoverable` result from the shared recovery preflight. `POST /api/work-runs/:id/recover` returns path-scrubbed 409 responses for unavailable recovery, 202 after successful redispatch, and reserves scrubbed 500 responses for internal/redispatch failures.
  `POST /api/ops/:id/cancel` passes the explicit `cockpit` cancellation source into the shared in-flight registry.
- **`backlog-actions.ts`** — Pure per-item action computation for `GET /api/backlog`: `computePlanAction`/`withActions` derive the plan action + `disabledReason` for each backlog item (precedence: planning-active > already-promoted > bug-done > loop-filed > parse-warning); types `BacklogItemAction`, `BacklogItemWithActions`, `BacklogDisabledReason`.
- **`webview-bootstrap.ts`** — `handleWebviewMessage(sender, userId, text)` — thin adapter over `dispatchText` for webview.
- **`projects-snapshot.ts`** — `getProjectSummaries()`: reads `docs/projects/index.md` + tasks.md per project; returns `ProjectSummary[]` with slug, status, task progress (done/total/perPhase), specPath, lastModified.
- **`state-snapshot.ts`** — `StateSnapshot` type + `getStateSnapshot()`: reads `logs/agent-runs.jsonl`, scheduler-state.json, active session/review, ingestion queue, playbook/proposal/intent counts, project summaries, active+recent mutations, in-flight Claude ops; used by `GET /api/state`.
- **`cockpit-run-status.ts`** — `mapVisibilityToRunStatus(visibility)`: pure projection from supervision `VisibilitySurface` → `RunStatusByProject`; `readCockpitRunStatus(filePath, now?)` wraps `readAllRuns` + `getVisibility` + mapper; blocked-on-human wins over running for the same project.
- **`work-run-projection.ts`** — `readWorkRunProjections(dir, indexFile, recent?, activeRuns?)`: reads `logs/work-runs/` index.jsonl + per-run summary.json (via `readWorkRunSummary`) + transcript.jsonl tail into a slug-keyed `WorkRunProjection` map; server-layer bridge between the jobs-layer store and the intent-layer cockpit view; best-effort. Holds the compile-time drift guard between jobs `WorkOutcome` and intent `WorkRunOutcome`. `transcriptUrl` points at `GET /api/work-runs/:id/transcript` when a transcript file exists.
- **`static/`** — Webview frontend (vanilla HTML/JS/CSS): index.html, app.js, app.css plus home-view.js, product-deep-view.js, run-feed-client.js, client-view.js, view-router.js. Cockpit sidebar, backlog drawer, planning panel, pending-approvals, product deep-view, real-time run-feed. Recover controls on project cards and Operations render only when the server-projected `recoverable === true`; `/api/state` overlays preserve that decision instead of inferring eligibility from mutation kind. The Home model selector defaults to GPT-5.6 Terra and synchronizes to the active webview session; product chat exposes the same Terra switch as a quick command. `monitoring-charts.js` (MCP monitoring redesign) — pure, NaN-safe inline-SVG chart builders (`renderSparkline`, `renderBarChart`); every color rides a CSS variable so charts inherit the app theme, minimal-ink design (baseline + marks + ≤3 sparse labels). `product-deep-view.js`'s `renderMonitoring` was rebuilt around `GET /api/mcp/monitoring`: KPI tiles with sparklines, a calls/errors bar chart, per-tool table, sessions/OAuth-clients list, and an alerts banner. `home-view.js`'s `renderMcpMonitoring` pill now shows uptime + session count + an `alert` status variant naming the active alert kinds when `mcp.alerts.count > 0`. Cockpit poll interval widened 1s→5s (the file-backed monitoring sections only change once a minute). See `subsystems.md` for the cockpit UX detail.

## `src/kb/`

- **`engine.ts`** — Orchestrates ingest/query/lint, processes ingestion queue.
- **`init.ts`** — KB directory scaffolding and schema initialization.
- **`ingest.ts`** — Copy source to raw/ → spawn wiki-compiler agent → entity-link touched pages. `determineRawDir()` routes by path (see `reviews-kb-vault.md` → KB raw-source routing).
- **`index-integrity.ts`** — Deterministic `knowledge/index.md` repair: scans `knowledge/wiki/**/*.md`, detects missing wiki links, and appends conservative summary lines under category headings without LLM calls.
- **`entity-extract.ts`** — `linkEntities()`: build alias map from JSON stores + `FAMILY_NAMES`, wikilink bare mentions in reference sections, append to `related:` frontmatter.
- **`query.ts`** — Single-pass retrieve-then-synthesize (RAG): all retrieval happens deterministically in-process, then the TOOL-LESS kb-query agent (`tools: []` frontmatter) synthesizes in one turn. The question is distilled to content terms (`contentTerms`, stopword-stripped — a raw question never matches page text literally); candidates are RANKED by weighted distinct-term coverage (`rankWikiPages` — rare terms dominate; an all-stopword question falls back to the raw-question `searchWithFilter`); the prompt injects the ranked candidates (+ their `knowledge/index.md` row summaries, snippets via `searchInFiles`, every quoted line capped at `MAX_SNIPPET_CHARS` = 300) AND the pre-fetched bodies of the top candidates (`MAX_PREFETCH_PAGES` = 8, `MAX_PAGE_BODY_CHARS` = 8k each, `MAX_TOTAL_BODY_CHARS` = 48k) so the agent never reads the ~185K-token index or re-does retrieval; when the search finds nothing it injects a bounded index excerpt instead (`MAX_INDEX_FALLBACK_CHARS` = 20k, degrade-to-nothing on an unreadable index). `QueryKBDeps.agentTimeoutMs` bounds the agent spawn (MCP callers pass 150s under the 180s `kb_query` wrapper override; Telegram `/kb` omits it and keeps the `CLAUDE_TIMEOUT_MS` default); `agentUserVisible:false` selects `runAgent()`'s background path for product-chat MCP synthesis, avoiding any operator/Telegram identity dependency.
- **`lint.ts`** — Spawn wiki-linter agent → health report.
- **`search.ts`** — ripgrep-based full-text search across vault + wiki; `searchVault`'s `directory` option is containment-guarded against the vault root (defense-in-depth — `vault_search` made it remotely reachable, project 16). `rankWikiPages(terms)` ranks wiki pages by weighted distinct-term coverage (per-term `rg -il`, weight = 1/files-matched, so rare terms dominate — an unranked alternation returns whatever files rg walks first); `searchInFiles(pattern, files)` pulls snippets from an already-ranked file set (both vault-containment-guarded, consumed by `kb_query`).
- **`queue.ts`** — JSON-file ingestion queue (enqueue/dequeue/clear).
- **`schema.ts`** — Default schema.md content for new knowledge bases.
- **`seed.ts`** — `seedAndProcess()`: enumerate vault files → enqueue new/mutable sources → process queue.

## `src/jobs/`

- **`scheduler.ts`** — Cron job registration: `startScheduler(bot)`, `stopScheduler()`.
- **`morning-prep.ts`** — Gather vault data → synthesize morning prep → write to journal.
- **`nightly.ts`** — Nightly orchestrator (18 steps): capture → daily tags → birthday alerts → playbook extract → registry rebuild → journal-intent producer → **Note triage** → journal ingest → meeting extract → library sync → KB queue → KB index repair → knowledge reconciliation → whoop activity → observation loop → learning loop → KB lint → mark processed → commit. `stepLearningLoop` reads machine-readable feedback records (`logs/feedback.jsonl`), applies a content-hash processed-marker (`logs/feedback-processed.json`), per-pass cap of 20, per-record 60s post-mortem timeout, per-record fault isolation; commits attributed lessons into `agents/<role>/memory.md`. `stepDailyTags` markdown side now handles only `health/nutrition.md` (ideas/writing-topics moved to the Note triage step, project 23). `stepNoteTriage` (project 23) runs after Journal-intent producer so the fresh product registry is available; delegates all I/O to `src/jobs/note-triage.ts`. **Dev rule:** when adding a nightly step, update `nightly.test.ts`'s step-count + ordered step-name snapshot (and any positional `steps[i]` index), and check `nightly.nosleep.test.ts`'s narrow `node:child_process` mock (it stubs `spawn` only — a step that transitively imports `execFile` makes the whole module fail to import as "0 test").
- **`capture.ts`** — Session capture logic (used by HTTP endpoint + nightly job).
- **`whoop-sync.ts`** — Whoop sleep sync (8am) + activity sync (nightly) + trends; `ensureWhoopSyncedForToday()` best-effort pre-sync for user-triggered handlers.
- **`playbook-extract.ts`** — Scan today's journal for `#playbook` tags → draft entries into `playbook-queue.json`.
- **`note-triage.ts`** — Note triage nightly step's I/O (project 23): `runNoteTriage(date, journal)` reads `policies/products.json` first (fail closed — no config, no LLM call, no writes), composes the prompt (registered-product table + project-page hints from `extractProjectPageHints` + delimited journal), calls the tool-less `note-triage` agent via `runAgent` (one retry on error/invalid JSON), then executes per-target-file writes with fault isolation per group: product `docs/projects/{ideas,bugs}.md` via `withFileLock` → `assertBacklogWriteAllowed` → title-dedupe (`containsNoteTitle`) → `writeFileAtomic` → `appendBacklogMutationLog` audit (no git commit — mirrors `fileTerminalBugsToBacklog`, leaves the product tree dirty for review); writing/research topics into the writing product's scoped `docs/rune/{writing-ideas,research-topics}.md` via the new `assertScopedTopicWriteAllowed` guard (file seeded on first write); vault new-product ideas via `readVaultFile`/`writeVaultFile` into `projects/ideas.md` (committed later by nightly's final commit). One failing target group never aborts the others.
- **`meeting-extract.ts`** — Scan today's journal for `#meeting` blocks → structured `Meeting[]` via `askClaudeOneShot`.
- **`book-summarizer.ts`** — Generate 1-2 sentence book summary via `askClaudeOneShot` (returns null on UNKNOWN).
- **`intent-scan.ts`** — Weekly Ask-Twice scan: reads `intent-log.jsonl` (last 30 days), groups via Haiku, dedupes against skill registry + pending queue, writes up to 3 proposals to `proposal-queue.json`.
- **`proposal-queue.ts`** — Proposal queue types + CRUD (`logs/proposal-queue.json`).
- **`mutations-log.ts`** — Append-only JSONL log for mutations (`logs/mutations.jsonl`): `appendMutationLine`, `readRecentMutations`, `reconcileOrphans` (flips stale `running` entries to `failed` at startup).
- **`work-runner.ts`** — `workRunApplier`: `MutationApplier` for `work-run` kind; spawns Claude CLI with `--output-format stream-json --verbose`, forcing the worktree-derived `RUNE_VITEST_CACHE_DIR` after inherited env. See `subsystems.md` → Work-run lifecycle for the full behavior (transcript tee, classification, finalizer, parked state, commit-poll). Stream scanning parks on either `RUNE_WORK_RUN_SENTINEL` or `AskUserQuestion` tool_use; user/system cancellation still takes precedence. `validate()` resolves product-scoped projects through `work-project.ts`, rejects when a `blocked-on-human` supervision record exists for the product+slug OR the worktree path exists, and shares the product+slug concurrency key with orchestrated work. Re-exports `workBranchName` from `src/intent/sandbox.ts` for back-compat. `WorkRunRuntimeDeps` injection seam with `__setWorkRunRuntimeForTest`/`__resetWorkRunRuntimeForTest`.
- **`work-project.ts`** — Shared project discovery for legacy and orchestrated work runners: `resolveLiveWorkProject` selects the payload product's configured `repoPath` (or `PROJECT_ROOT` for legacy product-less payloads), then `findWorkProjectDir` matches exact or numeric-prefix slugs under repository-root `docs/projects`. Product `scopePath` deliberately does not relocate lifecycle project metadata; config-unavailable, unknown-product, and project-not-found failures remain distinct.
- **`work-run-transcript.ts`** — Stream-json→display adapter + durable sink: `parseStreamJsonLine`, `streamJsonToDisplay`, `redactSecrets` (credential URLs, Bearer/sk- tokens, Telegram bot tokens, GitHub PATs, AWS keys, JWTs), `createRingBuffer` (bounded last-N line store), `createTranscriptSink` (per-run WriteStream to `<baseDir>/<runId>/transcript.jsonl`).
- **`jsonl-tail.ts`** — Shared fixed-byte text-tail primitive plus fixed-record JSONL adapter for model-facing diagnostics. Reads from the end, reports source truncation, drops a partial leading line, skips malformed JSONL records, and never loads the complete file; both transcript display tails and persisted JSONL readers use it.
- **`task-run-record-store.ts`** — Durable orchestrated `task-records.jsonl` append/read seam extracted from the runner. The diagnostic reader returns only the newest bounded records via `jsonl-tail.ts`; the runner re-exports the legacy functions for compatibility.
- **`work-run-diagnostics.ts`** — Product-scoped read-only diagnostic projection used by the Cockpit MCP tools. Resolves full or unique eight-character-prefix run IDs, requires a complete bounded supervision snapshot, and uses tri-state summary reads so missing evidence can fall back to supervision while corrupt/unreadable evidence fails closed. It authorizes all summary/supervision ownership evidence before artifact reads and returns bounded/redacted list, inspect, and active-run DTOs with the durable project-or-bug target identity (legacy records fall back to project) plus any durable nested-role cancellation correlation. It caps transcript source reads at 512 KiB and final JSON at 64,000 characters and recursively scrubs cancellation fields with every other DTO value; raw logs, secrets, and host/artifact/worktree paths are omitted.
- **`work-run-classify.ts`** — Terminal classification: `classifyOutcome(facts)` (branch-complete/partial/noop/dirty-uncommitted/failed), `parseTasks`/`computeTaskTransitions` (tasks.md delta), `computeWorkProduct` via injected `GitRunner` (scrubs host-absolute paths before they reach `mutations.jsonl`/bus), `finalizeWorkRun` (crash-safe single terminal event emitter), `applyOutcomeToDescriptor`.
- **`work-run-store.ts`** — Run-store: `writeSummary` (atomic temp-then-rename of per-run summary.json, including optional durable `{kind:'project'|'bug',slug}` target identity and `WorkRunCancellation {role,operationId,source,requestedAt}`), `appendIndexRow`, `readRecentIndex` (torn-line-tolerant), bounded newest-first index-tail readers for diagnostics, and `readWorkRunSummaryResult(dir, id)` (`found | missing | invalid`, validating the cancellation source enum while preserving corrupt/unreadable evidence for fail-closed authorization); `readWorkRunSummary` remains the nullable compatibility reader. Also owns `recordWorkRunPhase`/`readLastWorkRunPhase` (durable per-run finalize-phase store for gated-merge crash-resume).
- **`work-run-forensics.ts`** — `exportForensics(opts)` writes a reconstructable evidence bundle (diffstat.txt / status.txt / diff.patch / diff-staged.patch — redacted; bundle.git via `git bundle create`; untracked.tar) to `logs/work-runs/<id>/` BEFORE the worktree is destroyed; best-effort by contract.
- **`work-run-gc.ts`** — Retention GC: `planGc` (pure: selects terminal, unprotected runs to delete oldest-first to satisfy count+bytes caps) + `gcWorkRuns` (effectful; same-tick discipline; `isContainedIn` + branch-prefix + unmerged-branch guards before every destructive op — a `rune-work/` ref with commits not on its product's base branch is never pruned; fail-safe: an errored/unparseable `rev-list --count` keeps the ref).
- **`work-run-gc-runner.ts`** — Runtime glue: `runWorkRunGc(product?)` gathers live protected-set inputs and calls `gcWorkRuns` with the retention caps + per-product repo/base-branch maps from products config; swallows all errors (fire-and-forget callers).
- **`work-run-commit-poll.ts`** — `planCommitProgress(opts)`: parent-side throttle decision for the per-run commit-progress ping; `COMMIT_POLL_INTERVAL_MS` (10s), `COMMIT_PING_THROTTLE_MS` (10s), `SUBJECT_MAX_CHARS` (200); message format `📊 <subject> · X/Y tasks`.
- **`work-run-sentinel.ts`** — `parseWorkRunSentinel(text)` parses the `RUNE_WORK_RUN_SENTINEL { version, pendingCheck, command?, reason? }` line a `/work --auto` run emits to PARK for a human; fail-closed; `SENTINEL_FIELD_MAX_CHARS=500`; PURE.
- **`work-run-release.ts`** — Shared work-run RELEASE runtime: `releasePreflight(id)`, `runWorkRunRelease(payload, deps)`, `requestWorkRunRelease(id, opts?)` (ONE shared entry point for all surfaces), `formatReleaseRequestReply`, `workRunReleaseApplier` (`work-run-release` `MutationApplier`, autoApprove, `supervised:false`).
- **`work-run-question.ts`** — Pure parser for Claude `AskUserQuestion` assistant `tool_use` blocks; accepts `options`/`choices`/`answers` arrays of strings or `{label,value,description}` objects, assigns stable short option ids, and returns a malformed fallback so a seen-but-unparseable tool call parks instead of silently classifying.
- **`work-run-answer.ts`** — Shared answer/resume runtime for question-parked work-runs: `requestWorkRunAnswer(runId, optionId)` validates the parked record/question/worktree and creates `work-run-answer`; `workRunAnswerApplier` (`autoApprove`, `supervised:false`) spawns Claude in the existing worktree with the selected answer and its retained worktree-derived `RUNE_VITEST_CACHE_DIR` injected, appends to the original transcript, parks again on another question, or drives the existing gated finalizer.
- **`gen-eval-loop-runner.ts`** — `genEvalLoopApplier`: `MutationApplier` for `gen-eval-loop` kind; validates payload, per-product concurrency cap of 1, autoApprove:false. Exports `runGenEvalLoop(opts)`: orchestration core (generator claude/sonnet, evaluator resolved distinct-from-provider) → createWorktree → loop rounds → evaluate merge contract → mergeBranch on pass. `LoopSpawners` interface allows test injection.
- **`scaffold-approval.ts`** — Shared scaffold-approval runtime: `runScaffoldApproval(session, deps?)` drives an APPROVED planning session → scaffolded project files (+ linked promotion). Called by BOTH `/approve` and the webview approve route. `defaultScaffoldApprovalDeps()` is a lazy FACTORY; every effect injected for the unit test.
- **`sandbox-runtime.ts`** — Runtime complement to `src/intent/sandbox.ts`: transactional git worktree lifecycle (`createWorktree`/`destroyWorktree`/`cleanupOrphanWorktrees`) with exact provisioning postconditions and fresh Git/cursor revalidation before deletion. `linkWorktreeDeps(repoPath, worktree, scopePath?)` is ASYNC: it symlinks the parent checkout's `node_modules` for ordinary products, while direct Next.js projects (detected from repo-root `package.json` and, for scoped products, also a `scopePath`-joined `package.json` — JSON-parse guarded) receive a local copy instead, so Turbopack does not resolve dependencies outside its worktree root. The copy uses async `fs.promises.cp` (keeps the event loop free — this process also serves Telegram/HTTP/cron) into a sibling staging dir (`<worktree>.node_modules.staging`, outside the worktree so an interrupted copy never dirties git status there) and renames it into place atomically, so `node_modules` is only ever absent or complete; dangling symlinks are filtered out of the copy (dereference can't materialize them). Leftover staging dirs are removed on the next attempt and by the startup orphan sweep (never git-registered). `createWorktree` awaits it with `product.scopePath`. `vitestCacheDirFor(worktreePath)` derives an opaque deterministic `<os-temp>/rune-vitest-cache/<sha256(absolute-worktree-path)>`, and `removeVitestCache` removes only that cache best-effort during worktree destruction/reclaim/reconciliation/orphan cleanup. Parked worktrees retain their cache. Reads and validates `policies/products.json` via `readProductsConfig`/`getProductConfig`; all git calls go through injectable `GitRunner` seam (`defaultRunGit` exported); types `ProductConfig` (includes optional `orchestratedMode?`, validated `closeoutValidationStrategy?`, and the fail-closed `artifactMcp?: 'rune-kb-readonly'` policy) and `GitRunner`.
- **`artifact-mcp.ts`** — Product-scoped artifact-role MCP builder, fail-closed and macOS-only (`sandbox-exec`/Seatbelt is required). It validates Node/loader/broker/relay paths, a readable/searchable vault, and a writable worktree, then probes real shell startup plus a worktree write under the generated profile before starting the broker. The privileged broker has an empty environment, detached supervised process group, and receives the vault path only over stdin. Cleanup SIGTERMs then SIGKILLs the group if needed, unregisters it, and removes the private socket/profile/temp directory. The mode-0600 Seatbelt profile default-denies writes except lexical/real worktree, private runtime/temp, and device paths; denies configured/real vault reads and writes; hides lexical/real profile paths; denies reads of source Codex/Claude/SSH/cloud/config homes plus common secret literals; denies raw `localhost:*` TCP; and permits only the relay Unix socket. Provider-neutral sandbox env is just `TMPDIR`. Codex-only setup seeds a private runtime `HOME`/`CODEX_HOME` with only `auth.json`, keeping CLI auth/state/log/database writes inside the single outer profile while `--ignore-user-config` prevents inherited MCP/config and `shell_environment_policy.inherit="none"` prevents generated shell commands from inheriting auth/runtime env. Claude-format artifact sessions do not require or receive Codex auth. The shared `isolated-mcp-config.ts` encoder points strict Claude and replacement Codex MCP config only to the relay. Codex requires/pins/approves the three tools with 10s/60s timeouts; relay config has an empty environment and contains no fabricated Telegram fields, vault path, Rune secrets, `.env.local`, OAuth, or localhost transport.
- **`writing-product-orchestration.ts`** — Writing-run planning + orchestration (project 19 W4): `planWritingProductRun` (slug/branch/routePaths/migration/sourceAccess plan), overloaded `startWritingProductRun` — `(input)` returns the plan only; `(input, deps)` runs worktree → pipeline → validation guards → `StartedWritingProductRun` (`publish.mode:'branch-commit'`, never external deploy). `StartWritingProductRunDeps` = `createWritingWorktree` + deps-level `runWritingPipeline` + optional `deployExternal` (must never be called in V1). Exports `slugifyWritingIdentifier`/`writingTargetSlugSource`.
- **`writing-pipeline.ts`** — Pure writing state machine (`researching → drafting → critiquing → revising → ready-for-review → committed | failed`), deps-injected (`mcp.callTool`, `model.{plan,draft,critique,revise}`, `writeArtifact`, `commitArtifact`, `emitRunState`, optional `readArtifact`); `sanitizePublicMarkdown`/`collectPrivateSourceBoundaries` strip private research fragments before ANY artifact write. Critique mode (`input.critique {slug, outputPath, revisionRequested}`): judges the EXISTING `docs/rune/{slug}.md` (via `readArtifact`) into `outputPath`, optional revision, both committed on the draft's own branch; slug pre-derived by the caller. Module must stay vault-import-free (test-pinned).
- **`writing-run-deps.ts`** — Production writing deps (the seam that made /blog a no-op — docs/projects/bugs.md): `createProductionWritingWorktree` (createWorktree on the `writing` product, `rune-writing/{slug}` create-or-resume); `buildWritingPipelineDeps` — in-process dispatch to the real MCP tool handlers with input translation (`journal_range {maxDays}` → `{startDate,endDate}`, `follow_wikilinks {query}` → `{text}`), writer-SOUL one-shot model stages (`composeWriterContext` + voice + policy-resolved `writer` model when claude-format, else `ONESHOT_MODEL`), containment-asserted artifact io, branch-verified git commit in the worktree; every dep records its first failure (`recordFailure`) and polls cancel at stage boundaries. `buildProductionStartWritingDeps` bridges the deps-level shape to the core input and THROWS the recorded failure on a `failed` result; `deployExternal` deliberately absent.
- **`writing-run-runner.ts`** — `writingRunApplier`: `MutationApplier` for `writing` kind (autoApprove — explicit user command). apply(): writing-repo preflight → same-slug in-flight guard (shared worktree path) → pipeline states bridged to `output` events (run-feed log lines) → terminal `completed {outcome:'branch-complete', commitSha, routePath}` or `failed` (stage-attributed, path-scrubbed reason) → `destroyWorktree` in finally on success AND failure (committed work lives on the branch). 30s keep-alive/activity ticker shields silent one-shot model calls from the kind-agnostic quiet→cancel backstop.
- **`writing-user-reachability.ts`** — Assertable half of project-01 Phase C's `user-reachability-check` (deps-injected, no production caller yet): dispatch `/blog` → observe terminal → assert branch/target/pages on the michaelcjoseph.com branch.
- **`credential-injector.ts`** — Spawn-time env map builder for sandboxed Regime B runs: `readCredentials(path)`, `getBaseEnv(allowlist)`, `buildSandboxEnv(sandbox, opts)`; `DEFAULT_BASE_ENV_KEYS`. `getBaseEnv` routes the `PATH` key through `buildToolchainPath` (imported from `../utils/toolchain-path.js` — the builder itself moved there since it has no secret-handling role). `buildSandboxEnv` assigns Rune's worktree-derived `RUNE_VITEST_CACHE_DIR` last, after base env and product credentials. **Invariant:** only the run's own product credentials reach the child, and Rune's own secrets (`TELEGRAM_BOT_TOKEN`, etc.) never reach the child.
- **`egress-policy.ts`** — Runtime egress enforcement wrapper: `checkEgress(sandbox, host, opts)` delegates to `isEgressAllowed` and writes denied attempts via `appendEgressDenialLog` to `logs/egress-denials.jsonl`. `EGRESS_ENFORCEMENT_MODE` (`documented-gap` advisory today; flips to `proxy-enforced` when the per-run proxy ships).
- **`sandbox-fs.ts`** — In-process fs-write wrappers enforcing sandbox write boundaries: `assertWritable` two-stage guard (lexical containment + symlink resolution for macOS /var/folders parity); `writeFileInSandbox`, `appendFileInSandbox`, `mkdirInSandbox`, `rmInSandbox`.
- **`lenny-sync.ts`** — Exports `runLibrarySync()` + `LibrarySyncResult`; pulls new Lenny posts/podcasts via lenny-sync agent, updates `logs/lenny-sync-state.json`.
- **`supervision-store.ts`** — Persistent JSON store for `SupervisedRun[]` current state, including optional durable project-or-bug target identity: `readAllRuns`; `readAllRunsBounded`, which returns `{runs, complete}` and marks oversized, unreadable, malformed, or partly invalid snapshots incomplete for fail-closed model-facing authorization; `writeAllRuns`; `upsertRun` (**field-merges** `{...current,...run}` by id — project 15 P0.1 — so a keep-alive heartbeat can't clear `quietNudgedAt`; clearing a field needs `writeAllRuns`); `removeRun`. On upsert, `compactSupervisedRuns` keeps every running/parked row and only the newest terminal rows that fit a 768 KiB visibility budget; terminal history remains in work-run summaries and `mutations.jsonl`, preventing the complete-reader's 1 MiB authorization cap from becoming a permanent availability cliff. Writes are atomic temp-then-rename; the store is backed by `logs/supervised-runs.json`.
- **`supervision-recovery.ts`** — Startup recovery: `recoverSupervisedRuns(filePath)` flips stale `running` → `unknown` (legacy fallback); `recoverAndFinalizeStaleRuns(deps)` (project 15 P0.4) is the pure core that drives each stale `running` run to a real terminal state via an injected `finalizeStaleRun`.
- **`work-run-finalizer.ts`** — Shared idempotent phase-recorded finalizer (project 15): `runFinalizer(input, effects)`. `hold` mode classifies → flush transcript → write summary/index → resolve worktree (branch left intact) → terminal supervision write; NEVER merges/pushes/deletes. `gated-merge` mode (live via `runGatedMerge`): classify → flush → summary → index → gate → merge → push → delete → terminal, with crash-resume via `readLastPhase()`. See `subsystems.md` → Gated-merge finalizer.
- **`work-run-gate-runtime.ts`** — Validation runtime shared by closeout and the hard merge gate. `runValidationCommandArgv` is shell-free, passes only basic toolchain env plus a cwd-derived Vitest cache (never Rune/integration secrets), and on macOS uses Seatbelt to deny external network while allowing localhost. `buildValidationEnv` builds its base env via `getBaseEnv(DEFAULT_BASE_ENV_KEYS)` imported from `credential-injector.ts` (the former duplicate `VALIDATION_BASE_ENV_KEYS` list was deleted — one shell-basics key list, no drift between the sandboxed-agent env and validation env). Output is bounded; timeout diagnostics use the validation bootstrap below, then wait through SIGTERM→SIGKILL process-group escalation before resolving. `collectTaskChangedPaths` returns normalized/deduplicated changes plus untracked files; `taskChangesRequireFullValidation` flags deletions and global package/lock/compiler/test/build/loader config that Vitest related cannot safely map. `runValidationCommands` preserves ordered string commands; `runGate` uses a temporary integration worktree and removes its cache on teardown.
- **`worktree-sweep.ts`** — Worktree-scoped fallback reap (project 15 P2.7): `planWorktreeScopedReap` pure core selects pids whose cwd `isContainedIn` the worktree; `sweepWorktreeProcesses` SIGKILLs them — defense-in-depth for reparented grandchildren; injectable `SweepIO`; best-effort.
- **`recovery-finalize-runner.ts`** — Runtime glue (project 15 P0.4): `buildRecoveryFinalizeDeps(io?)` wires `recoverAndFinalizeStaleRuns` to config + real git/fs/stores; gated-merge crash-resume re-drives an interrupted push/branch-delete; `runRecoveryFinalize()` is the never-throws boot wrapper awaited in index.ts before the sweep. Successful recovery-finalizer removal must first invalidate the orchestrated cursor through the shared destructive-cleanup transaction; invalidation failure prevents worktree removal.
- **`orchestrated-run-store.ts`** — Durable orchestrated cursor store extracted from the runner: atomic write, shape-validated read bound to the requested run id, and atomic invalidation by replacing a resumable cursor with a non-resumable tombstone. Runner exports remain as compatibility re-exports.
- **`terminal-worktree-cleanup.ts`** — Shared destructive transaction for orchestrated terminal worktrees: synchronously invalidate the run cursor before invoking removal. Any invalidation exception prevents the removal effect, preserving the worktree fail-closed.
- **`stall-check.ts`** — Pure stall-check core: `checkStalledRuns(deps)` returns newly-nudged-id set; `formatStallNudge`/`formatQuietNudge`/`formatParkedNudge`; `TICK_INTERVAL_MS` (30s), `STALL_THRESHOLD_MS` (5min), `QUIET_THRESHOLD_MS` (5min, on `lastOutputAt`).
- **`work-dispatch.ts`** — Work-run applier dispatch seam (project 14 Phase 5): `resolveWorkDispatch(input)` maps the `resolveDispatchMode` decision to a mutation kind (`ORCHESTRATED_WORK_KIND` or `LEGACY_WORK_KIND`); `readDispatchModeInput` reads the per-product `orchestratedMode` override from products.json; a fallback always carries a recorded reason.
- **`orchestrated-work-runner.ts`** — `orchestratedWorkApplier`: `MutationApplier` for `orchestrated-work` kind (autoApprove:true, project 14 Phase 5); resolves product-scoped lifecycle projects through `work-project.ts`, creates a sandboxed worktree, drives `runProjectOrchestration` with real fs/git effects bound through `buildOrchestrationDeps`, and maps the terminal `OrchestrationResult` to a single `MutationEvent` (finalized→completed, held→completed-flagged-held, blocked→failed). Shares product+project and global caps with work-run. `OrchestratedRuntimeDeps` injection seam. Orchestrated mode is OFF by default. **Shutdown park** (docs/projects/bugs.md top bug): while `isMutationShutdownInProgress()`, `persistTerminalMutationState` returns immediately and the apply `finally` skips `destroyWorktree`, leaving the worktree in place for the parker/next-boot recovery. Exports `parkInFlightOrchestratedRuns(deps?)` (called from `index.ts`'s `shutdown()` after children are dead) + `defaultShutdownParkDeps()` + `ShutdownParkDeps`/`ShutdownParkResult` types: for each active `running` orchestrated run — system-cancel; a resumable cursor means leave it `running` for boot recovery's automatic resume; no cursor (mid-first-task) means resolve branch/worktree deterministically (`workBranchName` + `worktreePathFor`, `baseBranch` from product config), skip if the worktree is missing, best-effort WIP-commit a dirty tree, then write a `completed`+`parked:true` terminal (`operatorWorktreePath`/`branch`/`baseBranch`/`preserveBranch`/`preserveWorktree`) via `writeRecoveredTerminalMutation` — a blocked-on-human supervision row, visible-and-releasable in approvals after restart instead of orphaned-and-discarded. The WIP-commit helper (formerly `commitShutdownWip`) is now the shared `commitWorktreeWip(runGit, cwd, {message, logLabel, product, projectSlug})`, taking a caller-supplied message/log label. **Restart salvage** (restart-resume 2/2): in the apply loop's recovery branch, after `sandbox` is rebuilt from `recovery.worktreePath` and before `runOrchestration` resumes, `commitWorktreeWip` best-effort commits a dirty reused worktree as `rune(<product>): WIP — restart salvage — <project>` — so the re-run task starts from a clean tree and closeout's `git add -A` can't silently absorb the interrupted task's uncommitted dirt; the half-work stays inspectable on the branch. Shares the recovery funnel with cockpit `POST /api/work-runs/:id/recover`. See `subsystems.md` → Orchestrated work restart resumability.
  **Recovery safety:** `preflightOrchestratedRecovery` is the one fail-closed eligibility decision for boot recovery, operator recovery, shutdown preservation, and Cockpit projection. It requires mutation/cursor/product/project identity agreement, deterministic path/branch/base-branch agreement, an existing directory registered by Git on the exact branch, readable project tasks, and drift-free reconstruction from durable task records. Operator recovery acquires the lifecycle handoff claim, revalidates under protection, cancels and awaits the old `RunHandle.settled`, then revalidates once more before redispatching against the preserved worktree. Shutdown treats a cursor as resumable only when the same preflight passes.
  **Terminal disposition:** every normal terminal is resolved before its sole terminal publication. Git-proven-clean worktrees atomically invalidate their cursor before removal; dirty worktrees are parked and preserved (with a WIP commit when full resumability is verified). Status, WIP-commit, preflight, cursor-invalidation, or removal uncertainty fails closed to a parked/preserved worktree. A terminal-teardown lifecycle claim excludes concurrent recovery while the decision is in progress, and the finalizer uses the same disposition instead of publishing a conflicting earlier terminal.
  Nested role cancellation is mapped to the existing user/system terminal, rendered without blocked/model-failure wording, and copied through a scrubbed terminal record into `WorkRunSummary`; its worktree follows the same clean-remove versus dirty/uncertain-park disposition.
- **Closeout validation detail:** `vitest-related` passes changed paths argv-safely (a leading `-` becomes `./-…`) but falls back to the product's complete validation commands for deletions or global runner/config changes.
- **`team-task-deps.ts`** — Production `TeamTaskDeps` factory (project 14 Phase 8): `resolveTeamRoleModels` resolves the six product-team roles through `policies/model-policy.json` (reviewer resolved distinct-from coder.provider, fail-closed); `buildProductionTeamTaskDeps` binds all workflow seams; `createProductionTaskWorkflowRunner` is the `OrchestrationDeps.runTaskWorkflow` production binding. **Test-intent repair seam** (`techLeadRepairTests`, docs/projects/bugs.md 2026-07-08): an `execute('tech-lead')` worktree session that patches QA's tests on first rejection — repair delta computed against a pre-repair `git add -A && git write-tree` snapshot (never the scrubbed executor diff), delta paths outside `*.test.ts(x)` reverted on disk (`git restore --source`/`rm -f`; the allowlist deliberately excludes QA's diff paths — a QA stray into product source must not license a repair edit of it), then confirm-red via the product `validationCommands` (`runRepairValidation` seam, default `runValidationCommands`): green/timeout rolls the patch back to the snapshot and returns `not-repaired`; red updates `lastQaDiff` + threads `Confirm-red evidence` into the re-review body. `tl-test-review` verdicts carry optional `repairable`; new `TeamTaskSeams` members `runGit` (default `defaultRunGit`) + `runRepairValidation`. Every internal failure returns `not-repaired` — never a task-fatal throw.
  Judgment and execution bindings stamp `opKind:'agent'`, role attribution, and product scope for tech-lead/QA/coder/reviewer calls across Claude and Codex; structured provider cancellation is converted to `RoleCancellationError` instead of a normal role failure.
- **`execution-agent.ts`** — Production execution-role session runner (project 14 Phase 8): `runExecutionAgent(task, input, deps)` drives a worktree-scoped CLI turn; stage-then-diff capture; diff+errors scrubbed; returns structured `{ok:false}` on every failure path (never throws), including optional `OperationCancellation`. Every QA/coder/tech-lead execution is registered as a product-scoped `kind:'agent'` operation for both providers; the direct Claude child captures cancellation before unregistering. Injectable `{spawnAgent, runGit, buildEnv, buildArtifactMcp}` IO seam. Artifact MCP eligibility is checked from the explicit role at this boundary and is limited to `qa`/`coder`—tech-lead repair and other roles receive none. Each eligible configured invocation independently builds and tears down its broker before/after model spawn; setup/preflight failures return scrubbed `rune-kb not registered: …` evidence without consuming a role round. Both providers run under the generated outer profile and private `TMPDIR`; Codex additionally gets Codex-only private `HOME`/`CODEX_HOME`, external-sandbox bypass (never nested `workspace-write`), `shell_environment_policy.inherit="none"`, and the complete required MCP table, while Claude receives no Codex auth env and uses strict inline config. Unconfigured products preserve prior behavior.
- **`stall-check-runner.ts`** — setInterval glue: `startStallCheck(bus)`/`stopStallCheck()`; every 30s tick runs `checkStalledRuns` then `planQuietNudges`, then project-15 P2.7 escalation passes (quiet→cancel, max-runtime ceiling) and the project-13 Phase 1b `planParkedNudges` pass. Per-run errors individually caught.
- **`planning-expiry.ts`** — Pure planning-session expiry core: `findExpiredPlanningSessions(deps)` returns chatIds whose `lastActivity` exceeds the TTL; `PLANNING_EXPIRY_TTL_MS` (7 days), `PLANNING_EXPIRY_TICK_INTERVAL_MS` (1 hour); fail-toward-cleanup.
- **`planning-expiry-runner.ts`** — setInterval glue: `startPlanningExpiry()`/`stopPlanningExpiry()`; reads live in-memory state via `getAllPlanningSessions`, deletes via `deletePlanningSession`; timer unref()'d.
- **`work-run-release.ts`**, **`recovery-finalize-runner.ts`** — (see above).
- **`nudges.ts`** — Weekly and review nudge stubs.
- **`mcp-watchdog.ts`** — Pure alert-evaluation core (MCP monitoring redesign): `evaluateMcpWatchdog({now, health, windowDeltas, prev})` → `{state, notifications}`. Alert kinds: `daemon-down` (2+ consecutive unreachable ticks), `daemon-degraded` (3+ consecutive reachable-but-not-`ok` ticks), `error-spike` (≥5 errors AND ≥25% error rate over the window), `tool-failures` (≥3 errors for one tool over the window). Each alert key has a `NOTIFY_COOLDOWN_MS` (6h) re-notification cooldown while it persists, and emits one `✅ recovered: …` notice (deleting the cooldown entry so an immediate re-fire re-alerts) when the condition stops firing. No I/O, no `Date.now()` — everything comes from `input`, so the rule matrix is exhaustively unit-testable.
- **`mcp-watchdog-store.ts`** — Persistent store for `McpWatchdogState` (streak counters, active alerts, per-key notify cooldowns — current state, not an event history) at `config.MCP_WATCHDOG_STATE_FILE`: `loadWatchdogState`/`saveWatchdogState`. Tolerant on both sides — missing/corrupt/wrong-shape file loads as `defaultWatchdogState()`; a failed save is logged, never thrown.
- **`mcp-watchdog-runner.ts`** — `setInterval` glue (60s tick, `TICK_INTERVAL_MS`): `startMcpWatchdog(bus, deps?)`/`stopMcpWatchdog()`. Probes the daemon's `/health` (2s timeout; any HTTP response is reachable, a non-`ok` `status` feeds the degraded rule), reads the trailing ~15-minute `deltaSeries` window via `readMcpMetricsHistory`, calls `evaluateMcpWatchdog`, persists state, and publishes notifications to the Telegram bus (defensively `scrubAbsolutePaths`-scrubbed). Runs in the MAIN process (the daemon has no Telegram/scheduler); wired in `src/index.ts` after `startStallCheck`, torn down in shutdown. Whole tick wrapped in try/catch so a probe/store failure can't crash the process.
- **`__acceptance__/orchestrated-live.acceptance.ts`** — Project 14 Phase 8 LIVE acceptance harness (`npm run acceptance:orchestrated`): stub-free, zero-human-intervention proof that orchestrated `/work` does real work. See `subsystems.md` → Orchestrated work for the stage breakdown.

## `src/intent/`

- **`registry.ts`** — Product/project registry: `buildRegistry`, `readRegistry`/`writeRegistry`, `getAllProjects`; aggregating index (product → projects → lifecycle-status); persists to `logs/registry.json`.
- **`registration.ts`** — Product registration: `planRegistration`, `planReconciliation`, `applyRegistration`; propose-and-approve flow — planning is pure and never writes.
- **`overlay.ts`** — Product-overlay index: `buildOverlayManifest`, `scopedRetrieval`, `findStalePointers`; per-product pointer manifest into the type-organized vault.
- **`cockpit.ts`** — `buildCockpitView(registry, runStatus, taskProgress?, workRuns?, backlogCounts?, dispatchModes?)`: pure projection of registry + supervision run-status + optional task progress + work-run projections + backlog counts + dispatch modes into `CockpitView`. Null registry yields `available:false`. See `subsystems.md` for the full field catalog.
- **`product-deep-view.ts`** — Pure product-detail projection over registry, backlog, run summaries, active mutations, planning, and monitoring. Project run controls carry `recoverable` only when the server-enriched active mutation explicitly sets it true, so the client cannot infer recovery eligibility from mutation kind.
- **`journal-intent.ts`** — `planJournalIntent(input)`: deterministic journal-to-intent planner; routes into `IntentProposal` discriminated union (vault-intake / roadmap / register-product / disambiguation); pure.
- **`journal-intent-producer.ts`** — `scanJournalForIntent(content)` + `runJournalIntentProducer(...)`: scan → planJournalIntent → derive SHA-256 sourceNoteId → dedupe against existing queue; pure.
- **`journal-intent-consumer.ts`** — `actionApprovedIntentProposal(proposal, deps)`: dispatch core via injected `ConsumerDeps`; VALID_SLUG-guards the product; compile-time exhaustive switch.
- **`journal-intent-actions.ts`** — `realConsumerDeps`: vault-intake appends a journal-sourced bullet to `projects/<product>.md`; `appendRoadmap` and `registerProduct` throw "wire-up deferred".
- **`intent-proposal-queue.ts`** — Journal-to-intent proposal queue CRUD over `logs/intent-proposal-queue.json`.
- **`agent-def.ts`** — Model-agnostic agent definitions: `NeutralAgentDef`, `parseClaudeAgent`, `compileToClaude`, `compileToCodex`, `compileToGemini` (deferred stub). **Invariant:** `compileToCodex` names no model and sanitizes name against newline-injection.
- **`model-policy.ts`** — Model selection policy: `parsePolicy`, `loadModelPolicy`, `resolveModel`; deterministic resolver (pin → role-default → global-fallback); policy from `policies/model-policy.json`; cached per path.
- **`escalation.ts`** — Escalation policy: `parseEscalationPolicy`, `decide`, `decideFailClosed`; deterministic; **fail-closed** — a missing/malformed policy escalates.
- **`planner.ts`** — Planning state machine: PM-spec approval lifecycle — `startPlanning`/`proposeSpec`/`approvePlan`/`abandonPlan`/`isScaffoldReady`/`buildSetupWriterBrief`; the single human gate is approval of the PM spec.
- **`gen-eval-loop.ts`** — Generator-Evaluator loop (Layer 2): `recordRound` + `evaluateLoop` (bounded; pass → on-branch, N failed rounds → escalated).
- **`supervision.ts`** — Supervision (Layer 3): visibility surface — `isStalled`, `getVisibility`, `markCrashed`, `recoverRun`, `recordHeartbeat`. `SupervisedRun.target?` preserves the durable user-facing project-or-bug identity (absent on legacy rows). Two output fields (`lastOutputAt?`, `quietNudgedAt?`) + `isQuietRun`/`planQuietNudges`; project 13 Phase 1b `parkedNudgedAt?` + `isParkedRun`/`planParkedNudges`. `parkedQuestion?` carries durable `AskUserQuestion` metadata for question-parks. Parked predicates never fire on `running` runs.
- **`run-target.ts`** — Canonical resolver for a work run's durable user-facing `WorkRunTarget` discriminated union (`project | bug`). Prefers an explicit payload target, supports legacy `bugId`/`projectSlug`/`ref` payloads, and supplies the same identity to supervision, run-feed events, and terminal summaries so bug fixes are not relabeled as projects after restart.
- **`sandbox.ts`** — Sandboxing (Layer 4): `worktreePathFor` (slug-validated), `workBranchName` (deterministic branch name — moved here in project 13 Phase 1c so consumers like `work-run-release.ts` can import it without the Claude-CLI-spawn chain), `writingBranchName`/`writingSlugFromBranch` (the writing engine's `rune-writing/{slug}` siblings — deliberately not a prefix param on `workBranchName`), `isWriteAllowed`, `isEgressAllowed` (exact-match allowlist), `canReachCredential`; exports `VALID_SLUG` and `isContainedIn(root, target)`.
- **`dispatch.ts`** — Multi-model dispatch (Layer 5): `DispatchTarget`, `DispatchProvider`, `DispatchHandoff`, `DispatchResult`, `DispatchLogEntry`, `buildHandoff`, `recordDispatch`. **Invariant:** handoff `context` must never carry vault personal content when target is `codex`.
- **`adjudication.ts`** — Cross-model adjudication: `ReviewMode`, `Adjudication`, `MergeOutcome`, `resolveReviewMode`, `isCrossModel`, `evaluateMergeContract` (fail-closed, ordered gates).
- **`scheduler.ts`** — Concurrency scheduler: `schedule(running, queue, globalCap)` — global cap + per-product cap of one, FIFO walk, queued projects never dropped.
- **`observation-*.ts`** — Observation loop (§16): `observation-loop.ts` (core, `LoopOutcome` discriminated union), `observation-sensor.ts` (sensor-layer composer; **invariant:** `detail` carries only structured metadata, never raw user content), `observation-synthesis.ts`, `observation-triage.ts`, `observation-dispatch.ts`, `observation-nightly.ts`. See `subsystems.md` → Observation loop.
- **`friction-detect.ts`** — `FrictionSignal`/`AggregatedFriction`; `aggregateFrictions(raw)` dedupe-by-id with occurrence count.
- **`product-routing.ts`** — Product routing for captured ideas/bugs (project 16 R3): `resolveProductTarget(candidate, loadKnownProducts)` — exact (never fuzzy) match, unresolved → reserved `INBOX_PRODUCT` (`inbox`) fallback with typed reason; never throws.
- **`backlog-id.ts`** — `computeBacklogId`, `normalizeBacklogRaw`, `BacklogKind` — pure.
- **`backlog-parser.ts`** — Strict pure parser for `docs/projects/{bugs,ideas}.md`: `parseBugs`, `parseIdeas` → `{ items, fileWarnings }`.
- **`backlog-reader.ts`** — Filesystem + security layer: `readBacklogs(...)` realpaths each repoPath + requires it under `$WORKSPACE_ROOT`, rejects symlink escapes, fd-based size-capped reads; `computeBacklogCounts`.
- **`backlog-append.ts`** — Pure append core for the drawer `+` add: `appendBug`/`appendIdea` → `{ ok, content }` | error.
- **`backlog-write-lock.ts`** — Backlog safe-write substrate: `withFileLock(key, fn)` per-key async mutex, `writeFileAtomic`, `assertBacklogWriteAllowed`, `appendBacklogMutationLog`. Project 23 adds `ALLOWED_TOPIC_FILES` (`writing-ideas.md`, `research-topics.md`) + `assertScopedTopicWriteAllowed(repoPath, scopePath, absPath)` — a separate scoped-topic write guard (target must resolve to exactly `<repoPath>/<scopePath>/<basename>`) sharing the ancestor-realpath containment tail (`assertResolvesInsideRepo`) with `assertBacklogWriteAllowed`; deliberately not folded into the backlog-file allowlist so it doesn't legalize those paths for every existing call site.
- **`note-triage.ts`** — Nightly note-triage pure core (project 23), no I/O/config: `parseNoteTriageOutput` validates agent JSON output (fence-strip, drops malformed items, single-line discipline via `toSingleLine`, caps at `MAX_ITEMS_PER_PASS` = 20); `routeNoteItems` deterministically routes each item to a `NoteFilePlan` — re-validates every LLM-claimed product via `resolveProductTarget` (never trusts the agent's product field), an idea for the writing product coerces to a writing topic, an unroutable bug files to the vault with a `[Bug — unrouted]` title marker, an unknown/unregistered product falls back to the vault new-product path; `extractProjectPageHints` scans journal `[[wikilink]]`s against vault `projects/*.md` pages to hint the agent's product classification; `appendVaultIdeaBlocks` inserts `### Title` blocks at the end of the vault ideas file's `## Ideas` section (before `## Supersession audit`), `appendTopicLines` seeds (on missing file) or appends dedupe-checked topic lines to the writing product's topic files (cockpit-parseable bullet format).
- **`scaffold-target.ts`** — `resolveScaffoldTarget(product, registry, productsConfig)` → resolves a product's repoPath so project-setup-writer scaffolds into the TARGET product's repo; `scaffoldWriteScope(repoPath)`. Pure.
- **`scaffold-result.ts`** — `parseScaffoldResult(message)` + `crossCheckScaffold(parsed, newProjectDirs)` → `ScaffoldCheck`. Pure.
- **`planning-roles.ts`** — Downstream planning orchestration: `runDownstreamPlan(approvedSpec, deps)` turns the approved PM spec into tech-spec/tasks/test-plan/context through tech-lead breakdown, PM review match, Claude critique, Codex critique, and context seed; warnings/progress are streamed and no tech-spec/tasks approval gate is added.
- **`planning-critique.ts`** — Planning critique pass core (project 14 Phase 9): pure `runPlanningCritique(plan, deps)` — Rune-owned NEUTRAL cross-model hardening step; SEQUENTIAL (Claude then Codex compounds); fail-closed.
- **`planning-roles-wiring.ts`** — Thin production bridge: `buildPmRolePrompt`/`buildTechLeadRolePrompt`, `extractFencedJson`, `buildProductionCritiquePlan`, `parseCritiqueReply`, `defaultPlanningRoleDeps()`.
- **`project-context.ts`** — context.md section schema + planning-time seed: `CONTEXT_SECTIONS` (five required sections), `seedProjectContext`, `hasRequiredSections`, `escapeRegExp`. context.md is Rune-owned orchestration state, not role memory.
- **`context-curator.ts`** — Rune-owned context.md post-task updater (project 14 Phase 3): `applyContextUpdate(current, update)`; ordered gates; roles emit handoff notes threaded into Next Task Handoff. **The ONLY writer of context.md.** Pure.
- **`orch-task-select.ts`** — `selectNextTask(tasksMd)` returns first unchecked `- [ ]` with a TEXT-STABLE id via `computeTaskId`. Pure.
- **`orch-closeout.ts`** — `markSelectedTaskComplete(tasksMd, task)` ticks exactly the selected checkbox; refuses stale/ambiguous. Pure.
- **`orch-reconstruct.ts`** — `reconstructRun({tasksMd, records})` rebuilds partial run state from durable records; flags drift. Pure.
- **`orch-context-assembly.ts`** — `assembleTaskContext(input)` builds the fresh-context handoff bounded to `TASK_HANDOFF_MAX_CHARS` (24000). Pure.
- **`orch-run-record.ts`** — `TaskRunRecord` type + `buildTaskRunRecord`. Pure.
- **`orch-attempt-cap.ts`** — `decideAttemptOutcome(input)` → `AttemptAction`; precedence: open objection → blocked-on-human; success → proceed; below cap → retry; at cap → pm-wrapup. Pure.
- **`finalizer-handoff.ts`** — `FinalizerHandoff` payload + `buildFinalizerHandoff` + `runFinalizerHandoff(handoff, adapter)`; finalized → outcome, unavailable → held (no self-merge). Pure dispatch.
- **`orch-config.ts`** — `resolveDispatchMode(input)` → orchestrated | legacy with a `fallbackReason` on every legacy path. Pure.
- **`promotions.ts`** — Durable promotion job (09-expand-cockpit Phase 4): drives a backlog item through planning-started → scaffolded → marked-source | planning-abandoned | scaffold-error | mark-source-error. Pure state machine + append-only JSONL persistence at `config.PROMOTIONS_FILE`. **`appendPromotion` THROWS on disk failure** — this log is the restart-replay source of truth, not best-effort. `loadPromotions`/`resumablePromotions` for restart re-drive. **Invariant:** `snapshotRaw`/`errors` carry personal/agent text — never forward to client surfaces.
- **`backlog-mark-done.ts`** — `markBacklogItemDone(content, kind, snapshotRaw, slug)` → `MarkDoneResult`; matches by SNAPSHOT text (stable across edits); idempotent on retry; CRLF-tolerant; preserves every other byte. Pure.
- **`team-task-workflow.ts`** — Team-task workflow (project 14 Phase 4): pure orchestration over injected role seams (`TeamTaskDeps`). `runTeamTaskWorkflow(task, input, deps)` runs one task through role gates (reviewer-independence → QA-first → tech-lead test review → bounded coder→reviewer loop → objection gate → tech-lead diff review → designer IFF needed → PM wrap-up → `TaskEvidence`). `RoleCancellationError` carries role + shared operation metadata through best-effort repair/learning catches; the outer boundary returns `outcome:'cancelled'`, while ordinary exceptions retain `failed`. Never writes tasks.md/context.md/merges — Rune owns closeout. **Test-intent repair** (docs/projects/bugs.md 2026-07-08): on the first `test-intent` rejection the loop invokes the optional `techLeadRepairTests` dep (skipped for `repairable:false` verdicts and no-code-test rationales; attempted once per task) — a `repaired` result re-reviews to approve-or-bounce, `not-repaired`/throw degrades to the QA bounce with the repair outcome appended to `actionableNotes`; the attempt emits a `test-repair` activity event and lands as `TaskEvidence.testIntentRepair` on every terminal. The `ORCHESTRATED_ROUND_CAP` backstop is unchanged.
- **`project-orchestrator.ts`** — Multi-task project orchestrator loop (project 14 Phase 5): pure over injected `OrchestrationDeps` — `runProjectOrchestration(deps)` drives tasks to completion; never self-merges; `maxIterations` bound. Nested role cancellation maps to the existing cancellation terminal before closeout: Telegram/cockpit → `reason:'user'`, internal → `reason:'system'`; the selected task remains unchecked. **Restart resumability (restart-resume 1/2):** writes a best-effort task-start cursor right after task selection, before the workflow runs (`persistTaskStartCursorSafely` — `commitWipSafely` precedent, a failed write must not hold a run that has done no work), in addition to the fail-closed closeout checkpoint (`persistRunCheckpoint`). `buildRunCursor` takes an optional `currentTask` arg: non-null `cursor.currentTaskId` marks a task-start cursor (task in-progress/untrusted); closeout cursors keep `currentTaskId: null`. Makes a restart resumable from any point after the first task selection, not only after the first closeout. See `subsystems.md` → Orchestrated work restart resumability.
- **`feedback-record.ts`** — Feedback-record schema + validator (project 14 Phase 6): `FeedbackRecord`, `RoleStage`/`ROLE_STAGES`, `parseFeedbackRecord(raw)` → `FeedbackValidation` (fail-closed, durable skip reason; trust-boundary length caps). Pure.
- **`feedback-reader.ts`** — Production feedback reader: `readFeedbackRecords`, `feedbackRecordId` (SHA-256 content-hash), `readProcessedFeedbackIds`/`writeProcessedFeedbackIds` (JSON Set, atomic write).
- **`learning-loop.ts`** — Learning-loop composer (project 14 Phase 6): `runLearningLoop(deps)` pure orchestration over injected `{readFeedback, attribute, writeLesson}`; routes lesson/no-lesson attribution.
- **`postmortem.ts`** — Rune-owned post-mortem (project 14 Phase 6): `buildPostMortemPrompt`, `parsePostMortemResult` (fenced `postmortem` JSON; role/stage validated), `runPostMortem` (fails SAFE to no-lesson — never fabricates a lesson).

## `src/mcp/`

- **`artifact-readonly.ts`** — Privileged read-only artifact MCP broker. Receives its validated vault path over stdin (never argv/env), scrubs the already-minimal environment before dynamically importing Rune modules, restores only `VAULT_DIR`, and does not fabricate Telegram settings. It builds the in-memory vault index with symlink targets constrained to the real vault root and fails startup if indexing is not ready. It listens on the parent-created Unix socket and gives each relay connection an MCP server exposing exactly `vault_search`, `journal_range`, and `follow_wikilinks`; it never runs `initKB()`, initializes vault scaffolding, or exposes admin/logging/health/mutation tools. SIGTERM, SIGINT, or parent IPC disconnect destroys all accepted sockets before closing the listener, so a held relay cannot keep it alive.
- **`artifact-readonly-relay.ts`** — Model-facing stdio-to-Unix-socket relay for artifact MCP. Its argv contains only the temporary broker socket path; it pipes MCP bytes between model stdio and the privileged broker without receiving the vault path or importing Rune configuration.
- **`product-chat-tools.ts`** — Shared identity for the dedicated product-chat MCP surface: server name, scope environment key, and exact seven-tool list (four repo/KB reads plus three Cockpit run diagnostics).
- **`product-chat.ts`** — Dedicated stdio entrypoint for one configured product chat. Requires server-owned `RUNE_PRODUCT_CHAT_PRODUCT`, imports the server only after validating scope, creates `createProductChatServer(product)`, and—because operator-only Telegram config is lazy—does not need real or dummy bot credentials. It does not join the normal local or remote surfaces.

- **`daemon.ts`** — Standalone MCP daemon entrypoint (`npm run mcp:start`): starts a minimal HTTP server on `RUNE_MCP_HOST:RUNE_MCP_PORT` with daemon `/health`, OAuth metadata/register/authorize/token routes, and Streamable HTTP MCP at `/mcp`; does not boot Telegram, cockpit/webview routes, Whoop OAuth, scheduler, or watchers. Exports `startMcpDaemon(opts)` for lifecycle tests. `buildStatus()` (`/health` body) is enriched (MCP monitoring redesign) with `startedAt`, `bootId` (`getBootId()`), and per-session `sessions` (`McpSessionStat[]`) alongside the existing `activeSessions`/`warmIndex`. The daemon's tool set now includes `HEALTH_TOOLS` (20 tools total: App-surface + content + health/workout + utility). Starts `startMcpMetricsFlush` (writes `config.RUNE_MCP_METRICS_HISTORY_FILE` once the HTTP surface is listening) and stops it in `stop()`.
- **`server.ts`** — Shared MCP server factory (project 16): `createRuneMcpServer({tools, name?, productScope?})` builds an independent `McpServer` per call from a `TOOL_REGISTRY`. Exports App, admin, content, utility, health, and product-chat tool sets plus `ToolName`; `createKBServer()` remains the 15-tool local admin/health surface. `createProductChatServer(productScope)` validates server-owned scope and exposes exactly the four read-only repo/KB tools plus `cockpit_list_runs`, `cockpit_inspect_run`, and `cockpit_active_runs`; scoped `kb_query` injects `agentUserVisible:false`. The diagnostic registrations are read-only/non-destructive/closed-world and are absent from normal local, artifact, and remote surfaces.
- **`metrics.ts`** — In-memory MCP call metrics: wraps tool handlers with call/error/timeout counters and bounded per-tool latency windows; `mcp_metrics_snapshot` reads this process-local state, so metrics reset on daemon restart. `TOOL_TIMEOUT_OVERRIDES_MS` lets one tool win over the env-var-driven default timeout — current entries are the agent-spawning tools `generate_workout: 240_000` and `kb_query: 180_000`.
- **`metrics-history.ts`** — Daemon-side MCP metrics persistence (MCP monitoring redesign): `startMcpMetricsFlush(opts)` is the daemon's SOLE writer of `config.RUNE_MCP_METRICS_HISTORY_FILE` (JSONL), appending one cumulative-counter record per flush tick (default 60s) even when idle, so a timeline gap reliably means "daemon down"; daily atomic-rename compaction drops records past `retentionDays` (default 14). `getBootId()` — one `randomUUID()` per daemon process, stamped on every record and echoed on `/health`. `readMcpMetricsHistory(file, opts?)` — tolerant JSONL reader (missing/corrupt lines skipped). `deltaSeries(records)` — turns consecutive records into restart-aware per-interval `DeltaPoint`s; a `bootId` change or counter regression marks a restart boundary (the newer record's own cumulative values ARE the delta). Shared contract consumed by both `src/server/mcp-metrics-history-read.ts` (webview) and `src/jobs/mcp-watchdog-runner.ts` (watchdog).
- **`tools/`** — Per-tool handler modules, deps-injected. `types.ts` (shared `McpTextResult` + helpers, config-free), `sanitize.ts` (`sanitizeMcpError = redactSecrets∘scrubAbsolutePaths`), `cockpit-runs.ts` (shared adapter for the three Cockpit registrations: builds the product-scoped diagnostics service, dispatches list/inspect/active, serializes success, sanitizes errors), `cockpit-runs-deps.ts` (production-only binding to bounded summary/complete-supervision/task/transcript readers), `log-idea.ts` (PURE), `log-conversation.ts` (PURE), `read-tools.ts` (PURE `vaultSearch`/`crmLookup`/`getPriorities`), `journal-range.ts` (inclusive journal date-range JSON, 31-day cap, cold fallback only while the index is warming), `follow-wikilinks.ts` (warm-index-only Obsidian link traversal, depth/result caps), `tag-date-query.ts` (warm-index tag/date filtering, 50-result cap), `vault-index-tools.ts` (`refresh_vault_index` status wrapper), `health-read.ts` (PURE `whoop_snapshot`/`health_trends`/`workout_history`/`nutrition_log`/`health_doc` — config-free, deps-injected, never throws), `generate-workout.ts` (PURE `generate_workout` handler wrapping the injected `generate()` pipeline call), `log-workout-done.ts` (PURE `log_workout_done` — 48h staleness gate mirroring `/done-workout`, `confirm_stale` override), `log-meal.ts` (PURE `log_meal` + exported `insertMealLine` insertion logic — newest-date-first `## Meal Notes` sections, dedupe-by-exact-line), `update-workout-plan.ts` (PURE `update_workout_plan` — full-content replace with a stamped `> Updated … via MCP: …` footer, prior footers stripped on round-trip), `*-deps.ts` (production bindings — pull config, loaded only via dynamic import inside each handler; the five health/workout `*-deps.ts` files additionally bind `gitCommitAndPushOrThrow` for their write tools, and `generate-workout-deps.ts` binds `runAgent('workout-generator', …)` at a 210s timeout under the tool's 240s wrapper override).
- **`index.ts`** — Standalone stdio entry point for Claude Code; calls `initKB()` before `createKBServer()` (now `ADMIN_TOOLS + HEALTH_TOOLS`).

## `src/study/`

- **`sr-state.ts`** — Spaced-repetition state engine: read/write `study/spaced-repetition.json`, interval-ladder transitions.
- **`sr-pool.ts`** — SR pool source: `readPool()` reads `study/sr-seed.json`.
- **`sr-select.ts`** — `selectDueConcepts()` — due concepts, most-overdue first, capped at N.
- **`sr-session.ts`** — SR session orchestrator: `runSRSession()`/`handleSRMessage()` — event-driven question→grade→advance loop.

## `src/health/`

- **`workout-generation.ts`** — Workout-generation pipeline: `buildWorkoutPrompt` (goals/equipment/exercises/recent-workouts/Whoop-recovery/plan prompt bundle) + `generateWorkout(args, opts?)` (pre-syncs Whoop, runs the workout-generator agent via `runAgent`, persists `logs/last-workout.json` atomically on success). `GenerateWorkoutOpts` (`userVisible?`, `agentTimeoutMs?`) lets a caller run the agent non-user-visibly with a custom timeout — the MCP `generate_workout` tool passes `{userVisible:false, agentTimeoutMs:210_000}`. Extracted (behavior-preserving) from `src/bot/commands/workout.ts` so the Telegram `/workout` command and the MCP tool share one entry point.
- **`last-workout.ts`** — Shared reader/formatter for `logs/last-workout.json`: `readLastWorkout()` (`ok`/`missing`/`corrupt`, shape-validated), `formatBlock(entry)` (builds the `#workout` journal block the nightly pipeline parses into `health/workouts.json`), `clearLastWorkout()` (best-effort delete after a successful log — never throws). Extracted from `src/bot/commands/done-workout.ts` so Telegram `/done-workout` and the MCP `log_workout_done` tool stay consistent.

## `src/integrations/`

- **`telegram/client.ts`** — Message chunking, typing indicators.
- **`whoop/types.ts`** — Whoop API response types and daily data format.
- **`whoop/keychain.ts`** — macOS Keychain token storage via `security` CLI.
- **`whoop/client.ts`** — OAuth2 token management + Whoop API calls.
- **`readwise/client.ts`** — Save articles to Readwise Reader API.

## `src/workspace/` & `src/vault/`

- **`workspace/files.ts`** — Read/write/append/list workspace files (`assertWithinWorkspace`-guarded); mirrors `vault/files.ts` but rooted at `WORKSPACE_DIR ?? PROJECT_ROOT`.
- **`vault/files.ts`** — Read/write/append/list vault markdown files (`assertWithinVault`-guarded).
- **`vault/journal.ts`** — Journal file creation, append, `writeMorningPrep`; also `saveConversationSource` (KB raw-source conversation writer); re-exports `parseTag` from journal-parse.ts.
- **`vault/journal-parse.ts`** — Pure CONFIG-FREE journal parsing: `parseTag` (extracted so pure modules like read-tools MCP handlers can reuse it without config.ts's env requirements).
- **`vault/learnings.ts`** — `/learn`-authored JSONL store + prompt-prepend builder for `runAgent`.
- **`vault/git.ts`** — git add/commit/push helpers; `gitCommitAndPush` (best-effort) and `gitCommitAndPushOrThrow` (strict — throws on not-on-main/commit/push failure; `nothing-to-commit` benign) share one core. **On-`main` branch guard at the single chokepoint.**
- **`vault/sessions.ts`** — Transport/scope-keyed chat session Map with JSON persistence + crash recovery. Defines the provider-neutral `ChatAuthority` (`read-only` or `product-full-access`); Codex executor records bind it to the thread's cwd and scoped root while retaining legacy `writeEnabled` parsing compatibility. Pre-provider records migrate as Claude-backed using their legacy id; new records begin unbound. Replacing or deleting an executor cleans the corresponding Claude session or Codex rollout files best-effort. The full-access product identity prompt names the three scoped Cockpit diagnostics and explicitly forbids direct Rune-log reads.
- **`vault/equipment.ts`** — `readEquipment()` parses `health/equipment.md` into `{home, gym}` raw blocks.
- **`vault/whoop-recent.ts`** — `readRecentWhoopDays(n)` returns last n parsed `WhoopDailyData`; `readWhoopRange(start, end)` returns the inclusive `[start, end]` (`YYYY-MM-DD`) date-range equivalent, newest-first (added for the MCP `health_trends` tool). Both share a `parseWhoopDayFiles` helper that skips corrupt/shape-invalid files.
- **`vault/workouts.ts`** — `readRecentWorkouts(days)`: typed reader for the agent-written training log `health/workouts.json`, filtered to the trailing `days` days, newest-first; a missing/corrupt/non-array file yields `[]`. Extracted from `src/bot/commands/workout.ts` so the MCP `workout_history` tool and the workout-generator prompt builder share one parser.
- **`vault/watcher.ts`** — FSWatcher for Readwise article detection, TG notify + enqueue.
- **`vault/voice.ts`** — `buildVoicePromptSection()` re-reads `writing/voice.md` on every call (no cache); truncated at `VOICE_PROMPT_CHAR_BUDGET` (8000 chars).

## `src/writer/` & `src/roles/`

- **`writer/memory.ts`** — Writer-role memory loader (project 12): `composeWriterContext(baseInstructions)` reads SOUL.md (system-prompt authority) + memory.md (budget-trimmed, low-authority reference) from `PROJECT_ROOT/agents/writer/`; SOUL prepended on the system channel; memory rides the first user turn inside a `<writer-memory>` fence; `WRITER_MEMORY_CHAR_BUDGET` (14000).
- **`writer/seed.ts`** — Seed-mining contract (project 12): pure helpers for the one-time memory.md baseline mine.
- **`writer/sentinel.ts`** — Writer completion sentinel (project 12 Phase 2): `WRITER_COMPLETION_SENTINEL` `[[WRITER_MEMORY_COMPLETE]]` + `detectCompletionSentinel(text)`; only a FINAL-line sentinel counts.
- **`writer/capture.ts`** — TS-owned lesson capture (project 12 Phase 2): `parseCandidateBlock`, `isLessonPrivacySafe` (rejects private names / links / URLs / email / phone / long quotes), async `captureLessons` (feedback gate → privacy filter → dedupe → provenance-stamp → append → one atomic commit). Injection seams.
- **`writer/commit.ts`** — Memory-scoped commit helper (project 12 Phase 2): `commitWriterMemory({cwd?, message})` refuses to commit off `main`, pathspec-stages + commits ONLY `agents/writer/memory.md`; no push.
- **`roles/loader.ts`** — Product-team role loader (project 14 Phase 1): generalizes the writer loader to six fixed roles — `RoleName` (`pm`/`tech-lead`/`qa`/`coder`/`reviewer`/`designer`), `ROLES_ROOT` (=`<repo>/agents`), `composeRoleContext(role, baseInstructions, opts?)`; same two-channel authority boundary as writer/memory.ts.
- **`roles/memory-writer.ts`** — Role-memory lesson writer (project 14 Phase 6): `writeRoleLesson(input)` — privacy-filter → dedup → provenance-stamp → append → `commitRoleMemory`; serialized per-role via a `writeChains` Map.
- **`roles/commit.ts`** — Role-memory-scoped commit helper (project 14 Phase 6): `commitRoleMemory({role, message, cwd?})` stages ONLY `agents/<role>/memory.md`; refuses to commit off main; no push.

## `src/utils/`

- **`time.ts`** — America/Chicago timezone helpers (`getTodayFilename`, `getYesterdayFilename`, `getTimestamp`, `getDayOfWeek`, `getRecentFilenames`, etc.).
- **`toolchain-path.ts`** — `buildToolchainPath(inheritedPath?)`: launchd-safe PATH builder shared by every child-process spawn surface — prepends the running Node's own bin dir plus the Homebrew/system dirs that exist on disk, then dedupes against the inherited PATH. Moved here from `src/jobs/credential-injector.ts` (project history: it has no secret-handling role and is consumed by both sandboxed and non-sandboxed spawn paths). Consumers: `credential-injector.ts`'s `getBaseEnv` (routes the `PATH` key through it), `work-run-gate-runtime.ts` (validation env), `work-runner.ts` (legacy `/work --auto` spawn env).
- **`logger.ts`** — Structured JSON logging with component tags.
- **`redact-secrets.ts`** — Shared output redactor: removes exact values of token/secret/password/key/credential/cookie/auth-shaped environment variables (longest first), then applies credential-pattern redactions; exact values are injectable for deterministic tests.
- **`sanitize-paths.ts`** — `scrubAbsolutePaths(raw)`: replace `VAULT_DIR`/`PROJECT_ROOT`/`WORKSPACE_DIR`/the user's home directory with `<vault>`/`<project>`/`<workspace>`/`<home>` before surfacing a message to a user. Reads config at call-time.
- **`intent-log.ts`** — Ask-Twice telemetry: `appendIntent` → `logs/intent-log.jsonl`.
- **`observation-log.ts`** — Observation loop interaction writer: `appendInteraction` → `logs/observation-interactions.jsonl`. **Invariant:** `detail` carries only structured metadata, never raw user content.
- **`markdown.ts`** — Markdown parsing utilities (future).

## Root tooling, `cli/`, `scripts/`, `policies/`

- **`vitest.config.cjs`** — CommonJS Vitest config, deliberately avoiding Vite's ESM config-bundle write under shared `node_modules/.vite-temp`; preserves the suite include/setup/timeout/reporters/coverage contract and sets top-level `cacheDir` from `RUNE_VITEST_CACHE_DIR`, falling back to `node_modules/.vite` for ordinary local runs.
- **`cli/rune.ts`** — CLI entry point for local interactive use.
- **`scripts/run-evals.ts`** (+ `.test.ts`) — Dev tool: parse eval YAMLs, invoke agents via `runAgent()`, report pass/fail.
- **`scripts/register-ts.mjs`** (+ `.test.ts`) — Local Node runtime TypeScript loader using `module.registerHooks()`: resolves repo-local `.js` ESM imports to sibling `.ts`/`.tsx`/`.mts`/`.cts` files and transforms TypeScript through `esbuild` for app/script entrypoints.
- **`scripts/validation-report-bootstrap.cjs`** — `NODE_OPTIONS --require` bootstrap for validation timeout reports. Passes report-on-signal flags through npm/npx to the direct runner, then restores/removes them and private handoff variables before Next/Vitest workers are created.
- **`scripts/run-intent-scan.ts`** — CLI entry point for intent-scan job (`npm run intent-scan`).
- **`scripts/library-backfill.ts`** — CLI entry point for bulk library-to-KB backfill (`npm run library-backfill`).
- **`policies/model-policy.json`** — Declarative model registry + routing rules (aliases, providers, role-defaults, global-fallback). Committed config. Carries fable + opus (anthropic/claude) and gpt-5.6-sol + gpt-5.6-terra (openai/codex) with product-team roleDefaults: pm/tech-lead → fable; reviewer/designer → opus; qa → gpt-5.6-terra; coder → gpt-5.6-sol. Also declares `planning-critique` → gpt-5.6-sol, `evaluator` → gpt-5.6-terra, and `writer` → opus (writing pipeline model stages; must stay claude-format).
- **`policies/escalation-policy.json`** — Declarative escalation rules; the decision module fails closed on a missing/malformed file.
- **`policies/products.json`** — Per-product config: repo path, base branch, credentials file, egress allowlist, optional `orchestratedMode` boolean, optional `closeoutValidationStrategy` (`vitest-related` or default `product-commands`; Rune/Rune-MCP opt into related tests), and optional artifact-role MCP policy (`artifactMcp: "rune-kb-readonly"`; enabled for `writing` and `brand`); read by `sandbox-runtime.ts`; exposed via `config.PRODUCTS_CONFIG_FILE`.
