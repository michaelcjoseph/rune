# Warm Index Fixture

PARITY_KNOWLEDGE_MARKER proves knowledge coverage at line granularity.

The regex fixture includes literal.token(42) so escaped metacharacters behave like rg.

literal[unclosed appears here for invalid-regex literal fallback coverage.
